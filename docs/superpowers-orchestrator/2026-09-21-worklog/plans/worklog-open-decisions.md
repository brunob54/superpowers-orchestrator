# Open decisions — worklog

## Ruling 1 — 2026-09-21 — phase 4 — [L12] close and a deleted heading

- **Class:** forced
- **Item:** [L12 inv 1] n/a skills/worklog/SKILL.md:307 — close writes under Accepted limits and Decisions but has no instruction for a heading the user deleted; repairs run in update only (two reviewers recommend user-decision)
- **Contract clause:** "The repair of a missing heading or `Next item number` line runs in `update` only; `close` does not repair." — docs/superpowers-orchestrator/2026-09-21-worklog/plans/worklog.md, Task 3
- **Defensible answers:** n/a
- **Forks:** none; contradiction: none
- **Resolution:** fix it: before `close` writes anything, it checks that every section heading it reads or writes (`## Parts`, `## Open items`, `## Accepted limits`, `## Decisions`) stands; when one is missing, `close` stops, names the missing heading and tells the user to run `/worklog update`, which repairs it; `close` itself never adds a heading or the `Next item number` line — The spec allows the repair of a missing heading in `update` only (spec "Commands", line 430) and `close` step 2 writes under `## Accepted limits` and `## Decisions`, so with such a heading gone the only outcome that neither repairs inside `close` nor leaves `close` without an instruction (where a model could improvise the forbidden repair, or delete open-item rows without recording them) is a stop that names the heading and sends the user to `update`; the clause is a "Does NOT cover" line, which the plan's `**Body authority:**` note makes reference text, so `fix it` is valid.

## Ruling 2 — 2026-09-21 — phase 4 — [L18] a symbolic-linked docs folder is followed

- **Class:** design
- **Item:** [L18 inv 1] n/a skills/worklog/SKILL.md:112 — a symbolic-linked `docs` folder is followed by find, so the notice can name a file outside the clone; the spec's accepted limit names only a symbolic-linked docs/worklogs (two reviewers recommend user-decision)
- **Contract clause:** "[Discovery through the session-start hook] 'Run the list command, unchanged: with its folder test, its `|| true` and its file-name filter." — docs/superpowers-orchestrator/2026-09-21-worklog/plans/worklog.md, Global Constraints
- **Defensible answers:** (1) plan governs on Global Constraint 11, the list command and its hook copy stay as they are; (2) amend plan: Global Constraint 11 gains a sentence naming a symbolic-linked `docs` folder as an accepted limit, fix it: the skill's and the guide's accepted-limit text name it; (3) escalated (spec wrong): change the list command so that it does not follow a symbolic-linked `docs` folder, which changes the command text the spec pins.
- **Forks:** 3 of 3 — design consistency: VERDICT 1, plan governs on Global Constraint 11; implementation practicality: VERDICT 2, amend plan plus a text-only fix; adversarial: VERDICT 1, plan governs, the limit reported at Phase 5; contradiction: implementation practicality against the other two, settled on the merits — Global Constraint 11 holds only spec quotes, so a documentation sentence there would be a rule the plan invents; the same symbolic-link class in the check, line-1 and `new` commands is already carried as Minor (round 4 [M3]); and the practicality lens itself conceded that the layout is unusual and that leaving it undocumented loses no work and makes no wrong commit. Tabled by the forks and not taken: a carried ledger `Minor:` line (the skill gives the orchestrator no such write outside Resume step 3) and a line under `## Accepted limits` of `docs/orchestration-issues.md` (that file tracks the orchestration pipeline, not this feature).
- **Resolution:** plan governs: "[Discovery through the session-start hook] 'Run the list command, unchanged: with its folder test, its `|| true` and its file-name filter." — docs/superpowers-orchestrator/2026-09-21-worklog/plans/worklog.md — Global Constraint 11 makes the code change non-binding: the list command and its hook copy are the spec's pinned text, and the behaviour the finding names is that command's. The documentation gap stays: the spec's failure mode (line 700) covers a symbolic-linked `docs/worklogs` start path only, and `find` resolves every path part before its last one. It goes into the Phase 5 report as a known limit, so the user can decide on a documentation fix there.

## Ruling 3 — 2026-09-21 — phase 4 — [I1] budget case 8 cannot fail

- **Class:** forced
- **Item:** [I1 inv 1] Important tests/codex/test-session-start-budget.sh:312 — budget case 8 cannot fail for any notice the design allows: the plan-mandated oversized fixture of case 5 leaves about 898 characters of room against a largest notice of about 760, so no test checks that the notices part is counted in the budget; a separate near-limit case would not contradict the Contract; three earlier reviewers graded the same gap Minor (round 4 [M7], verification 2 [M1])
- **Contract clause:** "`tests/codex/test-session-start-budget.sh` case 8 (code artifact): with three active 40-character work logs and the oversized workspace files of case 5," — docs/superpowers-orchestrator/2026-09-21-worklog/plans/worklog.md, Task 4
- **Defensible answers:** n/a
- **Forks:** none; contradiction: none
- **Resolution:** amend plan: Task 4's Contract bullet for `tests/codex/test-session-start-budget.sh` case 8 also requires a case 9 — the three 40-character work logs of case 8 with workspace files sized so that the room they leave is smaller than the notice; `assert_common` passes and the whole notice is present; case 9 fails when the budget leaves the notice out of its count (Amendment 3 in the plan); fix it: add case 9 to `tests/codex/test-session-start-budget.sh` as the amended Contract states, and show once that it fails on a copy of the hook whose budget does not count the notice — Case 8 cannot fail under the fixture the Task 4 Contract mandates (about 898 characters of room against a notice of at most about 760), and the Contract forbids no further case, so `plan governs` fails guard 1 (the clause does not make the missing budget test non-binding), a bare `fix it` is refused against a stated Contract, and changing case 8's own fixture would drop the check it already makes; the only accepting answer is an amendment that adds a separate near-limit case.

## Ruling 4 — 2026-09-21 — phase 4 — [I1] the worklog intent patterns match the Jira term

- **Class:** forced
- **Item:** [I1 inv 2] Important hooks/skill-rules.json:43 — the two `worklog` intent patterns also match the Jira and Tempo term worklog in coding requests; replayed with matchSkills, 'update the worklog of issue PROJ-12 through the Jira REST API' suggests worklog alone, so no brainstorming hint is offered; the fix changes the patterns that Global Constraint 12 fixes; [M2] of this round removes the skill-side consequence (a plain-words request about another product's worklog runs no command); invocation 1 round 2 [M1] carried the same overlap as Minor
- **Contract clause:** "[Interfaces and contracts] The `hooks/skill-rules.json` entry is: '`skill`: `worklog`; `type`: `workflow`; `priority`: `high`.'" — docs/superpowers-orchestrator/2026-09-21-worklog/plans/worklog.md, Global Constraints
- **Defensible answers:** n/a
- **Forks:** none; contradiction: none
- **Resolution:** plan governs: "[Interfaces and contracts] The `hooks/skill-rules.json` entry is: '`skill`: `worklog`; `type`: `workflow`; `priority`: `high`.'" — docs/superpowers-orchestrator/2026-09-21-worklog/plans/worklog.md — Global Constraint 12 copies the spec's `keywords` and `intentPatterns` verbatim, so every pattern change is a spec change, and after the round 6 [M2] fix the only remaining effect is a skill-suggestion hint on another product's term (no command runs, no work is lost, no commit is wrong), which does not make the spec wrong; plan governs is the only answer that leaves the spec's pinned entry in force without escalating a hint-only effect. The overlap goes into the Phase 5 report as a known limit.
