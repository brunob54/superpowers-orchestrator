# Open decisions — execution-readiness-pass

## Ruling 1 — 2026-09-11 — phase 4 — [I1] clause-removal and triage-authority anchors carry no assertion

- **Class:** design
- **Item:** [I1 inv 1] Important skills/multi-doc-review/SKILL.md:672 — unresolved: verification cap — the clause-removal machinery and the triage authority rules carry no assertion: the removal table's `Clause removed` column, the no-renumbering paragraph, the paragraph that tells the controller where to stop, the `<k>` numbering rule that decides report usability, and Step 0's side-matching test with Step 1's `Fixed text` list.
- **Contract clause:** "- Invariants: the subsection carries the strings `pre-sequence`, `post-sequence`, `at most three passes`, `Readiness passes are not counted in N and are not par" — docs/superpowers-orchestrator/2026-09-10-execution-readiness-pass/plans/execution-readiness-pass.md, Task 2
- **Defensible answers:** fix it (pin the anchors as an ordinary fix); accept (ship the gap owed); plan governs (the Invariants list is the coverage the plan chose); amend plan + fix it
- **Forks:** 3 of 3 — design consistency: VERDICT: accept (tabling `fix it` as an ordinary fix, scoped per task); implementation practicality: VERDICT: fix it, narrowed to the `<k>` numbering rule and the removal table's `Clause removed` cells; adversarial: VERDICT: fix it, add the `check (3)`/`check (4)`/`check (5)` needles, record the rest as owed; contradiction: settled — Task 2's Contract already states the Invariant "the three fallback rows name check (3), check (5) and check (4) with their note lines" and its Verification claims section 13 proves it, while section 13 asserts only the three `**Note:**` lines, so those three needles are owed by contract; every other disputed anchor sits outside the Invariants list, and two lenses independently showed that pinning the pure-explanation paragraphs would consume Global Constraint 1's cut reserve at a one-line margin (1079 of 1080).
- **Resolution:** fix it: add `assert_folded_contains` needles for the three fallback rows `check (3)`, `check (4)`, `check (5)`, for the `<k>` numbering rule, and for the removal table's `Clause removed` cells; leave the no-renumbering paragraph and the "Stop at that paragraph" paragraph unpinned so Global Constraint 1 Move 2 keeps its cut reserve; record Step 0's folded side-matching test and Step 1's `Fixed text` list as owed against Task 3, which owns them — a bare `fix it` is valid because Task 2's `Invariants` list is a floor, not a ceiling, so the finding is against text outside the binding set, and the three fallback-row needles make an existing binding Invariant true rather than changing it.

## Ruling 2 — 2026-09-11 — phase 4 — [I2] the readiness lens-wiring sentence is pinned nowhere

- **Class:** design
- **Item:** [I2 inv 1] Important skills/multi-doc-review/SKILL.md:639 — unresolved: verification cap — the sentence that wires a readiness pass to its lens text carries no assertion in any suite: `[LENS_NAME]` = `Execution readiness`, `[LENS_INSTRUCTIONS]` = that lens's `plan:` cell, `[ROUND]` = `readiness <pre|post> <p>`. Both endpoints are pinned and the link joining them is not.
- **Contract clause:** "- Invariants: the subsection carries the strings `pre-sequence`, `post-sequence`, `at most three passes`, `Readiness passes are not counted in N and are not par" — docs/superpowers-orchestrator/2026-09-10-execution-readiness-pass/plans/execution-readiness-pass.md, Task 2
- **Defensible answers:** fix it; accept; plan governs; amend plan + fix it
- **Forks:** 3 of 3 — design consistency: VERDICT: fix it; implementation practicality: VERDICT: fix it; adversarial: VERDICT: fix it — add one `assert_folded_contains` needle covering the whole wiring sentence, no plan amendment; contradiction: none.
- **Resolution:** fix it: add ONE `assert_folded_contains` needle over the whole wiring sentence, covering all three placeholder mappings together, in the existing section-13 needle loop of tests/reviewer-templates/run-tests.sh — never `assert_file_contains` or `assert_file_has_line`, which would make the sentence a seventh member of Global Constraint 1 Move 1's no-reflow list and remove the reflow lever at a one-line margin; mind the apostrophe in `that lens's` when quoting the needle in bash. The spec's `## The lens cell` states the `plan:` text is copied verbatim into `[LENS_INSTRUCTIONS]`, so the reworded variant the finding describes would contradict the spec.

## Ruling 3 — 2026-09-11 — phase 4 — [I3] Task 3's Verification clause is false as shipped

- **Class:** design
- **Item:** [I3 inv 1] Important skills/multi-doc-review/SKILL.md:1035 — unresolved: verification cap — the plan states a verification the shipped test does not perform: no needle anywhere in tests/ matches `skip the rotating loop` or `tier 2, else tier 3`.
- **Contract clause:** "- Verification: the same suite section asserts the new bullet's three clauses and the narrowed invalid-N wording." — docs/superpowers-orchestrator/2026-09-10-execution-readiness-pass/plans/execution-readiness-pass.md, Task 3
- **Defensible answers:** fix it (add the missing needles); amend plan (narrow the Verification clause to what shipped); accept (ship the gap owed)
- **Forks:** 3 of 3 — design consistency: VERDICT: fix it; implementation practicality: VERDICT: fix it, one whole-bullet folded needle rather than fragments; adversarial: VERDICT: accept, on two claims — that the disposition is half wrong because the readiness clause is already pinned, and that `assert_folded_contains` normalises whitespace so it cannot enforce the "verbatim" the Invariant demands; contradiction: settled — the orchestrator verified both claims at their source. The first holds: `for a plan document the Execution readiness pre-sequence still runs` is pinned twice, at tests/reviewer-templates/run-tests.sh:428 and :495, while `tier 2, else tier 3` and `skip the rotating loop` match nothing in tests/. The second does not: `fold_file` strips only leading and trailing whitespace per line and joins lines with one space, and the needle then goes through `grep -qF` as a fixed string, so interior wording is matched verbatim and only the line-wrap position is left free — which is precisely what Move 1 (reflow) requires. The `accept` verdict therefore rests on a false premise about the assertion helper, and its remaining point (a fix landing after the cap with no cycle to review it) applies equally to rulings 1 and 2 and is answered by recording the edits as owed review.
- **Resolution:** fix it: add `assert_folded_contains` needles for the two genuinely unpinned fragments only — the tier sentence and `skip the rotating loop, log` — each as a whole-bullet needle, never the bare fragment `tier 2, else tier 3`, which the N-resolution prose elsewhere in the same file would satisfy while pinning nothing; do NOT re-pin `for a plan document the Execution readiness pre-sequence still runs`, which is already pinned twice; and correct the review-log disposition text, which claims the whole narrowed bullet is revertible when its readiness clause is pinned.

## Ruling 4 — 2026-09-11 — phase 4 — [L31] Task 10 Step 4 expects a suite that is red before this branch

- **Class:** forced
- **Item:** [L31 inv 1] user-decision docs/superpowers-orchestrator/2026-09-10-execution-readiness-pass/plans/execution-readiness-pass.md:1514 — the plan's Task 10 Step 4 expects every suite in its eight-suite chain to end "0 failed", but tests/smart-compress/run-tests.sh ends 79 passed, 8 failed; the controller re-ran it and confirmed the branch changes neither that suite nor hooks/, so the failures predate BASE.
- **Contract clause:** "Expected: PASS for every suite — each ends with "Results: <n> passed, 0 failed" (the Codex suite prints its own summary) and the chain exits 0." — docs/superpowers-orchestrator/2026-09-10-execution-readiness-pass/plans/execution-readiness-pass.md, Task 10
- **Defensible answers:** n/a
- **Forks:** none
- **Resolution:** amend plan: Task 10 Step 4 takes tests/smart-compress/run-tests.sh out of the `&&` chain and records its 8 pre-existing failures as predating BASE; fix it: nothing further in code — the forced fact is that the clause is binding plan text the finding proves unsatisfiable on this branch, so `plan governs` has no clause that makes the finding non-binding, a bare `fix it` is refused against binding text, and `accept` is valid only for an `unresolved` item, which this is not; `amend plan` is the only answer the guards leave.
