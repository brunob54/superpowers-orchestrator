# Plan Review Log — execution-readiness-pass

_Invocation 1 — 2026-09-10 — N=4 M=3 — gate: orchestration_

## Round 1 — Correctness & completeness — claude-opus-5[1m]
**Reviewers:** M=3, usable 3/3
**Reviewer verdicts:** r1: 1 Critical, 5 Important, 5 Minor | r2: 0 Critical, 6 Important, 6 Minor | r3: 0 Critical, 4 Important, 3 Minor
**Sources mapped:** 30/30
**Reviewer verdict:** 1 Critical, 10 Important, 11 Minor
**Converged:** no

### Dispositions
- [C1] applied — Task 4 Step 3 / once-per-gate paragraph: the existing SKILL.md sentence "An entry whose recorded N is `0` … go to **Otherwise**" is never narrowed to spec/general, so the file would carry two rules for one input and the unnarrowed one wins → Task 4 Step 3 now replaces that sentence with a spec/general-scoped form that defers a plan entry to `Readiness entries`; section 15 asserts the narrowed wording and the absence of the unnarrowed form ← 3/3: r1:C1, r2:I1, r3:I1
- [I1] applied — Global Constraint 1 / Step 4 of Tasks 1–4: the reference bodies add ~170 lines to a 150-line cap, so the 938-line check fails first at Task 4 with a remedy naming only Tasks 2–4 → GC1 now states the measured overshoot, names Task 1's cell inside the shortenable universe, and defines shortenable as "carries no needle of sections 12–15"; every Step 4 budget check points at that rule ← 3/3: r1:I2, r2:I5, r3:I2
- [I2] applied — Task 3 Step 3 / Error Handling: the bullet "N = 0 → skip, log" is left unchanged while Task 2 makes a plan's N = 0 still run the pre-sequence → Task 3 now rewrites that bullet, the Contract covers it, and section 14 pins the narrowed wording ← 2/3: r1:I4, r2:M1
- [I3] applied — Task 4 Step 3 / Completeness: "unchanged since that entry was written" had no defined test → the invocation note now carries ` — plan-blob <sha>` from `git hash-object`, an entry without the field counts as changed, and section 15 pins `git hash-object` ← 2/3: r1:I5, r2:I2
- [I4] applied — Task 1 Step 1: the header-sentence needle wraps across a line break, so `assert_file_contains` (grep -qF, single line) can never match → changed to `assert_folded_contains` with a comment ← 1/3: r1:I1
- [I5] applied — Task 4 Step 3 / completion report: the shape hardcoded `open after 3` while Task 2 caps a no-spec sequence at one pass → shape is now `open after <cap>` with `<cap>` defined as 3 normally, 1 with no locatable spec ← 1/3: r1:I3
- [I6] applied — Global Constraint 4 vs Task 3 needle: GC4 spelled `unresolved:` with inner backticks, the needle and body without, so the pinned string could not match → GC4 now fixes the no-inner-backtick spelling for all four sites, and Task 3's Contract restates it ← 1/3: r2:I3
- [I7] applied — Task 2 Step 3 / trigger paragraph: the unconditional "run the pre-sequence before round 1" is placed before a paragraph reached on resume too, so a resume would re-run a finished pre-sequence → the paragraph is now scoped to a fresh invocation and defers to the resume rule; Task 2's Contract carries the clause ← 1/3: r2:I4
- [I8] applied — Task 4 Step 3 / Review Log Format: the spec's layout of an N = 0 plan entry had no implementing task, and "Skipped invocations (N=0) get a one-line `skipped` entry" stayed unqualified → the layout sentence is added to `Readiness entries`, the skipped sentence is rewritten to name the plan layout, and section 15 pins both ← 1/3: r2:I6
- [I9] applied — Task 3 Step 3 / disposition table: the spec's second trigger for "fixed text vs fixed text" (a finding reversing an amendment made earlier in the same sequence) was dropped, removing the oscillation guard → the row carries both triggers, the Contract explains why, and section 14 pins the phrase ← 1/3: r3:I3
- [I10] applied — Task 7 Step 3 / Orchestration Log Format: the earlier-release `skipped (N_plan=0)` exception sat only in Phase 0 while the Log Format said flatly that Phase 2 is never skipped → the exception and the `(N_plan=0)` parenthetical now stand in the Log Format replacement, the Contract requires them there, and section 16 slices that section to assert both ← 1/3: r3:I4
- [M1] applied — Task 1 Step 4 / GC3: "only the three lines above appear as changed" is unreachable because the header sentence rewraps four physical lines → GC3 and Step 4 now count three *items* and verify by the fence, explicitly not by a line count ← 3/3: r1:M2, r2:M3, r3:M1
- [M2] applied — Task 1 "Does NOT cover": the rendered dispatch label `multi-doc-review round readiness pre 1: Execution readiness` was left for the reader to discover → recorded as display-only and out of scope under GC3 ← 1/3: r1:M1
- [M3] applied — Task 2 and Task 3 Step 3 placement: inserting both subsections before `**After the loop:**` would push that Procedure step under a `###` heading → both are now inserted after that paragraph, before `## Lens Rotation` ← 1/3: r1:M3
- [M4] applied — Task 7 Step 3: the Log Format template's Phase 2 line shape did not show the `(N_plan=0)` variant → the replacement now states that the `rounds <r>` field may carry it ← 1/3: r1:M4
- [M5] applied — Task 3 Step 3 / Error Handling: the spec sentence "a readiness pass with a prompt-delivery or round-level failure is handled by the existing Error Handling section, as a round is" was carried into no file → added to the new bullet and pinned by section 14 ← 1/3: r1:M5
- [M6] applied — Task 4 Step 3 / once-per-gate: the appended sentence spoke only of the completeness rule while the paragraph's closed enumeration is about fields → the sentence now names both the field list and the completeness rule ← 1/3: r2:M2
- [M7] applied — Task 10 Step 2: the three-paragraph summary was 124 words against a 120 maximum → the Problem paragraph is trimmed to bring the total to about 119 ← 1/3: r2:M4
- [M8] applied — Task 2 Step 3 body: "when N ≥ 1 … a second" contradicted "a fresh invocation always runs both" for N = 0 → rewritten as "always runs every sequence that applies to it", with the N = 0 case named inline ← 1/3: r2:M5
- [M9] applied — Task 9 Step 2: the guide's pipeline diagram node still read "Phase 2 — N_plan plan-review rounds" → the node is updated; the example orchestration log is deliberately left unchanged, with the reason recorded in the task, because it depicts an N_plan=3 run ← 1/3: r2:M6
- [M10] applied — Global Constraint 8: "the orchestrating-development and fill-prompt contract tests both assert it" overstated fill-prompt, which asserts only the byte-identical half and on the filled output (verified: `tests/fill-prompt/run-tests.sh:396`, `tests/orchestrating-development/run-tests.sh:311-313`) → GC8 now names what each suite asserts ← 1/3: r3:M2
- [M11] applied — Global Constraint 3: the spec says "two lines" in Non-goals and "three" in Files touched, and the real diff shows four → GC3 records that this plan adopts three items and supersedes the Non-goals figure ← 1/3: r3:M3

## Round 2 — Ambiguity & testability — claude-opus-5[1m]
**Reviewers:** M=3, usable 3/3
**Reviewer verdicts:** r1: 0 Critical, 5 Important, 6 Minor | r2: 1 Critical, 2 Important, 8 Minor | r3: 1 Critical, 3 Important, 5 Minor
**Sources mapped:** 31/31
**Reviewer verdict:** 1 Critical, 8 Important, 11 Minor
**Converged:** no

### Dispositions
- [C1] applied — Task 7 Step 1: the Log Format span ended at "the next line starting with `## `", which lands inside the fenced example log (line 650), so the span was 7 lines and both new assertions failed on a correct implementation; verified by running the same extraction on the file → the span now ends at `first_line_of "$ORCH" '## state.md Section'` (grep -nxF, real heading at line 724), and a comment records why the naive rule is wrong; re-verified that the target sentence lies inside the corrected span ← 2/3: r2:C1, r3:C1
- [I1] applied — Task 4 Contract and section 15: `Readiness conflicts applied:` was named in "Must convey" but pinned by no needle, so Global Constraint 1's tightening rule made it legally deletable → the label is added to the section-15 needle loop and to the Contract's invariant list, which now names four labels ← 3/3: r1:I1, r2:M7, r3:M4
- [I2] applied — Task 4 Step 3: "after the rules for the added `M ≥ 2` lines and before `## Error Handling`" admitted two positions, and the first would pull the clean-round and Skipped-invocations paragraphs under the new `###` heading → the two Review Log Format edits are reordered so the Skipped rewrite comes first, and the anchor is now a single place, with the hazard stated ← 3/3: r1:I2, r2:I1, r3:M5
- [I3] applied — Task 10 Contract and Step 3: the 120-word limit had no counting convention and no command → the Contract now defines the count as the three summary paragraphs excluding the bold labels, and Step 3 gains an `awk … | wc -w` command; run against the mandated body it reports 118 ← 2/3: r1:I5, r2:M4
- [I4] applied — Task 1 Step 3: the instruction replaced one sentence while the block given was the whole paragraph, which executed literally duplicates two sentences; and "exactly three lines" contradicted Global Constraint 3 → the step now replaces the whole first paragraph, says why, and counts items ← 2/3: r2:I2, r3:I1
- [I5] applied — Global Constraint 1: "shortenable = carries no needle" left every unpinned "Must convey" clause deletable → a sentence is now shortenable only when it carries neither a section 12–15 needle nor a `**Contract:**` "Must convey" clause ← 1/3: r1:I3
- [I6] applied — Task 2 Step 3, missing-structure table: removing a clause left "Run all five checks" and the numbering undefined, and "its coverage block" had no stated extent → the table row now names the exact lines removed, and a new paragraph states that the count word and the remaining numbers are left unchanged, so a gap in the numbering is the intended result ← 1/3: r1:I4
- [I7] applied — Task 4 Step 3: the `plan-blob` field had no position in the invocation line, and the once-per-gate sentence miscounted the added fields → the body now shows the whole invocation line verbatim with `plan-blob` after `<invoker>`, and the once-per-gate sentence reads "two entry fields and one invocation-note field" ← 1/3: r3:I2
- [I8] applied — Task 9 Step 2: `Only the round in flight is lost;` is a clause, not a sentence (verified at `docs/guide/README.md:765`), so inserting after it would strand the rest of its sentence → the block is now appended at the end of that bullet, with the reason recorded ← 1/3: r3:I3
- [M1] applied — Task 9 Contract and Step 4: a single `grep -c 'Execution readiness'` could not falsify any Must-convey item → six fixed-string needles are named across the three files, and the Contract states plainly which items stay reviewer-checked ← 3/3: r1:M2, r2:M3, r3:M3
- [M2] applied — Global Constraint 1: the stated overshoot of "about 170 lines — roughly 20 over" was wrong; measured from the plan's own fenced bodies plus separators the total is about 195 lines, about 45 over → both figures are restated, with the per-task running count (817, 870, 906, 982) that shows the breach is first at Task 4 ← 2/3: r1:M3, r2:M2
- [M3] applied — Task 3 Step 2: "the other eight needles" against a ten-needle loop, all ten absent today → corrected to nine, with the reason stated ← 2/3: r2:M1, r3:M2
- [M4] applied — Task 10 Step 3: the grep named five files while the Contract named six, so the `## v7.14.0` entry was never checked → `RELEASE-NOTES.md` is added, plus a `grep -cF` on the README release-list sentence that the badge alone would otherwise satisfy ← 1/3: r1:M1
- [M5] applied — Task 2/3/4 Step 1: sections 13, 14 and 15 asserted needles against the whole file, so a subsection landing in the wrong section passed → each of the three subsections now gets a position assertion in the shape section 12 already uses ← 1/3: r1:M4
- [M6] applied — Task 5 Step 2: the title "Prove the assertion can fail" overstated a command that re-runs the arithmetic → renamed to "Show the arithmetic reports an over-budget file", with a sentence saying the `bad` branch is shared code exercised elsewhere ← 1/3: r1:M5
- [M7] applied — Task 6 Step 3: whether the continuation joined the cost sentence's paragraph or started a new one was unstated → it is now explicitly a new paragraph directly beneath that paragraph ← 1/3: r1:M6
- [M8] applied — Task 5 Step 1: titled "Write failing test" although the assertion passes on its first run → renamed to "Add the budget assertion", with a sentence pointing at Step 2 for falsifiability ← 1/3: r2:M5
- [M9] applied — Task 8 Step 4: the chain omitted `tests/review-gates/run-tests.sh`, which asserts on the same template → added to the chain, with the reason ← 1/3: r2:M6
- [M10] applied — Task 9 Step 3: the quoted anchors are wrapped across lines in their files (verified at `docs/REVIEW-PROCESS-COMPARISON.md:110`) → the step now says to match every anchor ignoring line breaks and to keep the file's wrapping ← 1/3: r2:M8
- [M11] rejected: the premise does not hold — `first_line_of` searches for the fixed string `**Adversarial failure modes**`, with the asterisks; the Lens Rotation row is `| 4 | Adversarial failure modes |`, which carries no asterisks, so `grep -nF -- '**Adversarial failure modes**' skills/multi-doc-review/SKILL.md` returns line 642 only (verified). The check resolves to the cell, not the table — Task 1's placement anchor matches the rotation-table row instead of the cell ← 1/3: r3:M1

## Round 3 — Feasibility & architecture risk — claude-opus-5[1m]
**Reviewers:** M=3, usable 3/3
**Reviewer verdicts:** r1: 0 Critical, 3 Important, 4 Minor | r2: 0 Critical, 3 Important, 6 Minor | r3: 2 Critical, 4 Important, 3 Minor
**Sources mapped:** 25/25
**Reviewer verdict:** 2 Critical, 5 Important, 9 Minor
**Converged:** no

### Dispositions
- [C1] applied — Task 9 Step 1 vs its Contract and Step 4: the pinned needle is `add 2 to 6 further passes of M reviewers` but the mandated guide sentence wrote `Cost: 2 to 6 further passes…`, so Step 4 failed deterministically on a correct Step 1 (verified: the string is absent from `docs/guide/README.md` today and from every string Task 9 inserts) → the guide sentence now reads "For a plan the gate therefore adds 2 to 6 further passes of M reviewers…", carrying the needle verbatim ← 3/3: r1:I1, r2:I1, r3:C1
- [C2] applied — Global Constraint 1: two independent counts put the additions near 200-212 lines, about 60 over the cap, and the stated rule forbade rewording, which left roughly 12-15 legally removable lines against a 60-line requirement — the plan mandated an unreachable state → the rule is rewritten as two ordered moves. Move 1 is reflow, which changes no word and is safe because nearly every needle uses `assert_folded_contains` (the helper joins the file's lines before matching); the four lines pinned whole-line or single-line are named so they are never reflowed. Move 2 is deletion, under the earlier two-part test. The figures are restated as about 200 added, roughly 60 over, with the running count 818/878/915/991 ← 2/3: r2:I3, r3:C2
- [I1] applied — Task 4 Step 3: `plan-blob` had no producer — neither the Procedure's **Otherwise** step ("append an invocation note: date, N, M, and invoker") nor the fenced invocation-line example was edited, so no entry would ever carry the field and the N = 0 blocking rule was inert → Task 4 now edits both (verified both anchors exist verbatim in the file), the fenced example carries the unchanged shape and the plan variant as separate whole lines, section 15 pins both with `assert_file_has_line` plus the Otherwise wording, and the Contract's title and invariants cover the two edits ← 3/3: r1:I2, r2:I2, r3:I2
- [I2] applied — Tasks 4, 6 and 7 quoted replacement anchors that are wrapped across lines, two of them starting mid-line (verified at `skills/orchestrating-development/SKILL.md:709-711` and `skills/multi-doc-review/SKILL.md:313-314`), while only Task 9 carried the warning → Tasks 4 and 7 now carry the same warning, naming the mid-line anchors and stating that the Log Format replacement stays inside its parenthetical paragraph ← 3/3: r1:M2, r2:M1, r3:I4
- [I3] applied — Task 2 Step 3: "Removing a clause deletes its numbered item and nothing else" contradicted the table row that removes three items for a missing Global Constraints block, and both texts were mandated → the paragraph now says removal never renumbers, states that the checks (3) and (4) rows remove one item each, and names the three things the Global Constraints row removes and why ← 2/3: r2:M2, r3:I1
- [I4] applied — Task 1 Step 3: the coverage block is required by the lens text but the reviewer prompt's fenced `## Output format` — unchangeable under Global Constraint 3 — prescribes only marker, Verdict and Findings, so a reviewer following the format would omit it and every pass would go unusable → the cell now states where the block goes and says the output format lists no such block, so the lens text is self-sufficient; the residual risk and its cost (one retry, then an open pass) are recorded in Task 1's "Does NOT cover" ← 1/3: r1:I3
- [I5] applied — Task 4's `plan-blob` vs the Phase 2 controller's permission list: `doc-review-loop-prompt.md` enumerates what the controller may do and names no command, and Task 8 explicitly excluded the controller rules, so on the orchestration path — the path the spec's acceptance measure uses — the field would never be written → Task 8 now replaces that permission line to admit the read-only inspection commands the skill's procedure names, its "Does NOT cover" records the reason, section 17 pins the new wording, and the Contract's Must-convey list carries it ← 1/3: r3:I3
- [M1] applied — Task 4 Step 3 used the label "First" twice for two different edit groups → the once-per-gate pair is relabelled "Edit 1" and "Edit 2" ← 2/3: r2:M4, r3:M1
- [M2] applied — section 15 needle `'superseded'` already matches `skills/multi-doc-review/SKILL.md:300`, so it passed before the task and pinned nothing → replaced with the distinctive `gains ` — superseded` at the end of its heading` ← 1/3: r1:M1
- [M3] applied — the bare `(N_plan=0)` needle was satisfied by the earlier-release sentence in the same replacement, leaving the `rounds 0` shape unpinned → the needle is now the full `rounds 0 (N_plan=0) — cap — unresolved 0` ← 1/3: r1:M3
- [M4] applied — the File Structure note claimed every task is a self-contained vertical slice while Tasks 2 and 3 commit forward references to text Task 4 adds → the note now states the exception and says not to stop the run between Tasks 1 and 4 ← 1/3: r1:M4
- [M5] applied — the sequence cap is recomputed at read time and never recorded, so a spec that appeared or disappeared between runs reclassifies an existing entry → stated explicitly in `Readiness entries`, with the cost named (a pass, never correctness) ← 1/3: r2:M3
- [M6] applied — Global Constraint 4 said "use that spelling everywhere" while the `RELEASE-NOTES.md` entry of Task 10 writes the sentence with inner backticks → the constraint now scopes itself to the four enumerated sites and says prose elsewhere may keep the backticks ← 1/3: r2:M5
- [M7] applied — Task 7 Step 2 named two failing assertions where five fail → all five are listed, with the reason the two Log Format assertions fail before Step 3 ← 1/3: r2:M6
- [M8] rejected: the negative needle and the narrowed sentence differ by far more than one capital letter — the needle is `An entry whose recorded N is `0` (a skipped entry) does not block` while the replacement reads `For a `spec` or a `general` document, an entry whose recorded N is `0` (a skipped entry) does not block`, so the two differ by the whole thirteen-word qualifier as well as the case of `an`. A re-capitalisation alone cannot make the needle match again ← 1/3: r3:M2
- [M9] applied — Task 8's Contract quoted the pinned checklist line inexactly, so an implementer checking it verbatim would not find it → the invariant now names the file and line, quotes the line with its indentation, backticks and trailing period, and notes that fill-prompt pins the dedented form ← 1/3: r3:M3

## Round 4 — Adversarial failure modes — claude-opus-5[1m]
**Reviewers:** M=3, usable 3/3
**Reviewer verdicts:** r1: 1 Critical, 5 Important, 5 Minor | r2: 2 Critical, 4 Important, 3 Minor | r3: 1 Critical, 4 Important, 5 Minor
**Sources mapped:** 30/30
**Reviewer verdict:** 3 Critical, 9 Important, 10 Minor
**Converged:** no

### Dispositions
- [C1] applied — Task 4 Step 3: `plan-blob` was taken with the invocation note, before the invocation's own triage and self-review edit the plan, so any invocation that applied a finding left a hash that no longer matched and the N = 0 blocking rule fired only after a no-op gate → the field is now written at note time as a release stamp and rewritten once, when the `**Host self-review:** done` marker is written, to the hash of the plan as the entry left it; the body states that the first value is provisional and that presence and value carry different meanings ← 2/3: r1:C1, r3:I1
- [C2] applied — Task 4 Step 3, Completeness: "written before this release" was decided by the absence of a `## Readiness` heading, which is byte-identical to an entry interrupted during pass 1 of its own pre-sequence — the readiness feature would switch itself off, silently, on the pipeline's most common failure → the test now keys on the absence of the `plan-blob` field, which is present from the invocation note onward, and the body forbids the heading test by name ← 2/3: r1:I1, r2:C2
- [C3] applied — Task 9: the round-3 fix wrote "adds 2 to 6" and wrapped the phrase across a line break, while the Contract needle is the single-line `grep -cF` string `add 2 to 6 further passes of M reviewers` — a correct implementation failed the check for two independent reasons → the guide sentence now carries the needle verbatim and unbroken on one physical line, with a note not to reflow it ← 2/3: r2:C1, r3:C1
- [I1] applied — Global Constraint 1, Move 1: the never-reflow list was stated as exhaustive at four lines but omitted the two invocation-line shapes Task 4 pins with `assert_file_has_line`, at the very task where reflow is expected work → the rule is now stated by property (never reflow a line asserted with `assert_file_has_line` or `assert_file_contains`), with the current six-line membership listed and a note that a new whole-line assertion adds to it ← 3/3: r1:I3, r2:I1, r3:I4
- [I2] applied — Task 2 Step 3: the Global Constraints fallback said three things are removed, but the `coverage: GC<k>` shape sits inside the paragraph named as the second item → the rule and the table row now say two, name the paragraph as the carrier of the shape and the discard rule, and mark where to stop ← 2/3: r1:M2, r2:I3
- [I3] applied — Task 2 Step 3: the coverage-usability rule lived in the readiness subsection while Procedure step 2 states a closed usability definition no task edited, so a coverage-less report counted as usable and a pass could settle at pass 1 with the sweep skipped → Task 2 now appends one sentence to step 2's definition, section 13 pins it, and the subsection names itself as the second condition ← 1/3: r1:I2
- [I4] applied — Task 2 Step 3: the fallback left `Run all five checks:` beside four checks and a numbering gap, inviting a reviewer to reconstruct the removed check → the cell opens `Run all the checks below` with no count and tells the reviewer that a missing number was removed on purpose; the count phrase is pinned by section 12 and the Contract's Must-convey list is reworded ← 1/3: r1:I4
- [I5] applied — Task 7 Step 3: the wrapped-anchor warning said "both anchors" while the step quotes four, and the two Phase 2 anchors are wrapped too → the warning now covers every anchor of the step ← 1/3: r1:I5
- [I6] applied — Global Constraint 1, Move 2: the two-part test allowed cutting the two lens-cell sentences that Task 1 names as its mitigation for the unchangeable fenced output format, which would leave every readiness report unusable → a third condition is added: a sentence named in any task's "Does NOT cover" as a residual-risk mitigation may not be cut, with those two sentences quoted as the reason ← 1/3: r2:I2
- [I7] applied — the **On a resume** sentence still said `N=0` abandons an interrupted entry, contradicting the new resume order for a plan → narrowed to `spec` and `general`, with the plan branch continuing the entry and writing no second invocation note ← 1/3: r2:I4
- [I8] applied — only the N = 0 branch of the once-per-gate rule had been narrowed; the N ≥ 1 branch still ended "do not re-run the loop" before the completeness rule is reached, so an interrupted post-sequence or a missing marker was read as complete → the "Otherwise the entry is complete" sentence is narrowed the same way, with the defect stated; anchor verified verbatim in the file ← 1/3: r3:I2
- [I9] applied — Task 3 said every `rejected:` disposition goes in the `Owed:` block while Task 4 and the spec name three conflict kinds, and Task 3's own `out of lens scope` rejection has no two sides to name → Task 3 now lists the three kinds and excludes `out of lens scope` explicitly ← 1/3: r3:I3
- [M1] applied — Task 6 Step 3: both anchors are wrapped and the first ends mid-line inside one flowing paragraph, and the assertions normalise whitespace so a paragraph split would stay green → the step gains the warning, says the sentence is inserted inline between two sentences on one physical line, and notes that only the instruction prevents the split ← 3/3: r1:M1, r2:M2, r3:M3
- [M2] applied — Task 1's note attributed the untouched `description:` line to Global Constraint 3, whose verification covers only lines between `  prompt: |` and its closing fence → restated as a choice, with the gap in the verification named ← 1/3: r1:M3
- [M3] applied — Task 8: the permission clause makes the premise recorded beside `tests/orchestrating-development/run-tests.sh` section 5b ("this controller runs no commands") false → the task now states the assertion still passes, why one line of output needs no background read, and orders the comment updated ← 1/3: r1:M4
- [M4] applied — the Procedure's **Otherwise** invoker list did not admit `gate: orchestration`, the value the completion-report rule keys on → added to the list in the same edit ← 1/3: r1:M5
- [M5] applied — Task 8 quoted the Controller Rules anchor without its four-space indentation and gave no wrapped-anchor warning → both added, with the reason indentation matters for the filled prompt ← 1/3: r2:M1
- [M6] applied — the new review-gates block was numbered 16 although that suite's last label is "12/13/14" → renumbered to 15, and the three references to "section 16" of that suite updated ← 1/3: r2:M3
- [M7] applied — Task 7 Step 2 named four failing assertions where five fail; the fifth is `assert_not_icontains "orchestrator no longer narrows both phases with one N=0 sentence"`, still present at `skills/orchestrating-development/SKILL.md:263` → all five are listed ← 1/3: r3:M1
- [M8] applied — the section-14 needle `never amended` also matches "Fixed text, never amended by this triage", so deleting it from the table row left the assertion green → replaced with the row-specific `` `rejected: plan-mandated — <text>`, never amended `` ← 1/3: r3:M2
- [M9] applied — Task 8 Step 4 said "PASS for all three" while its chain runs four suites → corrected to four ← 1/3: r3:M4
- [M10] applied — the release summary sat about two words under its own 120-word cap, so any later wording fix would breach it → trimmed; the mandated counting command now reports 107 words, near the repository's "about 100" guidance ← 1/3: r3:M5

### Post-loop self-review (writing-plans Self-Review checklist)
- [S1] check 2 (placeholder scan): clean — no `TBD`, `add appropriate`, `XXX`, `FIXME` or bare "as needed" anywhere in the plan.
- [S2] check 4 (scope-reduction scan): clean — the only hits for `v1`/`basic`/`simple`/`minimal` are the standard step title "Implement minimal change".
- [S3] check 3 (type and name consistency): every needle the plan's own test blocks assert against `$DOC_SKILL`, `$READINESS_CELL` and `$DOC_LOOP_PROMPT` was matched, folded, against the plan's mandated bodies. Three needles are absent from the bodies by design — `N = 0 skips the loop and logs a `skipped` entry.`, `Harness probes owed:` and Deviation 3's sentence — because they assert that existing text is unchanged; all three were confirmed present in their target files today. Every one of the 24 `bash` blocks in the plan parses under `bash -n`.
- [S4] check 5 (contract audit): all ten tasks carry a `**Contract:**` field and none is `none — <reason>`. Every occurrence of `**Exact content:**` in the plan is a mention of the concept inside prose or a reference body, never a marker on a task-step block, so the plan pins nothing to itself.
- [S5] check 1 (spec coverage): every spec section maps to a task — lens cell → Task 1; sequences, coverage-usability and missing structures → Task 2; triage and error handling → Task 3; review log entry, fields read, completeness, resume, return contract and completion report → Task 4; size budget → Task 5; gate wording → Tasks 6 and 7; orchestrating-development changes → Task 7; controller template → Task 8; accepted limitations and cost → Task 9; rollout → Task 10.
- [S6] **merge-introduced, fixed inline:** re-measuring the reference bodies after the four rounds gave about 258 net added lines, not the 200 recorded in Global Constraint 1 — the round-3 and round-4 corrections themselves grew them, and the file lands near 1046 against a 938 maximum. Under the constraint's own rule that shortfall was not reachable. The budget is raised to 1080 (a 34-line margin), the literal is changed in Task 5's assertion, its comment, its Contract and its commit subject, and every Step 4 expectation. **The plan now records that this supersedes the spec's 150-line cap and 938-line maximum, and why:** five corrections found during review — the two once-per-gate narrowings, the **On a resume** narrowing, wiring the coverage condition into Procedure step 2's usability definition, and giving `plan-blob` a producer — each need prose in that file, and without them the feature is either contradicted by rules read earlier in the file or silently inert. This deviation from the spec is the one item to raise with the user at the gate.

_Loop complete — 2026-09-10 — rounds 4_
