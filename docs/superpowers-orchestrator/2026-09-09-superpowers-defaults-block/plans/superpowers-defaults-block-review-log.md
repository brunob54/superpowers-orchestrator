# Review Log — superpowers-defaults-block.md (plan)

_Invocation 1 — 2026-09-09 — N=4 M=4 — gate: orchestration_

## Round 1 — Correctness & completeness — claude-opus-5[1m]
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 4 Important, 7 Minor | r2: 0 Critical, 3 Important, 5 Minor | r3: 1 Critical, 4 Important, 3 Minor | r4: 1 Critical, 4 Important, 5 Minor
**Sources mapped:** 37/37
**Reviewer verdict:** 1 Critical, 10 Important, 10 Minor
**Converged:** no

### Dispositions
- [C1] applied — Task 8 Step 1: the Phase 0 reference body never writes the citation sentence Global Constraint 7 pins and Task 2's `CITE_MARKER` greps, so Task 8 could never turn the suite green → the body now opens with the exact sentence, then maps `<d-n>`, `<d-m>` and `<d-cap>` to it ← 3/4: r1:I1, r3:C1, r4:C1
- [I1] applied — Task 9 Step 3: three of the five named comment sites do not carry the removed tag; `test-multi-code-review.sh:15` uses "reviewers-per-lens" for review-log entry lines → the step now names only `test-multi-doc-review.sh:22` and `:179` for the tag substitution, gives the two variable-naming comments their own instruction, and lists the review-log sites that must be left alone ← 3/4: r1:I4, r3:I1, r4:I1
- [I2] applied — Task 3 Step 2 / Task 4 Step 2: Global Constraint 11 requires an echo for a silent tier-2 resolution, and M is never asked for in either skill, yet no step produced one (Task 4's contract asserted it) → both M entries now require the completion message to state the value and its block source ← 2/4: r1:I3, r2:I2
- [I3] applied — Task 7 Step 4: the citation ended with a colon instead of the pinned period → rewritten as the exact sentence followed by "In short:". (r2:I1 also claimed Task 7 would fail the citation assertion; verified false — Step 3 imports Task 5 Step 2's wording, which carries the exact sentence. The colon fix stands on its own.) ← 2/4: r1:M1, r2:I1
- [I4] applied — Task 7 Step 3: importing Task 5's N sentence would delete this gate's "label the zero option with that consequence" instruction and assert a `skipped` log entry the code gate never writes → Step 3 now carries its own N sentence keeping the gate's zero consequence ← 2/4: r3:I4, r4:I2
- [I5] applied — Task 10 Step 3: the replacement text kept a historical `<reviewers-per-lens>` mention that Step 4's grep and the task's own invariant forbid → the historical clause is dropped and the step says why ← 1/4: r1:I2
- [I6] applied — Task 3 Step 2 / Task 4 Step 1: the offered-default label was never applied to the ask-once question of a direct invocation → both steps now require the question to present the resolved value first with its three-way label, as a prose question ← 1/4: r2:I3
- [I7] applied — Task 3 Step 1, "The offered default": the directional labels named only rounds and reviewers, leaving a `batch-task-cap` of 4 or 5 unlabelled → a direction clause now states that a cap below the hardcoded default is stronger (recommended) and one above it weaker (session default) ← 1/4: r3:I2
- [I8] applied — Task 3 Step 1, "The rule" item 1: tier 1 said "valid for that parameter and entry point" without defining validity, and the spec's "What each range constrains" table had no implementing task → the five validity rows, including the cap's two different sets, are now in the normative section and in Task 3's Contract ← 1/4: r3:I3
- [I9] applied — Task 7 Steps 2 and 7: both named the block without the scoping phrase Global Constraint 9 calls load-bearing → both now read "the last complete `<superpowers-defaults>` block of the `hooks/session-start` injection" ← 1/4: r4:I3
- [I10] applied — Task 7 Step 6: the range 286–293 would delete the plan-complete branch paragraph that Step 7 rewrites → corrected to 286–290, with a sentence saying 292–295 stay unchanged ← 1/4: r4:I4
- [M1] applied — Task 4 Step 1: unclosed bold and a stray backtick in the load-bearing scoping phrase → "**of the `hooks/session-start` injection**;" ← 4/4: r1:M2, r2:M5, r3:M2, r4:M2
- [M2] applied — Task 4 Step 1: the quoted sentence to replace omitted the file's trailing word "run" → quoted as it stands ← 4/4: r1:M3, r2:M2, r3:M1, r4:M3
- [M3] applied — Task 10 Contract: "all four variables" followed by a list of six → "all six variables" ← 4/4: r1:M5, r2:M1, r3:M3, r4:M1
- [M4] applied — Task 8 Contract: the `<d-n>`/`<d-cap>` occurrence counts contradict Step 1's own body → restated as which question each placeholder serves, without a count ← 1/4: r1:M4
- [M5] applied — Task 7 Step 5: the cap rule did not name the resume prompt's `X=<x>` as a stated value → added ← 1/4: r1:M6
- [M6] applied — Task 3 Step 2 ("M entry (lines 40–65)" ends at 79) and Task 10 Step 4 (`exit 1` inside a `for` loop would end the session's own shell) → range widened to 40–79; the check now sets a flag and tests it ← 1/4: r1:M7
- [M7] applied — Tasks 5, 6 and 7 Contracts: "all 11 / all 10 former `<d>` occurrences read `<d-m>`" cannot hold after the same tasks' rewrite steps replace some of them → restated as "no bare `<d>` remains and every surviving default-for-M placeholder reads `<d-m>`" ← 1/4: r2:M3
- [M8] applied — Task 8 Step 1: the range 231–241 starts one line early and stops four lines short → corrected to 232–245 (verified against the file) ← 1/4: r2:M4
- [M9] applied — Task 3 Step 1: the section never introduced `<d-n>`, `<d-m>` and `<d-cap>` → one sentence now names what each denotes ← 1/4: r4:M4
- [M10] applied — Task 10 Step 1: the range 375–381 includes the `### Environment variables` heading itself → corrected to 377–381 ← 1/4: r4:M5

## Round 2 — Ambiguity & testability — claude-opus-5[1m]
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 4 Important, 5 Minor | r2: 0 Critical, 3 Important, 6 Minor, counts recomputed | r3: 0 Critical, 3 Important, 4 Minor | r4: 0 Critical, 6 Important, 5 Minor
**Sources mapped:** 36/36
**Reviewer verdict:** 0 Critical, 13 Important, 6 Minor
**Converged:** no

### Dispositions
- [I1] applied — Task 11: Global Constraint 2 forbids a complete block in any documentation file, but no check covered `RELEASE-NOTES.md`, the one documentation file Task 11 writes → Step 2 now states the delimiter rule for the entry, Step 3 runs the delimiter-pair check over the file, and the contract carries the invariant ← 4/4: r1:M3, r2:I3, r3:M4, r4:I5
- [I2] applied — Tasks 5, 6, 7 and 8, the "Rewrite …" step headings: the stated line ranges are narrower than the text each replacement body reproduces, so a line-based edit truncates or duplicates a sentence at five sites → every range recomputed against the working tree and stated as first-words/last-words anchors where a range alone is unsafe (Task 7 Step 2 → 79–86; Step 3 → the N sentence 95–97 and the M definition 97–105; Step 5 → 251–256; Task 6 Step 2 → 404–406 and 406–413). One claim was checked and rejected: Task 8 Step 1's 232–245 is correct — line 245 is `autonomy:` and the Branch-point bullet starts at 246 ← 3/4: r1:I2, r2:M4, r4:I2, r4:M4
- [I3] applied — Task 9 modifies three files but states a contract for only one; Step 2's widened `unset` is behaviour, not wording → a second contract entry now covers both call-site scripts, requiring all three variable names and the guard's current name ← 3/4: r1:M4, r2:M3, r4:I6
- [I4] applied — five reference bodies wrote the echo message with nested backticks and the ellipsis-broken tag, so neither the exact string nor whether the user sees the ellipsis was decidable → each message is now one unnested code span with the plain opening tag, and Global Constraint 2 states that the broken form is for a closing delimiter only and never reaches a user's screen ← 3/4: r2:M5, r3:I2, r4:M3
- [I5] applied — Task 7 Steps 6 and 7 gave contradictory ranges for the plan-complete paragraph; the sentence Step 7 replaces starts mid-line 295 and ends at line 300, so replacing 296–299 would orphan `The loop runs` and duplicate the closing clause → both steps now name the same anchors ← 2/4: r1:I1, r4:I1
- [I6] applied — several contract invariants (the echo sentences, the origin sentences, the README field shape, the release summary) sit under a `Verification` command that cannot reach them, so a green command would read as proof they hold → a `*(read-verified)*` marker is defined once in Task 2 and applied to every such invariant ← 2/4: r1:I4, r4:M1
- [I7] applied — Task 6 Step 2 said "the same four edits as Task 5 Steps 2, 3 and 4" and then listed five sites, with no body of its own, breaking the plan's own "retype it here" rule → the step now enumerates five numbered edits, each naming its Task 5 source body and its own range; Task 7 Step 3 names its source body the same way ← 2/4: r2:I2, r3:M3
- [I8] applied — Task 5 Step 4 said to "keep the existing recorded-on-the-log form" for N, but no standalone `N=<n> — …` form exists: the standalone forms are M's and sit inside the "recorded N is `0`" branch where N always comes from the user's answer → the step now names the four `Re-invoking multi-doc-review with …` sentences as the only site and forbids adding block-sourced N wording to the M list ← 1/4: r1:I3
- [I9] applied — Task 2 claimed all four parts of Global Constraint 8 were beyond an automated test, and so left the scoping phrase of Global Constraint 9 unasserted after deleting the anti-drift block → the suite now asserts the literal ``of the `hooks/session-start` injection`` in all six files, and the exclusion note names the three parts that really are prose-only ← 1/4: r2:I1
- [I10] applied — Task 1 Step 6's `git add` names a path Step 4 already removed with `git rm`; `git add` then exits 128 and stages nothing, so the commit would carry only the deletion (reproduced in a throwaway repository: exit 128) → the deleted path is dropped from the `git add` line, with a note saying why ← 1/4: r3:I1
- [I11] applied — Task 9's contract verification described a fixture at the wrong path (the guard reads `<plugin_dir>/.claude/settings.json`, never a file directly in `<plugin_dir>`) and no step ran it → Step 4 now carries a runnable check at the correct path, and the contract records that the "accepts" half depends on the machine's own settings files ← 1/4: r3:I3
- [I12] applied — Task 5 Step 2's range 124–130 starts two lines after the M definition begins and ends on the line that also carries `Say with the M`, so the definition would be duplicated and the cost sentence cut in half → the range is now 122–130 with the surviving text named explicitly ← 1/4: r4:I3
- [I13] applied — Task 8 Step 1's label rule said "recommended when it is stronger (more rounds, more reviewers)", which contradicts the batch-cap direction added to Task 3 in round 1, at the only site that offers the cap in a question → the Phase 0 body now states the inverted direction for the cap ← 1/4: r4:I4
- [M1] applied — the per-file verify steps pipe the suite into `grep`, so the exit status is grep's and no failure is signalled → a note before Task 1 explains that these steps are read by eye, why the suite is deliberately red between Tasks 2 and 8, and that the exit status is asserted once at Task 8 Step 2 ← 2/4: r1:M2, r2:M6
- [M2] applied — Task 11's version check used `grep -rn … | head`, which truncates and counts nothing, and its expectation of "one hit per required site" is wrong for `README.md` (three) → replaced with a per-file count loop stating the wanted number, plus a `7.12.0` residue check ← 2/4: r1:M5, r3:M2
- [M3] applied — Task 8 Step 3 says to record the citation counts in the commit message body, but Step 4's command had no body → a second `-m` carrying the five counts ← 2/4: r2:M1, r3:M1
- [M4] applied — Task 8 Step 3 counted with a weaker normalization than the suite's `normalize_file`, so a citation inside a block quote would count 0 against a green assertion → the step now reuses the suite's own function ← 2/4: r2:M2, r4:M2
- [M5] applied — Task 7 Step 8's grep filter cannot match the two `Integration …` assertions its expected result names → `Integration` added to the filter, with the reason ← 1/4: r1:M1
- [M6] applied — Task 3 Step 3's filter cannot show the five `cites the rule by name` assertions its expected result says will still fail → filter widened to `cites the rule` and `scoping phrase` ← 1/4: r4:M5

## Round 3 — Feasibility & architecture risk — claude-opus-5[1m]
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 1 Important, 4 Minor | r2: 0 Critical, 1 Important, 2 Minor | r3: 0 Critical, 2 Important, 3 Minor | r4: 0 Critical, 2 Important, 2 Minor
**Sources mapped:** 17/17
**Reviewer verdict:** 0 Critical, 2 Important, 8 Minor
**Converged:** no

### Dispositions
- [I1] applied — Task 5 Step 4 and Task 6 Step 2 item 5: the step keyed N's edit on the string "the log's invocation line does not record it, so this is the default", which occurs in each file exactly once and is M's standalone form inside the "recorded N is `0`" branch the same step forbids touching; inside the four `Re-invoking …` sentences N's default appears only in the joint plural form "does not record them, so these are the defaults" → the step now quotes the joint clause verbatim, requires it to be split into per-value parentheticals with N's own tier-2 and tier-3 forms, extends the only-one-recovered sentence for the case where N is the missing value, and names the M-only forms as untouched. Task 6 item 5 carries the same instruction translated to lines 379–395 and 382–383 ← 3/4: r1:I1, r3:I1, r4:I2
- [I2] applied — Task 8 Step 3's snippet uses a quoted heredoc (`<<'SRC'`), so `$(sed …)` reaches `. /dev/stdin` unexpanded and `normalize_file` is never defined; every count then prints `0` silently, which is the exact false zero the step exists to prevent (reproduced here: `/dev/stdin: line 1: normalize_file(): command not found`) → replaced with a temporary file, which this repository requires anyway because `/dev/stdin` is unavailable on Windows Git Bash, plus a `type` guard that says the extraction failed instead of printing zeros ← 3/4: r2:I1, r3:I2, r4:I1
- [M1] applied — `README.md` carries a fourth `7.12.0`, at line 37: the running release inventory, which Task 11 neither edited nor counted → Step 1 now extends that inventory with v7.13.0 and Step 3's expected README count is 4 ← 3/4: r1:M3, r2:M1, r3:M1
- [M2] applied — Task 9's contract heading and its call-site invariant name the pre-rename function, while Step 1 renames it and Step 4's grep forbids the old name anywhere under `tests/claude-code/` → both now name `check_no_superpowers_defaults_setting`, with the `export -f` line called out ← 2/4: r1:M1, r2:M2
- [M3] applied — Task 10 Step 2's guide-table range `:516-520` starts at the separator row while the reference table reproduces the header and the separator, so the replacement would duplicate two rows → corrected to `:515-520` with the row layout spelled out ← 1/4: r1:M2
- [M4] applied — Task 5 Step 2 said `Say with the M question:` "also sits on line 130"; it actually spans mid-130 into line 131, so a whole-line replacement of 122–130 would cut the pin in half → the step now says the definition ends mid-line 130 and names what survives ← 1/4: r1:M4
- [M5] applied — line numbers cited by later steps of a task are stale once an earlier step of the same task has replaced a shorter passage with a longer body, and Task 7 Step 6 was the only step identified by line number alone → a note before Task 1 states that numbers are as-of the unedited file and the quoted anchor is authoritative, and Step 6 now carries its opening anchor ← 1/4: r3:M2
- [M6] applied — the decoy-fixture comment justifies its size with the 200-line embedding threshold, which governs `project-map.md` only; `state.md` is embedded whole with `cat` and has no cap (verified at `hooks/session-start:262` and `:318`) → the comment now says so ← 1/4: r3:M3
- [M7] applied — Task 8 has no explicit placeholder-rename step, so "no bare `<d>` remains" holds only because the Phase 0 rewrite happens to span lines 234–237 → Step 1 now states that the replaced span carries both `<d>` and both `<reviewers-per-lens>` occurrences and is the task's only removal path ← 1/4: r4:M1
- [M8] applied — Task 4 Step 1 gives the N bullet as lines 65–84, but the bullet ends at 83 and line 84 opens the M bullet Step 2 rewrites → corrected to 65–83 ← 1/4: r4:M2

## Round 4 — Adversarial failure modes — claude-opus-5[1m]
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 4 Important, 5 Minor, counts recomputed | r2: 0 Critical, 3 Important, 5 Minor | r3: 1 Critical, 4 Important, 5 Minor | r4: 1 Critical, 3 Important, 4 Minor
**Sources mapped:** 35/35
**Reviewer verdict:** 2 Critical, 12 Important, 14 Minor
**Converged:** no

### Dispositions
- [C1] applied — Task 1 Steps 1 and 3: the test body and the hook body each carry the closing delimiter in the plan's broken `…` form with a prose instruction to write the literal. Copying both verbatim makes `expected_block` and the hook agree on the broken tag, so every assertion passes and the shipped hook emits a block no reader can complete — all three parameters then resolve at tier 3 with no error anywhere. Fixing only one of the two produces output identical to a correct red run → Step 2 now greps the test file for `0` ellipses and `2` literal closing tags before running, Step 5 greps the hook the same way, and Task 1's contract carries the invariant ← 2/4: r1:I4, r3:C1
- [C2] applied — Task 5 Step 4 and Task 6 Step 2 item 5 said to leave the "recorded N is `0`" branch alone, but M in that branch resolves from `<d-m>`, which is now block-resolved, while its only non-recorded origin form says "so this is the default" → a fourth choice is added to that list naming the block, used when `<d-m>` resolved at tier 2; the ban on block-sourced *N* wording in that branch stands, because N there always comes from the user's answer ← 1/4: r4:C1
- [I1] applied — quoted anchors are given with the file's line wrapping removed, so about half of them match zero times as a byte sequence; the plan's own note calls them authoritative → a second note tells the implementer to match after collapsing whitespace, and names the three sites where widening a failed match to whole lines destroys unprotected text ← 3/4: r1:M1, r2:I3, r4:I1
- [I2] applied — the Verification Summary lists nine suites but only two had an owning step, and five of the nine read files Tasks 3 to 8 rewrite → Task 11 Step 3 now runs the whole list as one `&&` chain, and the summary drops "untouched" for the five ← 3/4: r1:M4, r2:M3, r3:I2
- [I3] applied — Task 2's note claimed the scoping-phrase assertion protects the phrase; it is per file, at least once, while `subagent-driven-development` gets four citing sites and two other files get two each → the note now says per file, not per site, and Task 8 Step 3 records the per-file occurrence count of the phrase beside the citation count ← 2/4: r1:I2, r2:M2
- [I4] applied — the N sentence in all three gate files said "the `review-rounds` line of that same block", a forward reference to a noun the M paragraph introduces afterwards, leaving the N site with none of Global Constraint 8's four parts → all three N bodies now carry the full scoped form plus the tool-result and platform clauses ← 2/4: r1:M5, r4:I2
- [I5] applied — Task 3 Step 2's M entry replaced the file's strongest in-line anti-injection wording with a bare pointer, in the one skill that reads attacker-controlled documents; Global Constraint 8 forbids trimming to a pointer → the entry now restates the tiers, the scoping rule, the tool-result rule and the platform clause in place ← 1/4: r1:I1
- [I6] applied — Task 8 Step 2 pinned only `0 failed`, which is also what a suite that lost its assertion block prints, and Tasks 3 to 7 verify by eye → Task 2 Step 4 records the red run's `Results: <p> passed, <f> failed` in its commit message and Task 8 Step 2 checks the green total against `p + f` ← 1/4: r1:I3
- [I7] applied — the "gate invocation carrying no stated count uses `<d-n>`" paths in both review skills resolve at tier 2 without asking, and Global Constraint 11 makes an echo required, but only M and Batched Autonomous Mode's N had one → both N entries now carry the echo sentence ← 1/4: r2:I1
- [I8] applied — Task 5 Step 4's reference body showed one filled combination, so an implementer copying it literally would ship a gate claiming a block origin on Codex, on OpenCode, and whenever the `review-rounds` line is absent → the body is now in the choose-one form the file already uses at lines 87–90, naming both alternatives for each value ← 1/4: r2:I2
- [I9] applied — Global Constraint 9 stated the phrase as "of the session-start injection" while the assertion pins ``of the `hooks/session-start` injection``, so a writer following the constraint would fail the test → the constraint now states the exact byte form and says it is what the suite greps for ← 1/4: r3:I1
- [I10] applied — the `<d>` check is absence-only, so writing a literal `1` at the six sites outside the wholesale-replaced spans turns it green while deleting the indirection the change exists to add → presence assertions for `<d-m>`, `<d-n>` and `<d-cap>` are added beside it, in the files that must carry each ← 1/4: r3:I3
- [I11] applied — `tests/review-gates/run-tests.sh:241` pins "so the default applies and the origin echo names the default" in both gate spans, outside every replaced span, which contradicts the new rule that the echo names the tier → Task 5 Step 4 now says to read "the default" there as the value `Resolving a default` produced and to add the tier sentence after the pinned fragment, never inside it ← 1/4: r3:I4
- [I12] applied — Task 9 Step 4's guard check reads only the exit status, and the guard loops variable-outer, so on the machine it exists for it returns 1 on the first variable and reports success without testing either new name → the check now reads the guard's message and requires it to name the variable and the fixture path, for both new variables ← 1/4: r4:I3
- [M1] applied — assertions 2b and 2c decide by a failing `grep`, which exits 2 on an unreadable path and takes the pass branch → both loops skip-and-fail on an unreadable file, and a final check reports when the glob matched nothing ← 1/4: r1:M2
- [M2] applied — `expect_decoy_loses` checks only that `reviewers-per-lens=9` precedes the hook's block, so a decoy whose closing delimiter was never made literal still passes while the case is described as covering complete blocks → the check now requires the decoy's closing delimiter too ← 1/4: r1:M3
- [M3] applied — Task 2 Step 4's expected output omitted the six new scoping-phrase failures → listed ← 1/4: r2:M1
- [M4] applied — reference bodies are Markdown block quotes and no step said to strip the `> ` prefixes or keep the original indentation; `normalize_file` strips both before every assertion, so a body pasted with its prefixes passes the suite while rendering wrongly → the body-authority note now says so ← 1/4: r2:M4
- [M5] applied — `run_hook`'s `${SYSTEMROOT:+…}` and `${TEMP:+…}` are unquoted, so a Windows `TEMP` containing a space splits and `env` treats the fragment without an `=` as the command → built as an array and expanded quoted ← 1/4: r2:M5
- [M6] applied — three accepted-value rows assert the same block as the unset case and cannot distinguish a read from the fallback; the replaced test carried a comment about exactly this → the comment is carried forward ← 1/4: r3:M1
- [M7] applied — Task 3 Step 3's expected red lines named only the citation failures, not the five scoping failures its own widened filter displays → both families listed ← 1/4: r3:M2
- [M8] applied — Task 2 Step 2 gives two line ranges in one step, and deleting the first shifts the second by two lines → the step says the ranges are independent and to delete the later one first ← 1/4: r3:M3
- [M9] applied — Task 5 Step 2 calls `Say with the M question:` a pin, but no assertion pins it (the pinned neighbour is the cost sentence) → called a boundary the mid-line replacement must not cut ← 1/4: r3:M4
- [M10] applied — the guide's M description ends on line 178, not 177 → range corrected to `:171-178` ← 1/4: r3:M5
- [M11] applied — Task 7 Step 2's body nests `**of the …**` inside the paragraph's own `**…**` span, which would close the outer emphasis early → the scoping phrase is written unbolded in that one body, with the reason ← 1/4: r4:M1
- [M12] applied — Task 11 Step 3's loop only printed counts and never compared, and `grep -c` counts lines rather than occurrences → the loop compares, prints `FAIL` on a mismatch, and counts with `grep -o | wc -l` ← 1/4: r4:M2
- [M13] applied — assertion 2c scans `skills/*/SKILL.md` only, while Global Constraint 2 covers the prompt templates too → a scope note records the gap, that the templates are clean today, and how to widen the glob ← 1/4: r4:M3
- [M14] applied — `else M's default `<d-m>` (defined below) for M and N's default `<d-n>` for N` gave "(defined below)" to M only, though `<d-n>` is also defined later → added to both halves, in Task 5 and Task 6 ← 1/4: r4:M4

### Post-loop self-review (writing-plans Self-Review checklist)

- **1. Spec coverage** — every spec section maps to a task: Block format and Validation in the hook → Task 1; Testing strategy → Tasks 1, 2 and 9; The normative resolution rule, The rule, Placeholder names, The offered-default rule and What each range constrains → Task 3; Edit-site inventory → Tasks 3 to 8; Error handling → Tasks 3 and 4; Two defences → Global Constraints 2 and 9 with Task 2's assertions; Documentation → Task 10; Rollout and migration notes → Task 11. No gap found.
- **2. Placeholder scan** — one hit, `~/.claude/hooks-logs/...` inside a test comment: an elided path, not a plan placeholder. No TBD, "add appropriate", FIXME or step without code.
- **3. Type and name consistency** — `check_no_superpowers_defaults_setting`, `defaults_content`, `defaults_escaped`, `reviewers_per_lens` / `review_rounds` / `batch_task_cap`, `CITE_MARKER`, `SCOPE_MARKER`, `MDR_SECTION`, `expected_block`, `expect_block`, `expect_decoy_loses` and `run_hook` each appear under one spelling throughout. `<d-m>`, `<d-n>` and `<d-cap>` are used consistently.
- **4. Scope-reduction scan** — no occurrence of "v1", "basic", "simple", "for now", "placeholder", "initial version" or "minimal".
- **5. Contract audit** — all 11 tasks carry a `**Contract:**` (Task 1 has three, Task 9 two). No `**Exact content:**` marker exists in any task step, so there is no self-pin to check; the only occurrence of that label is the header note's own definition, which is out of scope. Every fenced block and `Run:` line falls under its task's contract or is procedural. No `**Global Constraints:**` entry restates the body of an artifact the plan creates while failing to trace to the spec.

One merge-introduced issue found and fixed inline: the glob-coverage check added in round 4 (`the skill glob matched <n> files`) had been placed after assertion 2c's loop while the counter it reads is incremented in 2b's; it now sits at the end of the 2b loop.

_Loop complete — 2026-09-09 — rounds 4_
