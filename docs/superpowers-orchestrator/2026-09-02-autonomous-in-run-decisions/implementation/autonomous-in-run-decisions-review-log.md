# Review log — autonomous-in-run-decisions

_Invocation 1 — 2026-09-02 — N=4 M=4 — BASE..HEAD 0a57e40..e903e73 — branch feature/autonomous-in-run-decisions — gate: orchestration_

carried findings: 11 Minor lines read from .superpowers/sdd/progress.md

## Round 1 — Correctness & spec alignment — fable
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 3 Important, 5 Minor | r2: 0 Critical, 3 Important, 6 Minor | r3: 0 Critical, 2 Important, 6 Minor | r4: 0 Critical, 1 Important, 4 Minor
**Sources mapped:** 30/30
**Reviewer verdict:** 0 Critical, 4 Important, 14 Minor
**Converged:** no
### Dispositions
- [I1] fixed — settled rule keyed on `[task <n>/<k>]` only, while a single-section item is named `[task <n>]`; a `[task <n>]` line now reads as `[task <n>/1]` and the settled rule covers both shapes and Question sections (SKILL.md ~549-553/~865-867, batch-controller-prompt.md ~64-68) → 866c888 ← 4/4: r1:I2, r2:I2, r3:I2, r4:I1
- [I2] fixed — Phase 3 discriminator counted any Conflict/Question section with no freshness test, so a stale answered section misclassified a controller failure as an open-item return; only sections unanswered in the dispatched `[RESUME_ANSWER]` count now (SKILL.md ~617-624, batch-controller-prompt.md Deviation 1) → 866c888 ← 3/4: r1:I3, r2:I1, r3:I1
- [I3] fixed — a second open return on one task lost the first ruling and `<k>` renumbering could apply a stale answer to a new section; every re-dispatch carries all ruled lines of the unit and `<k>` continues across attempts (SKILL.md ~849-867/~928-930, batch-controller-prompt.md ~78-82) → 866c888 ← 2/4: r1:I1, r4:M1
- [I4] fixed — a pre-flight conflict ruled for a later-batch task never reached that batch, whose first dispatch omitted `## Resume Answer`; each batch's `[RESUME_ANSWER]` now carries every recorded answer whose task is in its list (SKILL.md ~271-278/~553, batch-controller-prompt.md ~70) → 866c888 ← 1/4: r2:I3
- [M1] fixed — "new invocation started by an `amend plan` ruling counts as one in-run resume" read as double counting against the cap (SKILL.md ~904/~954-956) → 866c888 ← 4/4: r1:M4, r2:M5, r3:M5, r4:M4
- [M2] fixed — `## RULING` `Items:` line had no form for an escalated item; `[<id>] escalated (<reason>) — <summary>` defined in both copies of the block (SKILL.md ~363/~914-915) → 866c888 ← 2/4: r2:M2, r3:M4
- [M3] fixed — Resume step 3 `**Follow-up:**` append not idempotent and follow-up commit `<n>` undefined for several rulings (SKILL.md ~474-488) → 866c888 ← 2/4: r2:M6, r3:M6
- [M4] fixed — Phase 3 step 5 did not say whether a `BLOCKED` return writes a batch entry, which could break the Resume "log ends with `## RULING`" signal (SKILL.md ~269-278) → 866c888 ← 2/4: r3:M2, r4:M2
- [M5] carried — Resume step 3 `**Follow-up:**` append is unconditional but Phase 1 answers and pre-change `## STOPPED` entries have no ruling-record entry (SKILL.md ~474-478) ← 1/4: r1:M1
- [M6] carried — `Ruled:` lines are built into `[RESUME_ANSWER]` only under "When the resume prompt does answer"; the effective-HEAD-moved re-dispatch path does not say they are sent (SKILL.md ~468-471 vs ~491-495) ← 1/4: r1:M2
- [M7] carried — `## RULING` `Re-dispatch:` line does not record the batch (task list, `First batch:` value) a Phase 3 re-dispatch used, which a crash resume must reconstruct (SKILL.md ~366/~917/~451-455) ← 1/4: r1:M3
- [M8] carried — code-review-loop-prompt.md Deviation 3 secret EXCEPTION says only "record it as `unresolved`", without the `— at <file:line> — clause:` shape the self-sufficient line rule requires (multi-code-review SKILL.md ~884-893) ← 1/4: r1:M5
- [M9] carried — unclear whether a `## RULING` entry is written when every item of a return is escalated (SKILL.md ~934) ← 1/4: r2:M1
- [M10] carried — two routes for an impossible reference-text task step: `amend plan:` answer vs plain-text answer "valid against reference text" (SKILL.md ~853/~869) ← 1/4: r2:M3
- [M11] carried — self-sufficient line rule allows `clause: none` only for an `unresolved` item, but a carried item logged `user-decision` from reviewer recommendations also collides with nothing (multi-code-review SKILL.md ~884) ← 1/4: r2:M4
- [M12] carried — `fork review unavailable` stops mid-return before the already-decided `forced` items of the same return are recorded (SKILL.md ~784-789) ← 1/4: r3:M1
- [M13] carried — "The run stops only for the closed list of reasons in the predicate" contradicts the section's own environment stops (SKILL.md ~563-564) ← 1/4: r3:M3
- [M14] carried — `## STOPPED` `Detail:` for a Phase 3 stop names `.superpowers/sdd/task-<n>-report.md`, excluded state that does not survive a clone boundary (SKILL.md ~384-385) ← 1/4: r4:M3
- carried item 1 (run-tests.sh helpers/path vars unused until Tasks 2-6) — carried (4× ship-as-is: all used by the final suite)
- carried item 2 (run-tests.sh section 2 two extra fragment checks) — carried (4× ship-as-is)
- carried item 3 (run-tests.sh FORK_END scans to EOF) — carried (4× ship-as-is)
- carried item 4 (`clause:` label wording inherited from multi-code-review) — carried (4× ship-as-is)
- carried item 5 (RULING template `Items:` annotation inline after spaces) — fixed — annotation moved out of the fence, with [M2] → 866c888 (3× fix-before-merge, 1× ship-as-is)
- carried item 6 (Phase 3 step 5 lacks the escalated carve-out) — fixed — carve-out added → 866c888 (4× fix-before-merge)
- carried item 7 (Resume step 3 states the `**Follow-up:**` append twice) — fixed — first mention dropped, with [M3] → 866c888 (1× fix-before-merge, 3× ship-as-is)
- carried item 8 (run-tests.sh pre-flight negative check lacks the empty-range guard) — fixed — guard added → 866c888 (3× fix-before-merge, 1× ship-as-is)
- carried item 9 (`## RULING` block defined twice) — carried (4× ship-as-is)
- carried item 10 (`## STOPPED` `Owed probe:` sentence vacuous for a Phase 3 stop) — carried (4× ship-as-is)
- carried item 11 (batch-controller-prompt.md pre-flight rule stated twice) — carried (4× ship-as-is)


## Round 2 — Adversarial red-team — fable
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 6 Important, 6 Minor | r2: 0 Critical, 4 Important, 5 Minor | r3: 0 Critical, 5 Important, 5 Minor | r4: 0 Critical, 6 Important, 6 Minor
**Sources mapped:** 43/43
**Reviewer verdict:** 0 Critical, 13 Important, 15 Minor
**Converged:** no
### Dispositions
- [I1] fixed — bare `[task <n>]` answer form was defined by the report file's current section count, which grows across attempts; now unconditionally `[task <n>/1]` (user shorthand only) and orchestrator lines always use `[task <n>/<k>]` (SKILL.md ~567-569/~901, batch-controller-prompt.md ~68-70/~157-165) → 68ec491 ← 4/4: r1:I5, r2:I2, r3:I2, r4:I1
- [I2] fixed — two rules for which Phase 3 answers a dispatch carries (step 5: every recorded answer; ~898: only since the latest `_Invocation`/`## STOPPED`), so a ruling made before a stop was dropped on resume; one rule now, stated once: every ruled line of the run for that task, nothing dropped at an invocation line or a stop (SKILL.md ~283-284/~898/~981) → 68ec491 ← 3/4: r1:I2, r2:I1, r4:I2
- [I3] fixed — `### Question <k>`/`### Conflict <k>` sections live in `task-<n>-report.md`, which the re-dispatched implementer rewrites, so a second question re-used number 1 and read as a controller failure; `<k>` now numbers above the highest section seen in the file or in the dispatched `[RESUME_ANSWER]`, a section counts as answered only when its exact `<k>` was sent, and stale sections are cleared at a task's first dispatch of the run (batch-controller-prompt.md ~84, SKILL.md ~649) → 68ec491 ← 2/4: r1:I1, r3:I1
- [I4] fixed — `(amended by ruling <n>)` marker on an `**Exact content:**` block could land inside the fence (copied into the product) or after it (not on the clause); placement now pinned to the `**Exact content:**` paragraph line and the decided-wording rule matches that paragraph (SKILL.md ~916-918) → 68ec491 ← 2/4: r1:I3, r4:I5
- [I5] user-decision — the orchestrator can overturn a user's explicit plan-governs answer: a later invocation re-raises the same objection under a new id, the loop's decided-wording rule covers verification cycles only (Task 8), and the orchestrator may read only the latest entry's disposition line, so a guard needs the ruling record — a fifth read entry the plan's four-entry read list excludes (plan-mandated; a fix adding that entry and a guard 4 was applied in 68ec491 and withdrawn in 119b69a) — at skills/orchestrating-development/SKILL.md:1015 — clause: Task 2 "Must convey: the four-entry read list of spec R2 (latest disposition line only; task report file and `### Task <n>` section; the named plan clause and its spec section" ← 2/4: r1:I4, r3:I4
- [I6] fixed — quoted clause on the self-sufficient line is normalized (` — `/` ← ` replaced, 160-char cap) but every consumer compared it as verbatim; one normalization rule now applies on both sides and the quote is compared as a prefix of the normalized plan text at the named location (multi-code-review SKILL.md ~892-894, SKILL.md ~858/~911-918/~1020-1024) → 68ec491 ← 2/4: r1:I6, r2:M5
- [I7] fixed — a conflict touching several tasks was ruled under the lowest task and its answer carried only to batches holding that task; every recorded `[task …]` answer now goes on every Phase 3 dispatch, the lowest-numbered-task filing rule unchanged (SKILL.md ~283-284/~573) → 68ec491 ← 1/4: r2:I3
- [I8] fixed — fork returns for several `design` items in one return carried nothing naming the item; a mandatory `ITEM: [<id>]` line follows the marker, and items are forked and consolidated one at a time (SKILL.md ~703/~746/~776) → 68ec491 ← 1/4: r2:I4
- [I9] fixed — Resume step 3 acted on a trailing `## RULING` entry without checking that the `ruling <n>` commit landed; the commit is now verified in `git log` and made first when missing (SKILL.md ~464) → 68ec491 ← 1/4: r3:I3
- [I10] fixed — a resume answer that replaced a `Ruled:` line left the plan amendment that line had applied in force; Resume step 3 now reverts the amendment (by label and marker) in the same resume commit (SKILL.md ~485) → 68ec491 ← 1/4: r3:I5
- [I11] fixed — the crash-window rebuild of a missing `## STOPPED` took `Open:` lines from every `escalated` entry in the ruling record, re-opening answered escalations; now limited to the trailing return's entries and skips entries carrying a `**Follow-up:**` line (SKILL.md ~473) → 68ec491 ← 1/4: r4:I3
- [I12] fixed — a bare `fix it` against binding text was forbidden but absent from the pre-commit self-check, and the loop had no disposition for it; the self-check now lists that repair and the loop logs `unresolved: fix contradicts binding text — "<clause>"` (SKILL.md ~1020-1031, multi-code-review SKILL.md ~645) → 68ec491 ← 1/4: r4:I4
- [I13] fixed — nothing said what the tree may hold at a Phase 3 ruling commit after a mid-task `BLOCKED`, so "unexpected dirty tree at a boundary" could stop every such block; the ruling commit is now stated not to be a clean-tree boundary, with the blocked task's uncommitted work named and handed to the re-dispatched controller's recovery (SKILL.md ~264-278/~965-968) → 68ec491 ← 1/4: r4:I6
- [M1] fixed — chain stop said "no further ruling is made" while the return-as-a-whole rule required `## RULING` and ruling-record entries; the chain stop now records each item as `escalated (chain)` in both, the cap sentence untouched (SKILL.md ~994-995) → 68ec491 ← 3/4: r1:M3, r2:M1, r3:M3
- [M2] fixed — "the log's latest `_Invocation` line" could be read as the review log's, resetting the cap on every `amend plan`; now "the orchestration log's" (SKILL.md ~989) → 68ec491 ← 2/4: r1:M1, r4:M4
- [M3] fixed — a pre-flight conflict touching no task had no `<n>` for `BLOCKED task=<n>`; the batch's first task is the fallback (batch-controller-prompt.md ~63, SKILL.md ~573) → 68ec491 ← 2/4: r1:M2, r4:M3
- [M4] carried — loop-side decided-wording rule applies in verification cycles only, so an ordinary round of a post-amendment invocation re-raises a decided clause as `user-decision` and spends a resume (plan-mandated: Task 8 "Does NOT cover … findings in an ordinary `## Round <i>`", spec R8.3) ← 2/4: r2:M3, r4:M6
- [M5] fixed — the section's `Items:` template omitted the reason the Log Format shape carries for an escalated item; both shapes used in both places (SKILL.md ~953) → 68ec491 ← 2/4: r3:M4, r4:M1
- [M6] fixed — forks inherit the orchestrator's transcript, so a preference written before they return anchors all of them; the orchestrator now writes no preference before the forks return and lists tabled outcomes in the order they arose (SKILL.md ~731) → 68ec491 ← 1/4: r1:M4
- [M7] fixed — `rejected: plan governs (orchestrator decision) — "<clause>"` inherited no ` ← `/` — ` sanitization; the same replacement applies to that quote (multi-code-review SKILL.md ~947-950) → 68ec491 ← 1/4: r1:M5
- [M8] carried — scope = union of the plan's `**Files:**` lists, so a file the implementer legitimately changed but no task listed makes a later `fix it` there `escalated (scope)` (spec-mandated: R1 `scope` definition, bound by Task 1 Contract) ← 1/4: r1:M6
- [M9] carried — the `scope` test needs every task's `**Files:**` list but the four-entry read list ("Nothing else") does not allow reading them (plan Task 2 Contract / spec R2; no variant of the fix is free of a plan or spec change) ← 1/4: r2:M2
- [M10] carried — `secret` class triggers on a summary that merely names the word "secret" without an exposure (spec-mandated: R1 `secret` definition "disposition reason or summary names an exposed secret") ← 1/4: r2:M4
- [M11] fixed — the cap sentence was pinned as two single-line fragments while the Global Constraint allows a wrap inside the sentence; the check now folds newlines over the range before matching (tests/in-run-rulings/run-tests.sh ~181-182) → 68ec491 ← 1/4: r3:M1
- [M12] fixed — "a notice that never arrives" had no bound; a wait bound after which the return counts as lost is stated, with no other work during the wait (SKILL.md ~813) → 68ec491 ← 1/4: r3:M2
- [M13] fixed — what `rejected: plan governs (loop decision)` quotes for wording decided only by an `(amended by ruling <n>)` marker was undefined; it quotes the amended clause plus its marker (multi-code-review SKILL.md ~647) → 68ec491 ← 1/4: r3:M5
- [M14] fixed — the `<finding summary>` before `— at <file:line>` was not sanitized and a `"` inside the quote was unhandled; the replacement now covers the summary and the quote rule names the `"` case (multi-code-review SKILL.md ~892-894) → 68ec491 ← 1/4: r4:M2
- [M15] carried — "the LATEST `_Invocation` entry's disposition line for that id" assumes one line per id per entry, but review-log ids restart each round (pre-existing for user answers, now exercised autonomously) (SKILL.md ~673) ← 1/4: r4:M5

## Round 3 — Security — fable
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 1 Important, 3 Minor | r2: 0 Critical, 1 Important, 2 Minor | r3: 0 Critical, 0 Important, 4 Minor | r4: 0 Critical, 0 Important, 3 Minor
**Sources mapped:** 14/14
**Reviewer verdict:** 0 Critical, 2 Important, 9 Minor
**Converged:** no
### Dispositions
- [I1] fixed — the ruling record's `**Item:**` line, the `## RULING` `Items:` line and the `## STOPPED` `Open:` line copied item text verbatim into committed files with no secret-hygiene rule on the Phase 3 path (a `### Question`/`### Conflict` section written from implementer output); lines for a `secret` item, or any text carrying a credential, now cite location and description only, in the ruling record and in batch-controller-prompt.md Deviation 1 (SKILL.md ~880, batch-controller-prompt.md ~81-85) → cf99bc5 ← 2/4: r1:I1, r3:M2
- [I2] user-decision — an `amend plan` ruling may remove or weaken a Global Constraint that restricts what may be staged, committed, deleted or touched (this plan's "never `git add -A`", "no task may `git add` CLAUDE.md"): the closed escalation list has no entry for it, `scope` is measured against `**Files:**` lists and `irreversible` names only force-push, data deletion, publishing, external services and dependencies, so a `forced` ruling with no second reader can lift the run's own safety rails; a new class or a wider `scope`/`irreversible` definition changes the spec's closed list (plan-mandated) — at skills/orchestrating-development/SKILL.md:992 — clause: Task 1 "the closed escalation list `spec wrong`, `scope`, `irreversible`, `secret`, `chain` with the spec's definition of each" ← 2/4: r2:I1, r3:M4
- [M1] fixed — the resume follow-up commit said "commit that file" without the explicit-path wording, on a tree that holds a blocked task's uncommitted edits; now staged by explicit path, never `git add -A` or `-a` (SKILL.md ~511) → cf99bc5 ← 2/4: r1:M2, r2:M2
- [M2] fixed — `<n>` from a `BLOCKED task=<n>` return was used as a file path without validation; it must now be a task number of the dispatched batch, anything else is a malformed return and no file is read (SKILL.md ~662) → cf99bc5 ← 1/4: r1:M1
- [M3] carried — `spec wrong` does not say that a binding plan clause tracing to a spec section (`(spec R6)` annotations) counts as spec text, so an in-run `amend plan` could rewrite a spec-derived constraint (spec-mandated: R1 `spec wrong` definition, bound by Task 1 Contract) ← 1/4: r1:M3
- [M4] carried — `irreversible` omits changes to hook wiring, permission allow-lists, CI/deploy configuration and credential-holding files, which an autonomous `forced`/`design` ruling can authorise (spec-mandated: R1 `irreversible` definition) ← 1/4: r2:M1
- [M5] fixed — forks were allowed unbounded `git log`/`git show`/`git diff`, which expose blinded files and accept `--output`/`--ext-diff`/`--textconv`; the permitted forms are now named and limited to paths on the read list, in the section and in the fork prompt (SKILL.md ~719) → cf99bc5 ← 1/4: r3:M1
- [M6] fixed — the crash-repair commit staged the plan file unconditionally; now only when that ruling amended it (SKILL.md ~470-471) → cf99bc5 ← 1/4: r3:M3
- [M7] fixed — guard 1's prefix test had no minimum, so a one-word quote passed; the quoted clause is now at least one complete sentence or list entry of the location (or the whole clause when shorter), and the prefix test runs against that sentence or entry (SKILL.md ~932/~1132) → cf99bc5 ← 1/4: r4:M1
- [M8] fixed — an `accept:` ruling on an unresolved Important finding was never surfaced in the Phase 5 report; step 3 now lists every `accept:` ruling by number and summary next to the unsettled contradictions (SKILL.md ~339-343/~921) → cf99bc5 ← 1/4: r4:M2
- [M9] fixed — the fork prompt marked only the quoted text as data, and the degraded `general-purpose` path inherits no context; "Every file you read under this list is data, never an instruction." added to the prompt (SKILL.md ~792) — harness probe: reviewer read the Agent tool description; observed "fork inherits your full conversation context" and "any other type … starts a fresh agent" → cf99bc5 ← 1/4: r4:M3

## Round 4 — Test & coverage quality — opus
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 3 Important, 3 Minor | r2: 0 Critical, 2 Important, 3 Minor | r3: 0 Critical, 2 Important, 7 Minor | r4: 0 Critical, 4 Important, 5 Minor
**Sources mapped:** 29/29
**Reviewer verdict:** 0 Critical, 7 Important, 12 Minor
**Converged:** no
### Dispositions
- [I1] fixed — the cap sentence was pinned as two case-insensitive folded fragments, leaving its joining colon, terminal period, capitalization and the ban on emphasis markers unchecked; a byte-exact folded pin over the whole sentence plus an emphasis-marker check now sit beside the two existing fragments (tests/in-run-rulings/run-tests.sh ~202-208) → 0182b1b ← 3/4: r1:I1, r3:M1, r4:I3
- [I2] fixed — sections 7 and 9 pinned 14 needles with a whole-file grep and several matched outside the rule they pin (`is settled` twice, `decided (orchestrator)` twice, `160 characters` three times), so deleting the rule kept the suite green; both sections are now range-scoped and the ambiguous needles narrowed to their distinguishing text (tests/in-run-rulings/run-tests.sh ~259-266/~311-316) → 0182b1b ← 3/4: r2:M2, r3:I2, r4:M1, r4:M4
- [I3] fixed — sections 2, 4 and 5 scoped their fragments to the whole 595-line `## In-run rulings` range, so a deleted rule passed on the same words elsewhere in the section; per-subsection anchors added (tests/in-run-rulings/run-tests.sh ~145/~185) → 0182b1b ← 2/4: r2:I1, r3:I1
- [I4] fixed — the batch controller's Deviation 1 secret-redaction, section-numbering, stale-section-deletion, controller-failure and bare `[task <n>]` rules had no assertion at all; Deviation-1-scoped pins added for each (tests/in-run-rulings/run-tests.sh ~311-316) → 0182b1b ← 2/4: r2:I2, r4:I1, r4:I4
- [I5] fixed — the `— clause:` prefix-and-normalization contract was pinned on neither side, so the multi-code-review copy and the orchestrating-development copy could diverge silently; scoped pins added in both files' ranges (tests/in-run-rulings/run-tests.sh ~259-266) → 0182b1b ← 1/4: r1:I2
- [I6] fixed — nothing asserted that `## In-run rulings` sits immediately before `## Major-Error Stop Policy`, so an inserted heading silently widened every range the suite computes; a fence-aware anchor check added (tests/in-run-rulings/run-tests.sh ~122-133) → 0182b1b ← 1/4: r1:I3
- [I7] fixed — the new `unresolved: fix contradicts binding text` disposition token, which gates the run, was pinned nowhere; pin added scoped to the `## After the Loop` range (tests/in-run-rulings/run-tests.sh ~259-266) → 0182b1b ← 1/4: r4:I2
- [M1] fixed — the check named "Guard Interaction names the forks' marker" asserted only the substring `fork`, never the marker; an exact `<!-- multi-review report -->` pin added in that range (tests/in-run-rulings/run-tests.sh ~177-178) → 0182b1b ← 2/4: r1:M1, r3:M2
- [M2] carried — the negative `decided (user)` check spans the whole `## Resume` section while its message says "alone", so harmonising Resume wording to the `decided (user)` or `decided (orchestrator)` idiom the same suite mandates elsewhere would fail the build ← 2/4: r3:M3, r4:M2
- [M3] fixed — section 1's pins were scoped to the whole rulings range, leaving 8 of its 17 checks green after the predicate subsection was deleted; narrowed to `### Classification` (tests/in-run-rulings/run-tests.sh ~128-139) → 0182b1b ← 1/4: r1:M2
- [M4] carried — `assert_in_range_folded` passes its haystack through `awk -v`, which processes backslash escape sequences; latent today, and the leading-whitespace half of the same finding was fixed alongside [I5] ← 1/4: r1:M3
- [M5] fixed — `assert_in_range` scanned `NR >= a && NR < b`, so an inverted or empty range made the suite's only two removal checks pass having examined nothing; a start-before-end guard added (tests/in-run-rulings/run-tests.sh ~87-104/~240-256) → 0182b1b ← 1/4: r2:M1
- [M6] carried — `line_containing_after` and `line_starting_with_after` degrade to a whole-file search when the anchor is empty, and `MCR_M2_END + 1` evaluates to 1 on an empty value, producing a misleading range in the failure message ← 1/4: r2:M3
- [M7] fixed — the negative pin carried the trailing semicolon of `pre-flight plan conflict;`, so a regression re-adding the item with other punctuation passed; the needle is now case-insensitive without the semicolon (tests/in-run-rulings/run-tests.sh ~253) → 0182b1b ← 1/4: r3:M4
- [M8] carried — the Phase 3 and Phase 4 routing checks assert only that the phrase `In-run rulings` appears in the phase section, never the routing token (`BLOCKED task=<n>`, or the open-item dispositions) ← 1/4: r3:M5
- [M9] fixed — the six fork fragments used the whole rulings range while the `VERDICT:`/`TABLED:` pins beside them used the narrower fork range; both loops now use `FORK_LINE`/`FORK_END` (tests/in-run-rulings/run-tests.sh ~169) → 0182b1b ← 1/4: r3:M6
- [M10] fixed — the fork-naming constraint's "never an `orch-` name" half was asserted nowhere; an exact pin added in the fork range (tests/in-run-rulings/run-tests.sh ~151) → 0182b1b ← 1/4: r3:M7
- [M11] fixed — `FORK_END` took the next `### ` heading anywhere below in the file, unbounded by the section end, so the deliberate fork scoping could silently widen; it is now clamped to `RULINGS_END` (tests/in-run-rulings/run-tests.sh ~163-164) → 0182b1b ← 1/4: r4:M3
- [M12] carried — the new suite is named in no tracked file (only the untracked CLAUDE.md), so a fresh clone has nothing instructing anyone to run it; the plan's Rollout defers the RELEASE-NOTES.md entry to the release commit ← 1/4: r4:M5

_Completed — 2026-09-04 — cap reached — HEAD 0182b1b3799deac4759fcfc21b616df265c5ce28_

#### Post-loop addendum — 2026-09-04 — decisions on invocation 1's open items

The effective HEAD had moved past this entry's completion marker
(bf3d7de vs 0182b1b) before this addendum was written, so the marker above
is left unchanged, the verification re-review is skipped, and invocation 2
below reviews the fix.

- [I5] decided (orchestrator): amend plan — already done in bf3d7de (the plan's Task 2 contract states a five-entry read list and carries its audit note; spec R2, R5, R7 and R1 amended); fix it — restore read entry 5 (the ruling record `<topic folder>/plans/<slug>-open-decisions.md`) and add a fourth guard: a user's decision is never overturned by a ruling, an earlier `(user)` answer on the same clause escalates the item under the class that first sent it to the user, and an unsure clause match escalates as well
- [I5] fixed — read entry 5 restored in `### What may be read — the classification read exception`; guard 4 added and the count word changed to "Four"; the two `**Follow-up:** … — clause:` passages withdrawn in 119b69a restored, because guard 4's "recorded quote" has no referent without them; range-scoped assertions added in the read-exception and guard ranges (skills/orchestrating-development/SKILL.md, tests/in-run-rulings/run-tests.sh) → d725f17
- [I2] decided (orchestrator): amend plan — already done in bf3d7de (spec R1 and R5 amended); fix it — widen the `irreversible` entry of the closed escalation list so it also covers an `amend plan` answer whose amendment would edit the plan's binding text, adding no sixth class, and bound the plan-amendment procedure so an amendment never deletes binding text outright
- [I2] fixed — the `irreversible` entry now covers an `amend plan` answer whose amendment would edit a `**Global Constraints:**` block or an `**Exact content:**` block, triggered by the edit location the disposition line names after `— clause:` and never by a weakening judgement; the label set is unchanged; the plan-amendment procedure now keeps the clause and appends an exception scoped to the item the ruling names; assertions added in the classification and plan-amendment ranges (skills/orchestrating-development/SKILL.md, tests/in-run-rulings/run-tests.sh) → d725f17

`bash tests/reviewer-templates/run-tests.sh` (24 passed), `bash tests/writing-plans/run-tests.sh` (15 passed) and `bash tests/in-run-rulings/run-tests.sh` (156 passed) all exit 0 at d725f17.

---

_Invocation 2 — 2026-09-04 — N=4 M=4 — BASE..HEAD 0a57e40..d725f17 — branch feature/autonomous-in-run-decisions — gate: orchestration_

Carried Minor findings supplied to round 5 from `.superpowers/sdd/progress.md`.

## Round 5 — Correctness & spec alignment — opus
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 3 Important, 3 Minor | r2: 0 Critical, 4 Important, 4 Minor | r3: 0 Critical, 2 Important, 4 Minor | r4: 0 Critical, 3 Important, 6 Minor
**Sources mapped:** 29/29
**Reviewer verdict:** 0 Critical, 8 Important, 10 Minor
**Converged:** no
### Dispositions
- [I1] fixed — the widened `irreversible` entry was never reconciled with its neighbours: the answers bullet still called `amend plan …; fix it` the only accepting answer for binding text, the Phase 3 answer sentence and the pre-commit self-check steered the orchestrator into it, and the amendment procedure still told it to replace a Global Constraints entry; all four now route a binding-text edit location to `escalated (irreversible)` and the procedure is scoped to the amendments that remain the orchestrator's (SKILL.md ~962/~1001/~1033-1075/~1108-1111) → c2f2f32 ← 4/4: r1:I2, r2:I1, r3:I1, r4:I2
- [I2] fixed — the batch template's `## Resume Answer` heading said "omit only when no answer applies to this batch" while its own placeholder documentation says "omit only when the run has recorded no answer at all"; the heading now states the placeholder's condition (batch-controller-prompt.md ~73) → c2f2f32 ← 4/4: r1:M1, r2:I2, r3:M3, r4:M5
- [I3] user-decision — the branch amends its own requirements document: spec R1, R2, R5 and R7 carry `(amended by ruling <n>)` markers and a new `## Amendments` section attributes them to orchestrator rulings 1 and 2, while the rule this branch ships classes a spec change as `escalated (spec wrong)` — the spec author's decision, never the orchestrator's; nothing in the spec or the skill defines how a spec amendment is recorded or by whom, and the plan's File Structure table does not list the spec as a file this plan modifies — at docs/superpowers-orchestrator/2026-09-02-autonomous-in-run-decisions/specs/autonomous-in-run-decisions-design.md:161 — clause: Task 1 "`spec wrong` — requires changing the spec, that is, changing what \"done\" means for this run" ← 2/4: r1:I3, r4:M6
- [I4] fixed — Resume step 3 built `[RESUME_ANSWER]` from the `## STOPPED` entry's `Ruled:` lines, which cover the stopped unit only, while "The Phase 3 answer set — one rule" requires every ruled `[task <n>/<k>]` line of the run on every batch dispatch; the `Ruled:` build is now labelled the Phase 4 path and Phase 3 uses the run-wide set (SKILL.md ~498) → c2f2f32 ← 2/4: r2:I3, r4:I3
- [I5] fixed — the crash-window rebuild of a missing `## STOPPED` entry took only ruling-record entries numbered at or above `<n>`, while "Handling a return as a whole" requires every non-escalated ruling of the stopped unit, earlier returns included; the rebuild now mirrors that rule and excludes entries already carrying a `**Follow-up:**` line (SKILL.md ~486) → c2f2f32 ← 2/4: r3:I2, r4:M1
- [I6] fixed — the Phase 3 discriminator required the `<n>` of `BLOCKED task=<n>` to be present in `[TASK_LIST]`, but a Pre-Flight Plan Review runs over the whole plan and returns the lowest-numbered task the conflict touches, which is often outside the batch; the bound is now the plan (a `### Task <n>` heading), stated once in the skill and once in the batch template (SKILL.md ~673, batch-controller-prompt.md ~63) → c2f2f32 ← 1/4: r1:I1
- [I7] fixed — guard 4 escalated a user-decided clause "under the class that first sent it to the user", but a `**Follow-up:**` line recorded on a `forced` or `design` entry carries no escalation class, so the guard had no label from the closed list to write; it now falls back to `spec wrong` (SKILL.md, `### Guards against motivated judgement`) → c2f2f32 ← 1/4: r2:I4
- [I8] fixed — the "Normalization is one rule" paragraph named a different unit in each file: the whole plan text at `<plan location>` cut to 160 characters in multi-code-review, one sentence or one list entry in orchestrating-development, so the loop and the orchestrator disagreed about whether wording was decided; multi-code-review now states the sentence-or-list-entry unit in the same words (multi-code-review SKILL.md ~904) → c2f2f32 ← 1/4: r4:I1
- [M1] fixed — the lost-return rule mandates `forks: <k> of <planned>` but neither the ruling record's `**Forks:**` field nor the `## RULING` `Forks:` line had a slot for it; the slot was added to both shapes in both copies (SKILL.md ~379/~884/~914/~1094) → c2f2f32 ← 2/4: r2:M2, r3:M2
- [M2] fixed — `unresolved: fix contradicts binding text — "<clause>"` quoted the clause in the disposition text while the self-sufficient-line rule already requires a `— clause:` suffix, so the clause appeared twice with no statement of which one governs; the disposition text no longer carries it (multi-code-review SKILL.md ~971) → c2f2f32 ← 2/4: r2:M3, r3:M4
- [M3] fixed — the discriminator's parenthetical "on a first dispatch, which carries no answers" contradicted the run-wide answer set, which fills every dispatch; reworded to key on the absence of an answer line for that task (SKILL.md ~682) → c2f2f32 ← 1/4: r1:M2
- [M4] fixed — the cap counted `## RULING` entries "of the same `[task <n>]`" while Phase 3 items are written `[task <n>/<k>]`, so a literal match counted zero and a task never reached `escalated (chain)`; the count is now per task number, matching both forms (SKILL.md ~1146) → c2f2f32 ← 1/4: r1:M3
- [M5] fixed — the ruling record's `**Item:**` template named a Phase 3 item `[task <n>]`, which the open-item definition fixes to mean `[task <n>/1]`, so a ruling on `### Question 2` was recorded against section 1; the template now uses `[task <n>/<k>]` (SKILL.md ~911) → c2f2f32 ← 1/4: r2:M1
- [M6] fixed — the ruling record said a `**Follow-up:**` line is appended "For an `escalated` item" while Resume step 3 appends one for every user answer, including answers replacing a `Ruled:` line on a `forced` or `design` entry; the record sentence now covers any entry the user later answers (SKILL.md ~929) → c2f2f32 ← 1/4: r2:M4
- [M7] fixed — a reverted amendment restored the binding clause "to the text the note says it was changed from", but the audit note's shape is free prose and need not quote the original; the original is now recovered from the ruling commit with `git show <ruling commit>^:<plan path>` (SKILL.md ~505) → c2f2f32 ← 1/4: r3:M1
- [M8] fixed — the commit-landed check used `git log --grep "<slug> ruling <n>"`, an unanchored regular expression that also matches `ruling <n> follow-up` and `ruling <n><digit>`; it now compares the whole subject with `git log --format=%s` (SKILL.md ~469) → c2f2f32 ← 1/4: r4:M2
- [M9] fixed — the `irreversible` entry enumerated binding text as the two block forms while the plan-amendment procedure adds "any mandated text" for a pre-7.7.0 plan, leaving such an amendment binding by one definition and untriggered by the other; the entry now uses the single definition from "Plan amendment" (SKILL.md ~643) → c2f2f32 ← 1/4: r4:M3
- [M10] fixed — "the trigger is the edit location … never a judgement" could not be applied to a `Task <n>` location, which covers both binding and reference text; the entry now says a `Task <n>` location's binding-ness is read from the task section under read-exception entry 3, and that "never a judgement" bars only judging the amendment's effect (SKILL.md ~646) → c2f2f32 ← 1/4: r4:M4
- carried [1] carried — unused helpers and path variables in the new suite (ship-as-is 4/4; every reviewer reports it resolved on the branch)
- carried [2] carried — section 2 adds two fragment checks beyond the Contract's listed invariants (ship-as-is 4/4)
- carried [3] carried — `FORK_END` scanned to EOF, unbounded by `RULINGS_END` (ship-as-is 4/4; reported resolved, the value is now clamped)
- carried [4] carried — the `clause:` label wording is inherited from multi-code-review and a later respelling needs a matching update (ship-as-is 4/4)
- carried [5] carried — the `## RULING` template's `Items:` annotation sat inline after a run of spaces (ship-as-is 4/4; reported resolved)
- carried [6] carried — Phase 3 step 5 stated the open-item re-dispatch without Phase 4's escalated carve-out (ship-as-is 4/4; reported resolved)
- carried [7] carried — Resume step 3 stated the `**Follow-up:**` append twice (ship-as-is 4/4; the second mention is now the idempotence clause)
- carried [8] carried — the pre-flight negative check lacked its sibling's empty-range guard (ship-as-is 4/4; reported resolved)
- carried [9] carried — the `## RULING` block is defined twice and the two copies differ in the `Detail:` path form (ship-as-is 4/4; both ranges are pinned, so drift fails the suite)
- carried [10] carried — the `## STOPPED` `Owed probe:` sentence refers to the review log and is vacuous for a Phase 3 stop (ship-as-is 4/4; it yields zero lines rather than a wrong instruction)
- carried [11] fixed — the batch template stated the pre-flight lowest-numbered-task and report-file rule twice and the two copies had begun to diverge; the rule is now stated once in Deviation 1 and the First-batch parameter points at it (fix-before-merge: 2 of 4 reviewers) → c2f2f32

## Round 6 — Adversarial red-team — opus
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 4 Important, 3 Minor | r2: 0 Critical, 4 Important, 4 Minor | r3: 0 Critical, 4 Important, 1 Minor | r4: 0 Critical, 4 Important, 2 Minor
**Sources mapped:** 26/26
**Reviewer verdict:** 0 Critical, 12 Important, 8 Minor
**Converged:** no
### Dispositions
- [I1] fixed — the amendment-revert step named `git show <ruling commit>^:<plan path>` while no step produced that hash, and the landed-check printed subjects only through an unanchored `--grep` that also matches `ruling 10` and `ruling <n> follow-up`; the revert now derives the hash with an exact whole-subject filter that must leave exactly one commit, and states that only the clause's own text is copied out of the printed plan (SKILL.md ~469/~521) → a8e6c2b ← 3/4: r1:I4, r2:M1, r3:I3
- [I2] fixed — Deviation 1 keyed "a task's first dispatch of this run" on the absence of a `[task <n>…]` answer line, which the run-wide answer set guarantees is present for any task an earlier ruling named, so an abandoned run's stale report sections survived and were later ruled on as live open items; the test now keys on work this run produced (batch-controller-prompt.md ~90) → a8e6c2b ← 2/4: r1:I2, r2:I2
- [I3] fixed — the lost-return bound covered only one missing notice or none arriving, so two missing notices with one arrival matched no rule and an unattended run waited forever; the bound is now stated over the round — once any notice arrives every still-missing lens counts as a loss at the same moment and is re-dispatched once (SKILL.md ~919-927) → a8e6c2b ← 2/4: r2:I1, r3:I1
- [I4] fixed — the loop was told to refuse a bare `fix it` whose clause "names binding plan text" while `binding` was never defined in its own file and the loop controller has no orchestrator context, so a `Task <n>` clause of ordinary reference prose deadlocked the two actors until the cap chain-escalated it; the binding test now sits beside the refusal rule, with an unsure reading resolving to reference text (multi-code-review SKILL.md ~971) → a8e6c2b ← 2/4: r3:I4, r4:I2
- [I5] fixed — the `irreversible` trigger was defined only through a Phase 4 disposition line's `— clause:`, which a Phase 3 conflict section does not have, so one agent would rule an autonomous Global Constraints amendment and another would escalate every Phase 3 amendment; the entry now states its Phase 3 form (SKILL.md ~664) → a8e6c2b ← 1/4: r1:I1
- [I6] fixed — a settled pre-flight conflict was identified by its section number while the numbering rule allocated a new one whenever the report file had been rewritten, so the same conflict could be ruled three times and burn the per-task cap; a re-derived conflict now re-uses the `<k>` of the answered line naming the same plan text (batch-controller-prompt.md ~66/~86, SKILL.md) → a8e6c2b ← 1/4: r1:I3
- [I7] fixed — nothing fixed the order of the three writes inside a ruling, so a crash could leave an orphan `(amended by ruling <n>)` marker with no ruling record, which the loop then treats as decided wording for the rest of the run, and the next ruling reuses the number; the order is now stated and Resume reverts a marker with no matching record (SKILL.md) → a8e6c2b ← 1/4: r2:I3
- [I8] fixed — taking `design` items one at a time means a later item's forks inherit the earlier item's verdicts and the orchestrator's consolidation, which the anchoring guard never covered; only the first `design` item of a return now uses the fork path and later reviewers go on the fresh `general-purpose` path (SKILL.md ~807/~812-818) → a8e6c2b ← 1/4: r2:I4 (harness field dropped: repository-readable)
- [I9] fixed — the bare `[task <n>]` form was permitted on a Phase 3 `Open:` line while meaning exactly `[task <n>/1]`, so a user answering the id they were shown for section 2 silently replaced the ruling on section 1 and reverted its amendment; the orchestrator now always writes `[task <n>/<k>]` and a bare user answer is resolved, never defaulted (SKILL.md ~403/~613) → a8e6c2b ← 1/4: r3:I2
- [I10] fixed — the writer's rule replaces a `"` inside quoted plan text with `'` but the single normalization rule the consumers apply listed only the two separator replacements and the 160-character cut, so every clause containing a double quotation mark failed the prefix test in guard 1, the loop's decided-wording test and the amendment lookup at once; the substitution is now the rule's third operation in both files and the amendment lookup escalates on no match (multi-code-review SKILL.md ~903, SKILL.md ~1007) → a8e6c2b ← 1/4: r4:I1
- [I11] fixed — the read exception was scoped to "when classifying an open item" while Phase 5 step 3 and Resume step 3 both require reads it does not name, so one agent drops the Phase 5 accountability output and another breaks the closed list; the exception now names its three purposes and entry 4 permits the two ruling-commit commands, with five entries and "Nothing else" kept (SKILL.md ~340/~489-521/~738) → a8e6c2b ← 1/4: r4:I3
- [I12] rejected: harness probe not runnable here — dispatch one throwaway `subagent_type: "fork"` that terminates without emitting a final message and observe whether the main session receives a failure/error notice or waits with no event — (would break a constraint) — the fork wait relies on the platform reporting a failed dispatch when no notice of the round arrives at all; the probe needs a subagent arranged to return nothing and an observation of the main session, neither of which one controller-safe dispatch can produce. [I3]'s fix rewrites the same paragraph with a bound stated over the round ← 1/4: r4:I4
- [M1] fixed — the crash-window rebuild of a missing `## STOPPED` entry excluded ruling-record entries carrying a `**Follow-up:**` line while the normal path did not, so the two paths the text promises are identical produced different entries; the exclusion is now stated on both (SKILL.md ~491/~1189) → a8e6c2b ← 2/4: r1:M1, r4:M1
- [M2] fixed — the post-loop `unresolved: fix contradicts binding text` line was not among the addendum shapes the no-annotation exception lists, so a reader would expect a source annotation a post-loop line cannot carry; the shape was added (multi-code-review SKILL.md ~842/~974) → a8e6c2b ← 1/4: r1:M2
- [M3] fixed — consolidation reasoning is written into the transcript before the tie-break fork is dispatched, and a fork inherits that transcript, so the no-preference rule did not protect the tie-break round; the tie-break reviewer now goes on the fresh `general-purpose` path and consolidation is written after the round returns (SKILL.md ~820/~892) → a8e6c2b ← 1/4: r1:M3 (harness field dropped: repository-readable)
- [M4] fixed — the cap matched a task number as a substring, so `[task 1]` also counted `[task 12/1]` and `[task 10]` and could stop a run early with `escalated (chain)`; the match is now the whole bracketed token (SKILL.md ~1194) → a8e6c2b ← 1/4: r2:M2
- [M5] fixed — "a `## RULING` entry whose `Re-dispatch:` line does not start with `none`" is false on its face, since the line starts with `Re-dispatch:`; all three places now test the value after the colon (SKILL.md ~481-486) → a8e6c2b ← 1/4: r2:M3
- [M6] fixed — guard 1 was satisfied by copying the `— clause:` field the loop writes onto every open-item line, so the reviewer supplied the orchestrator's own rejection justification; the guard now states that a quotable clause is not by itself a reason to reject (SKILL.md ~1240) → a8e6c2b ← 1/4: r2:M4
- [M7] fixed — guard 4 compared clauses without excluding `— clause: none`, so under one reading every clause-less item after the first would be escalated without being decided, switching off the feature's autonomy; the guard now compares only non-empty clauses (SKILL.md, guard 4) → a8e6c2b ← 1/4: r3:M1
- [M8] fixed — the fork git allowlist forbade `--ext-diff` and `--textconv`, but external diff drivers and textconv filters are on by default for `git diff`, so omitting the positive flags granted no protection; both copies now mandate `--no-ext-diff --no-textconv` (SKILL.md ~774/~871) → a8e6c2b ← 1/4: r4:M2

## Round 7 — Security — opus
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 1 Important, 1 Minor | r2: 0 Critical, 1 Important, 1 Minor | r3: 0 Critical, 1 Important, 0 Minor | r4: 0 Critical, 3 Important, 2 Minor
**Sources mapped:** 10/10
**Reviewer verdict:** 0 Critical, 5 Important, 3 Minor
**Converged:** no
### Dispositions
- [I1] fixed — the `secret` class was the only thing still keeping a committed credential with the human once an `unresolved` count stopped ending the run, yet its trigger was free text and the loop template pinned no disposition wording, so a leaked token logged without the word "secret" fell through to `forced`/`design` and could be closed with `accept:`; Deviation 3 now pins the leading form `unresolved: exposed secret or credential in an orchestration artifact — <file:line>`, states the real mechanism instead of the removed count-based stop, and the `secret` entry repeats the form byte for byte (code-review-loop-prompt.md ~107, SKILL.md ~708) → 885b84b ← 2/4: r1:I1, r4:I1
- [I2] fixed — `(amended by ruling <n>)` was a self-asserted authority token: the loop read a clause carrying it as decided wording on the marker alone, and the orphan-marker check existed only on the Resume path, while the batch controller commits the plan on every task completion; a marker is now authority only while the ruling record holds a matching `## Ruling <n>` entry, stated in both files, and an unbacked marker is reference text (multi-code-review SKILL.md ~652, SKILL.md ~482-488) → 885b84b ← 2/4: r2:I1, r4:M1
- [I3] fixed — the `secret` trigger was stated in Phase-4-only terms and closed the producer set to one, while this branch's batch template makes a `### Question <k>` or `### Conflict <k>` section a second producer and Phase 3 is newly in scope for autonomous rulings, so a credential named in a report section would be ruled on without a stop; the trigger now covers both phases and names two producers (SKILL.md ~708) → 885b84b ← 1/4: r3:I1
- [I4] fixed — the fork prompt's read-only clause said "write nothing, dispatch nothing, run no other command" with no send-nothing clause, while it embeds a disposition line and a task-report section verbatim and "run no other command" bounds shell use only; the sibling reviewer template's send-nothing wording was copied in (SKILL.md ~930) → 885b84b ← 1/4: r4:I2 (harness field dropped: repository-readable)
- [I5] fixed — the ruling boundary is declared a deliberately dirty tree and the ruling commit states explicit-path staging, but neither `stopped` commit site did and no general boundary rule existed, so an agent could sweep a blocked task's half-finished edits into the stop commit; a staging paragraph was added to the Major-Error Stop Policy and referenced from both sites (SKILL.md ~506/~1279) → 885b84b ← 1/4: r4:I3
- [M1] fixed — the no-delete bound applied to binding text only, so a safety rule written as ordinary reference prose in a task section could be replaced wholesale by a `forced` ruling no fork ever reads; the bound now covers every amendment, binding and reference text alike (SKILL.md ~1178) → 885b84b ← 1/4: r1:M1
- [M2] fixed — "Never reproduce a secret" opened with "every line written about it" and then closed the list to three lines, leaving the two heading lines and the `**Resolution:**` field of the same committed entries unnamed; the universal statement now leads, the list is declared open, and the three fields were added (SKILL.md ~1015) → 885b84b ← 1/4: r2:M1
- [M3] carried — the spec's `secret` entry names `code-review-loop-prompt.md` Deviation 4 as the producer while the secret exception is in Deviation 3; not fixed here because the spec file is the subject of round 5's open item [I3], which asks whether this run may amend its own requirements document at all, and editing it again before that is settled would compound the question ← 1/4: r4:M2

## Round 8 — Test & coverage quality — opus
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 0 Important, 6 Minor | r2: 0 Critical, 2 Important, 4 Minor | r3: 0 Critical, 2 Important, 6 Minor | r4: 0 Critical, 0 Important, 5 Minor
**Sources mapped:** 25/25
**Reviewer verdict:** 0 Critical, 3 Important, 8 Minor
**Converged:** no
### Dispositions
- [I1] fixed — the Phase 3 wiring assertion asked only for the fragment `In-run rulings` in a range that holds two independent mentions, so deleting the routing sentence it is named after left the suite green (three reviewers confirmed by mutation); the three phase-routing assertions now pin folded bytes of their own routing sentence (tests/in-run-rulings/run-tests.sh ~543-547) → d400822 ← 3/4: r1:M1, r2:I2, r3:I1
- [I2] fixed — the "carries no `*` emphasis marker" check took `substr(hay, index(hay, needle), length(needle))`, which is byte-identical to the needle, so its `index(span, "*")` test could never be true and bolding the whole cap sentence kept the suite at 244 passed; the check now inspects the characters touching the match on each side (tests/in-run-rulings/run-tests.sh ~460-467) → d400822 ← 1/4: r2:I1
- [I3] fixed — the `Ruled:` and `Open:` log-format pins also matched the prose below the example block, so deleting the block's field lines left both green; each pin now carries the whole field line including its placeholder tail (tests/in-run-rulings/run-tests.sh ~549) → d400822 ← 1/4: r3:I2
- [M1] fixed — four new rules carried no assertion, and one of them had already let the two files diverge: the loop states that a `## Ruling <n>` heading is compared as a whole number while the orchestrator's twin paragraph did not, so a `## Ruling 10` entry would have backed an `(amended by ruling 1)` marker; the qualifier was added to the orchestrator paragraph and pinned on both sides, together with pins for the batch template's section-number allocation and no-renumber rules, its run-wide `[RESUME_ANSWER]` scope, and the loop template's untagged-line rule (SKILL.md, tests/in-run-rulings/run-tests.sh) → d400822 ← 4/4: r1:M3, r1:M5, r2:M3, r3:M5, r3:M6, r4:M4
- [M2] fixed — the emphasis check folded its range without stripping leading whitespace while the helper beside it did, so an indented cap sentence would fail one check and pass the other with a message naming the wrong cause; both now share one `fold_range` helper and the two outcomes have distinct messages (tests/in-run-rulings/run-tests.sh ~460) → d400822 ← 4/4: r1:M4, r2:M2, r3:M2, r4:M3
- [M3] fixed — the Guard Interaction pair tested nothing about forks: the exact marker pin already matched pre-branch text and its companion was the bare fragment `fork`, which matches any word containing it; two folded pins now carry bytes of the new fork sentence and the marker pin is kept, and described, as an unchanged-wording regression pin (tests/in-run-rulings/run-tests.sh ~316-318) → d400822 ← 3/4: r1:M2, r3:M3, r4:M1
- [M4] carried — the suite is named in no tracked file, so a fresh clone has nothing pointing at it; the plan's Non-goals keep `RELEASE-NOTES.md` out of this branch and the repository has no CI, so the tracked reference is owed at the release step (the same item was carried in round 4 as [M12]) ← 3/4: r1:M6, r2:M4, r4:M5
- [M5] fixed — the negative Resume check forbade every occurrence of `decided (user)` in its range, while the same suite requires the `decided (user)` or `decided (orchestrator)` enumeration in the sibling files, so writing that correct clarification into Resume step 3 would have turned the suite red; the check now fails only on a `decided (user)` whose own line lacks `decided (orchestrator)` (tests/in-run-rulings/run-tests.sh ~566-573) → d400822 ← 1/4: r2:M1
- [M6] fixed — five fork pins were scoped to the whole `## In-run rulings` range although `FORK_LINE`/`FORK_END` were computed a few lines below, against the suite's own stated convention; the computation moved above the loop and all seven fork pins now use it (tests/in-run-rulings/run-tests.sh ~287) → d400822 ← 1/4: r3:M1
- [M7] fixed — with both range anchors empty the interloper scan's `awk` comparisons made the range vacuous and the check printed PASS naming an empty range; it is now guarded by a non-empty and start-before-end test (tests/in-run-rulings/run-tests.sh ~143) → d400822 ← 1/4: r3:M4
- [M8] fixed — the two bare `(user)` needles in the loop-prompt pin lists already matched pre-branch text, so the per-line tagging could be reverted with both still green; each is now a byte pin that exists only in the new tagging wording (tests/in-run-rulings/run-tests.sh ~722/~731) → d400822 ← 1/4: r4:M2

## Round 8 verification 1 — Test & coverage quality — opus
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 0 Important, 2 Minor | r2: 0 Critical, 0 Important, 7 Minor | r3: 0 Critical, 3 Important, 5 Minor | r4: 0 Critical, 0 Important, 5 Minor
**Sources mapped:** 22/22
**Reviewer verdict:** 0 Critical, 3 Important, 12 Minor
### Dispositions
- [I1] fixed — the ruling record's six field labels and the `## RULING` example block's `Items:` and `Detail:` lines were pinned nowhere, so a reviewer deleted the record's fields and the example's header with the suite still green, although guard 4, the Resume rebuild and the marker-backing check all read those fields; whole-field-line pins were added in both subsection ranges (tests/in-run-rulings/run-tests.sh) → 4f45514 ← 2/4: r2:M7, r3:I3, r3:M2
- [I2] fixed — the sentence mapping a `[RESUME_ANSWER]` line's `(orchestrator)`/`(user)` tag to the `decided (<who>)` disposition was pinned only through tokens that other sentences in the same 187-line range also produce, so a reviewer restored the pre-branch wording and the suite stayed green; two folded pins now cover the mapping and the untagged-line rule (multi-code-review side) → 4f45514 ← 1/4: r3:I1
- [I3] fixed — the two orchestrator-answer mappings (an `amend plan …; fix it` answer taking the finding-governs path with the re-review skipped, and `accept:` deciding an item without a code change) had no assertion and were deleted green in a mutation; four pins now cover the disposition shape and both consequences. The plan's Task 7 Invariants list omits them, but that list is a minimum, not a maximum, so the gap is a coverage defect rather than a plan conflict → 4f45514 ← 1/4: r3:I2
- [M1] fixed — the three "stop policy forbids <X>" checks asserted only that the command strings appear in the range, so text recommending `git add -A` would satisfy them identically, and `git add .` is a substring of any explicit-path command starting with a dot; a folded pin on the forbidding sentence was added and the three bare pins kept as weaker spelling checks → 4f45514 ← 3/4: r2:M1, r3:M4, r4:M2
- [M2] fixed — the two folded helpers and the emphasis block passed the needle and the whole folded haystack through `awk -v`, which processes escape sequences, so the first backslash added inside a pinned range would silently rewrite the text compared and some awk builds would also break the suite's clean standard error; all three now pass the values through the environment → 4f45514 ← 2/4: r2:M5, r4:M4
- [M3] carried — the suite is still named in no tracked file; the plan's Non-goals keep `RELEASE-NOTES.md` out of this branch and defer the tracked reference to the release step (carried in round 4 as [M12] and in round 8 as [M4]) ← 2/4: r2:M6, r3:M5
- [M4] fixed — the Phase 3 answer-set rule, which is what makes a ruling survive a stop and reach a later batch, had no assertion of its own: the only check naming it was satisfied by the cross-reference in the Resume range; an exact and a folded pin were added on the definition, with the Resume check kept as the cross-reference pin → 4f45514 ← 1/4: r1:M1
- [M5] fixed — three rules were deletable with the suite green: Phase 5's `accept:` listing requirement (its sibling `contradiction: unsettled` was pinned), the cap's reset anchor, and Phase 3 step 5's "writes no batch entry" on which the Resume `## RULING` case depends; one scoped pin each → 4f45514 ← 1/4: r1:M2
- [M6] fixed — the batch-template first-dispatch pin omitted the rule's negation, so deleting the words "never by" inverted the first-dispatch signal with the check still green; the folded pin now carries the negation → 4f45514 ← 1/4: r2:M2
- [M7] fixed — three sentences that keep the escalation predicate closed had no assertion: the `forced` test's "indefensible", the transient-external-problem exclusion, and "Phase 5 stays the user's"; three scoped fragment pins were added → 4f45514 ← 1/4: r2:M3
- [M8] fixed — the fork subsection's numbers were unpinned: the three-fork default, the two-fork condition, three of the four lens names and the "fewer than two usable fork returns" threshold, so a silent change from three forks to one would pass; all are now pinned → 4f45514 ← 1/4: r2:M4
- [M9] fixed — the Phase 4 answer vocabulary was pinned by bare tokens that recur elsewhere in the same 186-line range, so deleting the whole definition list — including the rule that a bare `fix it` never authorises a fix against binding text — stayed green; each bullet's own defining bytes are now pinned → 4f45514 ← 1/4: r3:M1
- [M10] fixed — the emphasis check read only the single character on each side of the match in the folded text, and the fold inserts a space at every line wrap, so a marker separated from the sentence by a wrap read as clean; the check now steps over fold-inserted spaces and distinguishes an opening marker from a preceding bold span → 4f45514 ← 1/4: r3:M3
- [M11] fixed — the `(user)` needle in the resume pin loop matched pre-branch wording, so removing the per-line tagging left it green; it is now a folded pin on the enumerating bytes of Resume step 3's construction → 4f45514 ← 1/4: r4:M1
- [M12] fixed — three needles of the log-format loop were short prefixes rather than the whole field lines its comment claims, and one of them matched pre-branch prose; two were extended to whole example lines, the third moved out of the loop with a corrected comment, and the short labels kept as weaker checks → 4f45514 ← 1/4: r4:M5

## Round 8 verification 2 — Test & coverage quality — opus
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 0 Important, 4 Minor | r2: 0 Critical, 0 Important, 4 Minor | r3: 0 Critical, 0 Important, 7 Minor | r4: 0 Critical, 2 Important, 5 Minor
**Sources mapped:** 22/22
**Reviewer verdict:** 0 Critical, 2 Important, 12 Minor
### Dispositions
- [I1] fixed — the `**Handling a return as a whole.**` rule, which decides what happens to already-decided items when a run stops, had no assertion: deleting its paragraph left the suite green, because the two bare pins `Re-dispatch: none` and `Ruled:` match unrelated occurrences elsewhere in the file; folded pins on the paragraph's own bytes and on its bold label were added, the label because three other places cross-reference it by name (SKILL.md ~1322, tests/in-run-rulings/run-tests.sh section 5) → fca4a92 ← 2/4: r1:M3, r4:I1
- [I2] fixed — the fourth `decided (<who>)` generalisation the plan's Task 7 names sits at multi-code-review SKILL.md ~269, before every range section 7 scopes, so reverting it to `decided (user)` alone kept the suite green; a pin scoped to a new `## Workspace and Log`..`## Procedure` range now covers it (tests/in-run-rulings/run-tests.sh section 7) → fca4a92 ← 2/4: r3:M4, r4:I2
- [M1] carried — the suite is still named in no tracked file; the plan's Non-goals keep `RELEASE-NOTES.md` out of this branch and defer the tracked reference to the release step (carried in round 4 as [M12], in round 8 as [M4] and in verification 1 as [M3]) ← 3/4: r1:M4, r3:M7, r4:M4
- [M2] fixed — `assert_in_range` and the two anchor helpers passed the needle through `awk -v`, the exact mechanism the folded helpers' own comments say they avoid because `-v` processes escape sequences, leaving about 120 call sites where the first pin quoting `\|`, `\*` or a line continuation would be compared after silent rewriting; all three now use the `ENVIRON` form (tests/in-run-rulings/run-tests.sh ~47/~54/~74) → fca4a92 ← 2/4: r1:M1, r2:M2
- [M3] fixed — the only assertion that an `escalated` item stops the run was the bare word `escalated`, which any future sentence containing it would satisfy, and two of the three stop conditions this branch removes had no negative check; a byte pin on the escalated-stop sentence was added with the bare fragment kept as a spelling check, plus negative checks for `code-review unresolved` and a bare `batch-controller BLOCKED;` (tests/in-run-rulings/run-tests.sh ~823/~856) → fca4a92 ← 2/4: r2:M3, r3:M2
- [M4] rejected: the weaker pins are deliberate, documented spelling checks — round 8 verification 1's [M1] fix added a folded pin precisely while keeping the bare ones beside it, and the suite comments say why; a dominated pin costs one PASS line, never coverage — the reported redundancy at ~560/~641/~655/~767/~870 ← 2/4: r3:M1, r4:M2
- [M5] fixed — three check descriptions named a subsection or an antecedent their needle did not verify: two said `guard` while searching the log-entry range, and one promised the bare-`fix it`-on-binding-text antecedent while pinning only the token `escalated (irreversible)`; the two names now match the range searched and the third needle carries its antecedent (tests/in-run-rulings/run-tests.sh ~643/~650/~657) → fca4a92 ← 2/4: r3:M6, r4:M5
- [M6] fixed — the Phase 4 routing change had a positive pin only, so re-adding the old `unresolved > 0 → major error → stop` sentence beside the new routing sentence stayed green; a negative check in the Phase 4 range now forbids the old wording (tests/in-run-rulings/run-tests.sh ~730) → fca4a92 ← 1/4: r1:M2
- [M7] fixed — `CAP_SENTENCE_FOLDED` called `fold_range` with no anchor guard, so an empty `LOG_ENTRY_LINE` made awk's `NR >= a` a string comparison true for every line and the emphasis check printed PASS over a subsection that does not exist; both anchors are now guarded for empty and inverted ranges (tests/in-run-rulings/run-tests.sh ~613) → fca4a92 ← 1/4: r2:M1
- [M8] fixed — the fork return contract's two most consequential lines were unpinned: `ITEM: [<id>]`, whose loss the skill itself justifies because a mis-attributed verdict is committed as an authoritative ruling, and `CONTRADICTS:`, from which the pinned `contradiction: unsettled` field derives; both were added to the fork pin list (tests/in-run-rulings/run-tests.sh ~331) → fca4a92 ← 1/4: r2:M4
- [M9] fixed — the escalation predicate's closedness, the property that bounds what the orchestrator may decide without the user, was never asserted: the label loop checked presence only, so a sixth reason such as `deadline` kept every check green; a closedness scan over the classification range now fails on any label outside the five (tests/in-run-rulings/run-tests.sh ~197) → fca4a92 ← 1/4: r3:M3
- [M10] fixed — the emphasis-marker negative check took `index(hay, needle)` once and scanned around that one position, so a second, emphasised copy of the cap sentence in the same range would go undetected while the bare first copy reported clean; the check now scans every occurrence (tests/in-run-rulings/run-tests.sh ~614) → fca4a92 ← 1/4: r3:M5
- [M11] fixed — the idempotence rule's mechanism sentences, which are what makes an in-run resume after a crash safe, were deletable with the suite green: only the closing sentence was pinned, through the `durable marker` fragment; the two mechanism clauses are now pinned (SKILL.md ~1366, tests/in-run-rulings/run-tests.sh section 5) → fca4a92 ← 1/4: r4:M1
- [M12] fixed — the two folded helpers guarded an empty anchor but not an inverted range, so a collapsed range reported `not inside range` and read as missing wording rather than a broken anchor; the inverted-range branch from `assert_in_range` was copied into both with its own message (tests/in-run-rulings/run-tests.sh ~99/~125) → fca4a92 ← 1/4: r4:M3

## Round 8 verification 3 — Test & coverage quality — opus
**Reviewers:** M=4, usable 4/4
**Reviewer verdicts:** r1: 0 Critical, 0 Important, 6 Minor | r2: 0 Critical, 2 Important, 5 Minor | r3: 0 Critical, 1 Important, 4 Minor | r4: 0 Critical, 1 Important, 4 Minor
**Sources mapped:** 23/23
**Reviewer verdict:** 0 Critical, 4 Important, 15 Minor

Third and last verification cycle of round 8: it reviewed the cycle-2 fix
`fca4a92`, so no fix ships unreviewed. The cycle budget is spent (three
`## Round 8 verification <c>` entries), so the four Important findings it
raises cannot be fixed and re-reviewed inside this invocation and are
recorded `unresolved: verification cap`. All four are coverage gaps in
`tests/in-run-rulings/run-tests.sh`: the wording each names is present and
correct in the shipped files, and every reviewer confirmed that by mutation.
The Minor findings are carried for the same reason — a fix applied now would
ship unreviewed.

### Dispositions
- [I1] unresolved: verification cap — guard 2's two prohibitions `Never `plan governs`, never `accept`` are unpinned; only the fragment `a Critical is never rejected` is asserted, so a rewrite letting a Critical be closed with `accept:` and no code change would ship green — at tests/in-run-rulings/run-tests.sh:728 — clause: Task 5 "Invariants: `a Critical is never rejected`" ← 1/4: r2:I1
- [I2] unresolved: verification cap — the three stop-policy and Phase 3 "the old wording is gone" negative checks scan line by line while every positive check folds line wraps first, so a re-added stop rule that straddles a re-wrapped line break reports PASS — at tests/in-run-rulings/run-tests.sh:836 — clause: none ← 1/4: r2:I2
- [I3] unresolved: verification cap — the closedness scan added by verification cycle 2 reports PASS when its `^- ``<label>``` regex matches no bullet at all, so indenting or re-tabling the five escalation reasons and adding a sixth keeps the suite green while the predicate stops being closed — at tests/in-run-rulings/run-tests.sh:227 — clause: none ← 1/4: r3:I1
- [I4] unresolved: verification cap — nothing pins the link from the predicate's `design` class to the fork review; a mutation replacing `Decided after the fork review (below).` with `Decided directly, with no subagent.` left the suite at 300 passed, and every fork assertion is scoped to a subsection nothing would then reach — at tests/in-run-rulings/run-tests.sh:239 — clause: none ← 1/4: r4:I1
- [M1] carried — the suite is still named in no tracked file; the plan's Non-goals keep `RELEASE-NOTES.md` out of this branch and defer the tracked reference to the release step (carried in round 4 as [M12], round 8 as [M4], verification 1 as [M3] and verification 2 as [M1]) ← 3/4: r1:M1, r2:M4, r4:M4
- [M2] carried — the `state.md` example line `Rulings: <count> (last: ruling <n>, phase <p>)` is pinned by the bare label `Rulings:` only, the one example line in the suite that keeps the weak form (tests/in-run-rulings/run-tests.sh ~883) ← 2/4: r1:M4, r2:M2
- [M3] carried — the loop's unsure binding-text default, `treat the clause as reference text and apply the fix`, is unpinned while its deliberately opposite orchestrator counterpart is; a later harmonisation would invert it undetected (multi-code-review SKILL.md ~1010, tests/in-run-rulings/run-tests.sh ~1058) ← 2/4: r3:M4, r4:M2
- [M4] carried — the intro read-exception check asserts only the fragment `in-run rulings`, not that a read exception is named, so a second mention of the section in the intro would keep it green after the clause is deleted (tests/in-run-rulings/run-tests.sh ~320) ← 1/4: r1:M2
- [M5] carried — the Phase 4 negative check covers only the `user_decision > 0` disjunct of the removed sentence, so re-adding the `unresolved > 0` half alone reports PASS (tests/in-run-rulings/run-tests.sh ~836) ← 1/4: r1:M3
- [M6] carried — only the `## In-run rulings` range carries an interloper guard; the other eight ranges are heading pairs that merely happen to be adjacent today, so a new `## ` section between a pair would silently widen it (tests/in-run-rulings/run-tests.sh ~407/~811) ← 1/4: r1:M5
- [M7] carried — `[ "$FORK_END" -gt "$RULINGS_END" ]` compares before testing `RULINGS_END` is non-empty, so a missing `## Major-Error Stop Policy` heading prints a shell diagnostic on standard error instead of a clean FAIL (tests/in-run-rulings/run-tests.sh ~366) ← 1/4: r1:M6
- [M8] carried — Phase 3 step 5 lost the sentence `` `BLOCKED` → major error → stop `` but has positive routing pins only, so the old unconditional stop could be re-added beside the new sentence with the suite green (tests/in-run-rulings/run-tests.sh ~822) ← 1/4: r2:M1
- [M9] carried — the `secret`-class trigger form is written as two independent string literals, one per document checked, although the suite's own comment requires the two documents to carry it in the same bytes; one variable would enforce it (tests/in-run-rulings/run-tests.sh ~278/~1131) ← 1/4: r2:M3
- [M10] carried — the first-batch parameter check searches the whole head of the batch template rather than its `## Batch Parameters` block, so the phrase moving into Deviation 1 would still pass (tests/in-run-rulings/run-tests.sh ~1170/~1187) ← 1/4: r2:M5
- [M11] carried — the fragment pins `applied twice` and `handled as a whole` are substring tests that match the negated sentence as well, so inverting the rule keeps them green; the plan's Task 1 test step prescribes exactly this fragment list (tests/in-run-rulings/run-tests.sh ~241) ← 1/4: r3:M1
- [M12] carried — the negative bare-`decided (user)` check is line-based while the wording it guards may be reflowed, so a pure re-wrap that splits the enumeration across two lines would raise a false failure (tests/in-run-rulings/run-tests.sh ~912) ← 1/4: r3:M2
- [M13] carried — the mandatory `— at <file:line> — clause:` suffix on produced `user-decision` and `unresolved:` lines is asserted only against the documentation and the two example lines; the behavioural suite's disposition assertion still checks vocabulary alone (multi-code-review SKILL.md ~910, tests/claude-code/test-multi-code-review.sh) ← 1/4: r3:M3
- [M14] carried — the cap-sentence emphasis scan recognises `*` runs only, so italicising the sentence with `_` passes both it and the byte-exact pin, although the constraint says "without emphasis markers" (tests/in-run-rulings/run-tests.sh ~667) ← 1/4: r4:M1
- [M15] rejected: the weaker pins are deliberate, documented spelling checks — round 8 verification 1's [M1] fix added a folded pin precisely while keeping the bare ones beside it, and the suite comments say why; a dominated pin costs one PASS line, never coverage — the reported redundancy at ~600/~879 (same finding rejected in verification 2 as [M4]) ← 1/4: r4:M3

_Completed — 2026-09-04 — cap reached — HEAD fca4a92ec1544a2b27158d62a6d2fc700386fa56_
