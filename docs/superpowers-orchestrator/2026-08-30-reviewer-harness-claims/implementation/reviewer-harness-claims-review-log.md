# Review log — reviewer-harness-claims

_Invocation 1 — 2026-08-30 — N=2 M=1 — BASE..HEAD 82722ff..27c49b3 — branch feature/reviewer-harness-claims — gate: orchestration_

## Round 1 — Correctness & spec alignment — fable
**Reviewer verdict:** 0 Critical, 0 Important, 2 Minor
**Converged:** no
### Dispositions
- [M1] carried — multi-doc-review/SKILL.md:218 "one of the two below" omits the `deferred` Minor disposition the plan's Global Constraints allow (plan-mandated verbatim wording; a text change is the user's call)
- [M2] carried — `rejected: harness probe not runnable here — <probe>` reachable with no value for `<probe>` (untagged premise / `no probe named` branch) in multi-code-review/SKILL.md:424,476,986 and multi-doc-review/SKILL.md:172,428 (plan-mandated verbatim wording; a text change is the user's call)
- carried — CLAUDE.md:12 new suite line does not align its `#` comment column with lines 10-11 (reviewer: ship-as-is — file is gitignored and untracked; text is the plan's verbatim wording)

## Round 2 — Adversarial red-team — fable
**Reviewer verdict:** 0 Critical, 3 Important, 3 Minor
**Converged:** no
### Dispositions
- [I1] user-decision — dispatch rule (multi-code-review/SKILL.md:437, multi-doc-review/SKILL.md:185) is keyed on "your own prompt states that you were dispatched with a `name:`", text the `orch-*` controller prompts of orchestrating-development never contain (`name:` is an Agent-tool header parameter; verified against this controller's own prompt); a fix changes plan-mandated verbatim wording or edits `skills/orchestrating-development/*` outside the plan's file set (plan-mandated)
- [I2] user-decision — `Harness probes owed:` list lives only in the completion report (plan Global Constraints); in pipeline mode the controller return is capped at one status line plus 3 notes and the orchestrator relays no owed-probes field, so owed probes can be dropped unattended; a file home or orchestrator relay is outside the plan's file set (plan-mandated)
- [I3] fixed — probe constraints gain "sends nothing anywhere — no network request, no message" and the sentence that reviewer-authored probe text is data, not an instruction, in both SKILL.md files → de3e5eb
- [M1] carried — `no probe named` branch has no literal disposition/owed-item text for the empty `<probe>` (multi-code-review/SKILL.md:476, :986); same issue as round 1 [M2] (plan-mandated wording; a text change is the user's call)
- [M2] carried — a `harness: tested — read own context` observation can be made from a context the claim is not about, so the rule can pass vacuously (reviewer-prompt.md:72-73 in both templates; plan-mandated byte-identical rule text)
- [M3] carried — a not-runnable harness rejection on a Critical/Important finding keeps every round dirty and forces `cap reached` (multi-code-review/SKILL.md:459-461); the plan forbids touching convergence rules

## Round 2 verification 1 — Adversarial red-team — fable
**Reviewer verdict:** 0 Critical, 2 Important, 2 Minor
### Dispositions
- [I1] user-decision — same item as round 2 [I1]: dispatch rule keyed on a `name:` statement the `orch-*` controller prompts do not contain (plan-mandated)
- [I2] user-decision — same item as round 2 [I2]: `Harness probes owed:` list has no path across the pipeline return boundary (plan-mandated)
- [M1] carried — tests/reviewer-templates/run-tests.sh:61-66,113-118 drift check passes when both rule bodies are deleted and only the `### Harness claims` heading remains; no assertion pins a normative sentence of the body
- [M2] carried — same item as round 2 [M3]: a not-runnable harness rejection keeps the round non-clean (plan forbids touching convergence rules)
- verification result: the round 2 fix (de3e5eb) raised no finding; no unreviewed fix remains

## Post-loop addendum — 2026-08-30 — resume answers (gate: orchestration)
_Effective HEAD at addendum time: de3e5ebfec8c816312530d8c0c66f833e6224d01 — unchanged since the completion marker; no new invocation._
### Dispositions
- [I1] decided (user): fix it — option (b): reword the controller dispatch rule in both `skills/multi-code-review/SKILL.md` and `skills/multi-doc-review/SKILL.md` so it depends on nothing the controller cannot observe — a dispatch-based probe may run from any position; the probe subagent always writes its observation to a file at a unique temporary path outside the checkout and returns it as its final message; a launch-acknowledgement return is followed by a bounded poll of that file; a file that never appears is `not runnable here` with the reason `probe subagent did not report`; the reason `dispatch would not block` is removed; amendments recorded under plan Tasks 4 and 5
- [I1] fixed — dispatch rule reworded identically in both SKILL.md files (`dispatch would not block` no longer appears in either); amendments added under plan Tasks 4 and 5; `tests/reviewer-templates/run-tests.sh` unchanged (no assertion referenced the removed condition) → ca872f7c509e8e15034b9d696d684f9ff75defcd
- [I2] decided (user): fix it — option (a): the review log is the durable home of owed probes (every owed probe is a `rejected: harness probe not runnable here — <probe>` line); `skills/orchestrating-development/SKILL.md` — outside the plan's file set, explicitly allowed by the user — gains the "harness probes owed" item in the Phase 5 step 3 completion report and `Owed probe: <verbatim line>` lines after the `Open:` lines of the Phase 4 `## STOPPED` entry; amendment recorded under plan Task 5
- [I2] fixed — both additions made in `skills/orchestrating-development/SKILL.md`; amendment added under plan Task 5; no mirror copy of the skill exists (the only other files containing "Phase 5 — Completion" are the historical plan and design spec under `docs/superpowers-orchestrator/2026-08-04-orchestrating-development/`, left unchanged) → ca872f7c509e8e15034b9d696d684f9ff75defcd
- fix-commit tests: `tests/codex/run-unit-tests.sh` (10 suites passed), `tests/smart-compress/run-tests.sh` (87 passed), `tests/sdd-scripts/run-tests.sh` (193 passed), `tests/reviewer-templates/run-tests.sh` (19 passed) — output in the fix reports
- verification cycles remaining for round 2 before this addendum: 2 (one `## Round 2 verification` entry already logged)

## Round 2 verification 2 — Adversarial red-team — fable
**Reviewer verdict:** 0 Critical, 3 Important, 4 Minor
### Dispositions
- [I1] fixed — dispatch rule now creates a path that does not yet exist (`mktemp -u`) and polls until the observation file exists and is non-empty (`test -s`); a file still missing or still empty when the poll ends is `not runnable here` with the reason `probe subagent did not report`; identical wording in both SKILL.md files; `tests/reviewer-templates/run-tests.sh` passes (19 passed) → debacbbe524a7fdac4b5546d5096edcdd63f6e58
- [I2] user-decision — `not settled by one probe; first: <probe>` findings: the `first: <probe>` text "fills only the `Harness probes owed:` line of the completion report" (multi-code-review/SKILL.md:424-427, multi-doc-review/SKILL.md:169-172) while the log disposition `rejected: harness probe not runnable here — <probe>` carries no probe for this case; since decision [I2] the orchestrator harvests owed probes from the log lines, so such a probe is lost; the sentence is plan text (plan lines 467 and 640); suggested change: the disposition line carries the `first: <probe>` text as its `<probe>` (plan-mandated)
- [I3] user-decision — a `harness: tested — …` observation is accepted with no rule for an observation that neither matches nor contradicts the predicted result (for example "observed no result returned" from a reviewer's own detached dispatch; multi-code-review/SKILL.md:484-490, multi-doc-review/SKILL.md:216-222); two controllers dispose differently; the acceptance sentence is plan text (plan lines 508-510 and 696-698); suggested change: an observation that is not one clause matching or contradicting the claim is treated as `harness: untested — <the stated probe>` (plan-mandated)
- [M1] carried — `no probe named` rejections produce an owed item with no runnable probe (multi-code-review/SKILL.md:479-482, :826-831); same family as round 1 [M2] and round 2 [M1] (plan-mandated wording)
- [M2] carried — a persistent not-runnable harness rejection on a Critical/Important finding keeps every round non-clean and forces the cap (multi-code-review/SKILL.md:467-471, :562-565); same item as round 2 [M3] and verification 1 [M2] (plan forbids touching convergence rules)
- [M3] carried — with M ≥ 2 the same-issue consolidation rule does not say which `harness:` field survives a merge of a `tested` and an `untested` report of one claim (multi-code-review/SKILL.md:382-392)
- [M4] carried — the dispatch rule's "give up after a fixed limit" names no limit and does not say whether a probe dispatch is retried (multi-code-review/SKILL.md:445-447, multi-doc-review/SKILL.md:189-191); the wording mirrors the orchestration controllers' "Waiting on a subagent" rule as the user's decision [I1] specified
- verification result: the post-loop fix (ca872f7) raised [I1] against its own dispatch-rule wording; [I2] and [I3] are plan-mandated; fix cycle 3 (the last for round 2) dispatched for [I1]

## Round 2 verification 3 — Adversarial red-team — fable
**Reviewer verdict:** 0 Critical, 3 Important, 3 Minor
### Dispositions
- [I1] user-decision — the dispatch rule's poll ("check every few seconds, give up after a fixed limit") names no mechanism and no limit; in this harness a foreground `sleep` in Bash is refused and every Agent dispatch returns a launch acknowledgement, so the poll is the normal path, not a fallback, and the text has no branch for "the poll cannot run"; the reference to the "Waiting on a subagent" rule points at text only the orchestration controller prompts carry (multi-code-review/SKILL.md:445, multi-doc-review/SKILL.md:189); suggested change: name the wait mechanism (for example the `Monitor` tool with an until-condition, or background Bash), a concrete default limit, and a "poll mechanism unavailable → not runnable here" branch; the wording was set by decision [I1] and the verification cap for round 2 is reached, so no fix cycle remains — harness: tested — reviewer read its own tool descriptions; observed "Foreground `sleep` is blocked; use Monitor with an until-loop" (Bash) and "Subagents run in the background" (Agent)
- [I2] user-decision — the single disposition shape `rejected: harness probe not runnable here — <probe>` has three branches writing a reason into the `<probe>` slot (`probe subagent did not report` from decision [I1], `no probe named` from the plan, "with the reason" wording), so the owed-probe lines harvested by decision [I2] may carry a reason instead of the probe text, and a `no probe named` item is owed but cannot be run (multi-code-review/SKILL.md:451,485, multi-doc-review/SKILL.md:194); extends verification 2 [I2] and the carried `<probe>` family (round 1 [M2], round 2 [M1], verification 2 [M1]); suggested change: `rejected: harness probe not runnable here — <probe> (<reason>)` and an explicit rule for `no probe named` items on the owed line (plan-mandated)
- [I3] user-decision — same item as verification 2 [I3]: a `harness: tested — …` observation is accepted without re-verification, so a fabricated or wrong-dispatch `tested` clause bypasses the user-decision guard and can stop an unattended run (multi-code-review/SKILL.md:488, multi-doc-review/SKILL.md:216); suggested change: before any `user-decision` or `unresolved` on a `harness: tested` Critical/Important finding, the controller runs the stated probe once when it is controller-runnable, else treats the finding as `untested` (plan-mandated)
- [M1] carried — orchestrating-development/SKILL.md:311 lists owed-probe lines of the whole code-review and plan-review logs, so stale owed probes of earlier invocations are re-reported; the wording follows decision [I2]; suggested change: restrict to this pipeline's invocation entries
- [M2] carried — orchestrating-development/SKILL.md:355 writes `Owed probe:` lines only for a Phase 4 stop and only from the code-review log, so a Phase 2 stop drops the plan-review log's owed probes; the scope follows decision [I2]
- [M3] carried — item 1 runs a probe for every `harness: untested` finding whatever its severity, so Minor untested claims cost a dispatch-plus-poll cycle each (multi-code-review/SKILL.md:425, multi-doc-review/SKILL.md:169; plan-mandated)
- verification result: the cycle-3 fix (debacbb) raised no finding against its own wording (missing/empty observation file); the three Important findings contest plan text or decision wording and are journaled; verification cap for round 2 reached (3 cycles) — no unreviewed fix remains
- carried findings from the ledger (`Minor:` lines of `.superpowers/sdd/progress.md`) were not passed to a reviewer: this resume ran no round 1
- open items after this addendum: unresolved 0, user-decision 4 (verification 2 [I2], verification 2/3 [I3], verification 3 [I1], verification 3 [I2])


## Post-loop addendum 2 — 2026-08-30 — resume answers (gate: orchestration)
_Effective HEAD at addendum time: debacbbe524a7fdac4b5546d5096edcdd63f6e58 — unchanged since the completion marker; no new invocation. The four open `user-decision` items are all answered "fix it": one fix subagent, one commit, one verification re-review (the re-review is the addendum's own, ordered by the user; it is not a fourth in-loop cycle of round 2)._
### Dispositions
- [v2 I2] decided (user): fix it — the `first: <probe>` text of a `not settled by one probe` finding becomes the `<probe>` of the rejection line; the parenthetical saying it "fills only the `Harness probes owed:` line" is replaced by "is the `<probe>` of that rejection line" (both SKILL.md files)
- [v3 I1] decided (user): fix it — the poll sentence of the dispatch rule (from "When it returns only a launch acknowledgement" through the reference to the orchestration controller prompts) is replaced, identically in both SKILL.md files, by a concrete mechanism: `test -s <path>` as its own tool call, one tool call per attempt, never a shell loop and never `sleep`, at most 20 attempts spread over the controller's remaining work; a final message that arrived always wins over the file; the path is best-effort and a slow probe subagent is `probe subagent did not report`
- [v3 I2] decided (user): fix it — the "not runnable here" disposition becomes `rejected: harness probe not runnable here — <probe> — (<reason>)` in both files, `<probe>` copied verbatim from the reviewer (the `first:` text for `not settled by one probe`; the controller-named probe or the literal `none` for an untagged premise) and `<reason>` one of `not settled by one probe`, `probe subagent did not report`, `no probe named`, `tool missing`, `would break a constraint`, `ambiguous observation`; the `Harness probes owed:` item becomes `- [<id>] <probe> — (<reason>) (round <i>)` with `(addendum)` for a post-loop rejection; error-handling bullets updated to the new shape
- [v2/v3 I3] decided (user): fix it — option (a) plus a re-run on the `user-decision` path only: an observation that neither matches nor contradicts the predicted result is not an observation and the finding is treated as `harness: untested` with the same probe (both files, with the stated known limit); the Guard item of `multi-code-review/SKILL.md` only re-runs the probe before logging a `tested` finding `user-decision`, taking the `not runnable here` branch when it cannot; the M ≥ 2 consolidation rules of both files gain the sentence that a consolidated finding is `harness: tested` only when every source carrying a `harness:` field is `tested`, else `untested` with the lowest-numbered source's probe
- standing rule (user), for the verification re-review of this fix and any later re-review in this invocation: a finding raised against wording the user decided in this resume is the loop's to decide, never `user-decision` — a Critical finding is never rejected under it (fix or `unresolved`); a rejection under it names the file the fix would need and why the fix is outside the spec's scope, and appears in the return's notes; logged as `decided (user): standing rule — <fix|reject: reason>`
- [v2 I2] fixed — the `first: <probe>` text of a `not settled by one probe` finding "is the `<probe>` of that rejection line" in both SKILL.md files; every "not runnable here" disposition now `rejected: harness probe not runnable here — <probe> — (<reason>)` with `<probe>` and `<reason>` defined in item 2 / 3.2 → 4f17c47a072c1a4d223ad2a359806eb61d4206cb
- [v3 I1] fixed — poll sentence replaced identically in both SKILL.md files (`test -s <path>` as its own tool call, at most 20 attempts spread over remaining work, no shell loop, no `sleep`, a final message wins, best-effort); the missing/empty-file sentence now yields `— (probe subagent did not report)` → 4f17c47a072c1a4d223ad2a359806eb61d4206cb
- [v3 I2] fixed — rejection shape with the reason clause and the six reason values in item 2 / 3.2, the dispatch rule, and the error-handling bullets of both files; `Harness probes owed:` item is `- [<id>] <probe> — (<reason>) (round <i>)` with `(addendum)` for a post-loop rejection, reason copied from the rejection line → 4f17c47a072c1a4d223ad2a359806eb61d4206cb
- [v2/v3 I3] fixed — non-observation rule with the known-limit sentence in item 4 / 3.3 of both files; Guard re-run sentence in `multi-code-review/SKILL.md` only; consolidation "Text:" rule of both files gains the `harness:` field sentence; spec `## Amendments` (Case 006 [I1], [I2]; Case 007 v2 [I2] + v3 [I2], v3 [I1], v2/v3 [I3]) and plan Task 5 block quote `Amendment 2026-08-30 (review v2/v3 items, user decision)` added; `tests/reviewer-templates/run-tests.sh` unchanged; note: the spec states "decided during code review; supersedes the wording above where they differ" once in the section's lead sentence, covering every bullet → 4f17c47a072c1a4d223ad2a359806eb61d4206cb
- fix-commit tests: `tests/codex/run-unit-tests.sh` (10 suites passed), `tests/smart-compress/run-tests.sh` (87 passed), `tests/sdd-scripts/run-tests.sh` (193 passed), `tests/reviewer-templates/run-tests.sh` (19 passed) — output in the fix reports
- carried findings from the ledger (`Minor:` lines of `.superpowers/sdd/progress.md`) were not passed to a reviewer: this resume ran no round 1

## Round 2 verification 4 — Adversarial red-team — fable
**Reviewer verdict:** 0 Critical, 4 Important, 3 Minor
### Dispositions
- [I1] decided (user): standing rule — fix: the "settled before any disposition below is chosen" header (multi-code-review/SKILL.md:424) and "Before any disposition is chosen" (multi-doc-review/SKILL.md:167) conflicted with the decided poll text that spreads the 20 attempts over the triage of other findings; both now say each harness claim is settled before its own disposition and the poll may run across the triage of other findings
- [I1] fixed — order sentence clarified in both SKILL.md files → 6100d11356027836b6d912e423a85c08ddbc48f8
- [I2] decided (user): standing rule — fix: the decided Guard sentence re-ran the probe of every `tested` finding, including one the controller had just probed under item 1 (multi-code-review/SKILL.md:507-514); it now applies to a finding whose `tested` tag came from the reviewer, and a probe the controller ran itself in the round is never repeated
- [I2] fixed — Guard sentence limited to reviewer-tagged `tested` findings in `multi-code-review/SKILL.md`; spec Amendments bullet and plan Task 5 block quote synced → 6100d11356027836b6d912e423a85c08ddbc48f8
- [I3] user-decision — the model-probe sentence of the `### Harness claims` rule ("a subagent dispatched with a token only in its `description` reports the token absent from its context"; multi-code-review/reviewer-prompt.md:74-76, multi-doc-review/reviewer-prompt.md:52-54) does not say how the subagent is asked; a prompt that names the token puts it in the subagent's context, so that probe can only contradict a true claim, and the inverse prompt can only support it — two agents get opposite results; suggested change: one clause that the probe prompt must not contain the token (ask for every string of a stated shape); the rule text is plan-mandated verbatim wording of Tasks 2 and 3, byte-identical in both templates and pinned by `tests/reviewer-templates/run-tests.sh` (plan-mandated)
- [I4] user-decision — the tie-break "ask: can its truth be read from the repository? If not, it is a harness property" (multi-code-review/reviewer-prompt.md:69-71, multi-doc-review/reviewer-prompt.md:47-49) is wider than the runtime-behaviour definition before it: a claim about a library, an operating system, or a remote service also fails the test and becomes a harness claim whose only probe (a network request) every constraint forbids, so it is rejected `not runnable here` and never applied while a second reviewer applies it as an ordinary finding; suggested change: scope the tie-break to the agent runtime; same plan-mandated template text as [I3] (plan-mandated)
- [M1] decided (user): standing rule — fix: "the lowest-numbered source that carries one" in the decided consolidation sentence (multi-code-review/SKILL.md:395-398, multi-doc-review/SKILL.md:143-146) could mean any source with a `harness:` field or only an `untested` one; now "the lowest-numbered source tagged `untested`" in both files, spec and plan synced
- [M1] fixed — consolidation sentence disambiguated in both SKILL.md files → 6100d11356027836b6d912e423a85c08ddbc48f8
- [M2] carried — `<probe>` copied verbatim into a line whose `<reason>` is "the last parenthesised clause" and which orchestrating-development lists one per line breaks when the reviewer's probe text spans two lines or ends with its own parenthesised clause (multi-code-review/SKILL.md:487-499, multi-doc-review/SKILL.md:214-225; consumers orchestrating-development/SKILL.md:311-314, 355-357); suggested change: one-physical-line reviewer field, controller collapses newlines (decided wording; the loop's call — carried as Minor)
- [M3] carried — a `not runnable here` rejection is raised again by every blinded later round, so no round is clean and the same probe subagent is re-dispatched each round (multi-code-review/SKILL.md:487-499, :569-573); same family as round 2 [M3], verification 1 [M2], verification 2 [M2] (plan forbids touching convergence rules)
- fix-commit tests: `tests/codex/run-unit-tests.sh` (10 suites passed), `tests/smart-compress/run-tests.sh` (87 passed), `tests/sdd-scripts/run-tests.sh` (193 passed), `tests/reviewer-templates/run-tests.sh` (19 passed) — output in the fix reports
- verification result: the post-loop fix (4f17c47) raised [I1], [I2], [M1] against the decided wording — fixed under the standing rule (6100d11) and re-reviewed in the next cycle; [I3] and [I4] contest plan-mandated template text and are journaled; this addendum's verification is capped at 3 cycles like a round's

## Round 2 verification 5 — Adversarial red-team — fable
**Reviewer verdict:** 0 Critical, 2 Important, 5 Minor
### Dispositions
- [I1] decided (user): standing rule — fix: the decided Guard re-run (multi-code-review/SKILL.md:503-509) had the controller re-run a reviewer's context-read probe from its own context — a different vantage point (main session versus subagent) — and "use your own observation", so a correct finding (for example "subagents do not receive CLAUDE.md") is rejected as contradicted; the sentence now re-runs a context-read probe from the reviewer's position, as one dispatch of a throwaway subagent that writes what it observes, never as a read of the controller's own context
- [I1] fixed — Guard sentence extended in `multi-code-review/SKILL.md`; spec Amendments bullet and plan Task 5 block quote synced → 8f13ea631da464bf058063da26bef4e8f3331454
- [I2] user-decision — same item as verification 4 [I3]: the model-probe sentence of the `### Harness claims` rule (multi-code-review/reviewer-prompt.md:75-76, multi-doc-review/reviewer-prompt.md:53-54) cannot be followed literally — a prompt that names the token puts it in the subagent's context, so the probe contradicts a true claim and supports a false one; suggested change: the probe subagent writes its whole received prompt/context verbatim to the file without being told the token and the controller greps the file itself; plan-mandated verbatim template text of Tasks 2 and 3, pinned by `tests/reviewer-templates/run-tests.sh` (plan-mandated)
- [M1] carried — when the launch-acknowledgement path is hit, the "remaining work" over which the 20 polls are spread may be only a few dispositions before the single fix dispatch, so the poll can still end as `probe subagent did not report` (multi-code-review/SKILL.md:441-458); the decided text accepts this as best-effort (decided wording; carried as Minor)
- [M2] carried — the closed `<reason>` list has no value for "a probe you would not have named yourself" or "a platform without nested dispatch", the clause "the dispatch rule of item 1 fails" is stale, and "copied verbatim" does not say whether `; observed <result>` belongs to `<probe>` for a `tested` finding downgraded to untested (multi-code-review/SKILL.md:479-487, multi-doc-review/SKILL.md:227-235); the six reason values were set by the user's decision (decided wording; carried as Minor)
- [M3] carried — consolidation takes the probe of the lowest-numbered `untested` source even when it is `not settled by one probe` while a higher-numbered source names a runnable probe (multi-code-review/SKILL.md:398, multi-doc-review/SKILL.md:146) (decided wording; carried as Minor)
- [M4] carried — on a platform without nested dispatch every model-probe finding is rejected `not runnable here` each round and rejections never make a round clean, so the loop runs to cap (multi-code-review/SKILL.md:479-487, :607-609); same family as round 2 [M3], verification 2 [M2], verification 4 [M3] (plan forbids touching convergence rules)
- [M5] carried — orchestrating-development/SKILL.md:312-315 lists owed-probe lines of the whole code-review and plan-review logs, so a resumed pipeline re-reports stale owed probes; same item as verification 3 [M1]
- fix-commit tests: `tests/codex/run-unit-tests.sh` (10 suites passed), `tests/smart-compress/run-tests.sh` (87 passed), `tests/sdd-scripts/run-tests.sh` (193 passed), `tests/reviewer-templates/run-tests.sh` (19 passed) — output in the fix reports
- verification result: the cycle-2 fix (6100d11) raised no finding against its own wording; [I1] is a new defect in the decided Guard wording, fixed under the standing rule (8f13ea6) and re-reviewed in the third and last cycle of this addendum; [I2] contests plan-mandated template text and is journaled

## Round 2 verification 6 — Adversarial red-team — fable
**Reviewer verdict:** 0 Critical, 2 Important, 4 Minor
### Dispositions
- [I1] user-decision — a finding mis-tagged `harness:` on a premise whose truth can be read from the repository (a defect in `hooks/subagent-guard.js` tagged `untested — run node hooks/subagent-guard.js …`) has no way back to ordinary triage: the probe runs code from the change under review, so it ends `rejected: harness probe not runnable here — … — (would break a constraint)`, non-blocking, and a real code defect becomes an owed line (multi-code-review/SKILL.md:423-439, multi-doc-review/SKILL.md:170-190; reviewer-prompt.md:68 in both templates, whose example list "what a hook injects, what a tool does" invites the mis-tag although its definition already requires that the truth "cannot be read from the repository"); suggested change: a strip rule before item 1 / 3.1 ("a tag on a premise readable from the repository is wrong: strip it and triage normally") and a qualifier in the template examples; the item structure and the template rule are plan-mandated verbatim wording of Tasks 2-5, the template text pinned by `tests/reviewer-templates/run-tests.sh` (plan-mandated)
- [I2] decided (user): standing rule — reject: the decided Guard re-run of a context-read probe "from the reviewer's position, as one dispatch of a throwaway subagent" (multi-code-review/SKILL.md:507-508) is not the reviewer's full dispatch shape (no template, lens, or `description` canary), so its observation can contradict a true `tested` claim and produce a plain `rejected: harness probe — <observation>`, which is not owed; the fix would either have the controller reproduce the reviewer's whole dispatch shape — contradicting the spec's definition of a probe as one self-contained action — or list guard-re-run rejections on the owed line — contradicting the spec's definition of an owed probe as a `not runnable here` rejection; both change `specs/reviewer-harness-claims-design.md` (Definitions, Interfaces and contracts) and both SKILL.md files beyond the rulings of this resume, so the fix is outside the spec's scope; the accepted known limit applies: a wrong re-run observation costs one wrong disposition that the next blinded round re-raises
- [M1] carried — the poll budget is a tool-call count with no time basis; in the nested-controller position the "remaining work" may be one log entry and the 20 polls end before the probe subagent starts (multi-code-review/SKILL.md:452-457, multi-doc-review/SKILL.md:197-202); same family as verification 5 [M1] (decided wording; carried as Minor)
- [M2] carried — "write the round's log entry so far" could be read as appending a partial entry to the on-disk log, which a later resume would read as a real entry (multi-code-review/SKILL.md:453, multi-doc-review/SKILL.md:198); suggested wording: draft the entry, append only once every disposition is known (decided wording; carried as Minor)
- [M3] carried — "A final message that did arrive always wins over the file" has no cut-off for a message that arrives after the disposition is logged (multi-code-review/SKILL.md:455, multi-doc-review/SKILL.md:200) (decided wording; carried as Minor)
- [M4] carried — orchestrating-development/SKILL.md:311-313, 355-357 list owed-probe lines of the whole log, not the current invocation; same item as verification 3 [M1] and verification 5 [M5]
- verification result: the cycle-3 fix (8f13ea6) raised [I2] against its own wording — rejected under the standing rule (reason above); [I1] contests plan-mandated text and is journaled; third and last verification cycle of this addendum — no fix cycle remains, no unreviewed fix remains (every fix commit was reviewed by the cycle after it; the last, 8f13ea6, by this one)
- Harness probes owed: none (no `rejected: harness probe not runnable here` disposition in this invocation)
- open items after this addendum: unresolved 0, user-decision 3 distinct — model-probe sentence of the template rule (verification 4 [I3] = verification 5 [I2]), tie-break sentence of the template rule (verification 4 [I4]), strip rule for a mis-tagged premise (verification 6 [I1]); the four items answered by this resume are closed

_Completed — 2026-08-30 — cap reached — HEAD 8f13ea631da464bf058063da26bef4e8f3331454_
