# Marker Position Tolerance — code review log

_Invocation 1 — 2026-09-06 — N=2 M=2 — BASE..HEAD f112faa..c41d44c — branch feature/marker-position-tolerance — gate: orchestration_

## Round 1 — Correctness & spec alignment — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 0 Important, 2 Minor | r2: 0 Critical, 0 Important, 3 Minor
**Sources mapped:** 5/5
**Reviewer verdict:** 0 Critical, 0 Important, 4 Minor
**Converged:** no
### Dispositions
- [M1] fixed — the two rewritten `## Guard Interaction` bodies (multi-doc-review, multi-code-review) said a report with a sentence above its marker is still exempt, without saying the widened rule governs hook blocking only while report usability still needs the marker on the first line; a qualifying clause was added to both → a79242e ← 2/2: r1:M1, r2:M1
- [M2] carried — the 15-line return cap now has no receiver-side effect and an over-long controller return is accepted with no trace; reviewer proposes the orchestrator log a one-line observation. Not fixed: the plan mandates the acceptance behaviour (Global Constraint 4) and a new logging behaviour is beyond this branch's scope ← 1/2: r1:M2
- [M3] fixed — the `MARKER_SEARCH_LINES` comment justified the bounded window with a case the prefix-match predicate already excludes (a marker quoted inside a finding line); reworded to name the real over-exemption case, a marker at the start of a line further down → a79242e ← 1/2: r2:M2
- [M4] carried — `tests/orchestrating-development/run-tests.sh:289` asserts the 10-non-blank-line phrase over the whole `## In-run rulings` range rather than the `**Lost returns.**` paragraph, so the pin is weaker than its label. Not fixed: the assertion's range is plan-mandated (Task 3, Contract item 3) ← 1/2: r2:M3
- [CF1] carried — task-1 report's RED list names 11 failures where the plan's Step 2 predicts 12; both reviewers recommend ship-as-is, the plan's own Contract note explains the difference and nothing depends on the count
- [CF2] carried — `tests/codex/test-subagent-guard.js:465,:474`, the two blank-line cases pass against the unmodified hook; both reviewers recommend ship-as-is, they are regression guards for "blanks do not consume the window"
- [CF3] carried — `tests/codex/test-subagent-guard.js:439-442`, the whole-file negative wording assertion; both reviewers recommend ship-as-is, it is the plan-mandated form and cannot produce a false pass
- [CF4] carried — residual exposure: a marker at the start of a line inside the first 10 non-blank lines escapes leakage detection; both reviewers recommend ship-as-is, the bounded window is the documented, deliberate mitigation
- [CF5] fixed — the colloquial clause "and keep saying so" in the Return contract bullet replaced with "and that instruction does not change"; both reviewers recommended fix-before-merge → a79242e
- [CF6] fixed — the long measurement sentence in `skills/orchestrating-development/SKILL.md` `## Guard Interaction` split into short literal sentences; one reviewer recommended fix-before-merge, the other ship-as-is, so the corroborated caution rule selects fix-before-merge → a79242e
- [CF7] carried — the six rewritten passages copy the plan's reference wording nearly verbatim; both reviewers recommend ship-as-is, the plan's Body-authority note permits it

## Round 2 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 2 Important, 2 Minor | r2: 0 Critical, 2 Important, 3 Minor
**Sources mapped:** 9/9
**Reviewer verdict:** 0 Critical, 2 Important, 4 Minor
**Converged:** no
### Dispositions
- [I1] fixed — the `**Lost returns.**` passage stated the hook's 10-non-blank-line window but its loss test still read "arrives without the marker line" with no position rule, so two orchestrators could accept or lose the same fork return; the loss sentence now names the window explicitly → 33d38b0 ← 2/2: r1:I1, r2:I1
- [I2] user-decision — taking the FIRST qualifying marker line in the window as the report start silently ignores everything above it, so a controller that quotes a marker line or corrects a draft return has its wrong block parsed; both reviewers propose taking the last qualifying line or requiring a recognised leading token (plan-mandated) — at skills/orchestrating-development/SKILL.md:170 — clause: Global Constraints "With more than one such line, the first begins the report; everything above it is ignored." ← 2/2: r1:I2, r2:I2
- [M1] fixed — the `MARKER_SEARCH_LINES` comment read as if the bounded window closed the indented or fenced quotation case, while such a quotation inside the window still exempts; the comment now records that accepted residual → 33d38b0 ← 2/2: r1:M1, r2:M1
- [M2] carried — the two negative wording assertions ban the bare phrases `opens with` and `hang the dispatch` over whole files, so an unrelated future comment fails the suite with a message pointing at the wrong sentence. Not fixed: the `tests/codex/test-subagent-guard.js` form is plan-mandated (Task 1, Contract item 3) ← 1/2: r1:M2
- [M3] carried — the 15-line return cap now has no receiver-side effect, so an oversized controller return enters the orchestrator context with no trace; reviewer proposes recording the return's line count in the orchestration log. Not fixed: the acceptance behaviour is plan-mandated (Global Constraint 4) and the new logging is beyond this branch ← 1/2: r2:M2
- [M4] carried — the "Measured on 2026-09-06" sentence in `## Guard Interaction` carries no pointer to where the measurement is recorded and does not name the dispatch shape measured. Not fixed: the probe run, its method and its observation are recorded in this run's own spec at `docs/superpowers-orchestrator/2026-09-06-marker-position-tolerance/specs/marker-position-tolerance-design.md:67-79`, and a citation of a repository-local topic folder inside a shipped skill file would not resolve in any other installation (harness field dropped: repository-readable) ← 1/2: r2:M3

## Round 2 verification 1 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 2 Important, 3 Minor | r2: 0 Critical, 1 Important, 3 Minor
**Sources mapped:** 9/9
**Reviewer verdict:** 0 Critical, 2 Important, 6 Minor
### Dispositions
- [I1] user-decision — taking the FIRST qualifying marker line in the window as the report start makes a superseded draft block authoritative and removes the retry that used to catch it; both reviewers propose taking the last qualifying line or declaring a second marker line malformed. Same open item as round 2 [I2] (plan-mandated) — at skills/orchestrating-development/SKILL.md:170 — clause: Global Constraints "With more than one such line, the first begins the report; everything above it is ignored." ← 2/2: r1:I1, r2:I1
- [I2] user-decision — with the 15-line cap no longer a malformed condition, no rule bounds WHERE in the return a consumed field is read, so an appendix line holding `unresolved=2` below a `unresolved=0` token block can flip a stop rule; reviewer proposes reading consumed fields only from the marker line and the 14 lines below it (plan-mandated) — at skills/orchestrating-development/SKILL.md:176 — clause: Global Constraints "The malformed list stays closed: no marker line in the window, no leading token, or a consumed field (`tasks=`, per-task numbers, `rounds=`, `outcome=`," ← 1/2: r1:I2
- [M1] carried — the branch leaves three marker predicates (prefix over a window, whole-line equality over a window, first-line exact) and an agent may carry the wrong one across passages; reviewer proposes naming the predicate once and referencing it. Not fixed: a cross-skill refactor is beyond this branch ← 1/2: r1:M1
- [M2] carried — the "Measured on 2026-09-06" sentence cites no artifact and the negative wording pin now makes restoring the stronger claim a test failure. Not fixed: the probe run is recorded in this run's own spec at `docs/superpowers-orchestrator/2026-09-06-marker-position-tolerance/specs/marker-position-tolerance-design.md:67-79`, and a repository-local citation inside a shipped skill file would not resolve in another installation (harness field dropped: repository-readable) ← 1/2: r1:M2
- [M3] carried — the hook exemption widens for all three markers while only the orchestrator's controller-return acceptance widens with it, so on the two review paths the widening only enlarges the leakage surface; reviewer proposes widening the usability rules or narrowing the hook per marker. Not fixed: the plan's Task 4 "Does NOT cover" excludes the receiver-side reviewer rules and Global Constraint 5 forbids the hook a second condition ← 1/2: r1:M3
- [M4] fixed — the loss sentence written in round 2 counted its window in the completion notice while the Return contract bullet and the hook count it in the final message; the sentence now names the reviewer's final message → 6dd6ae6 ← 1/2: r2:M1
- [M5] fixed — `skills/researching-prior-art/SKILL.md` `## Guard interaction` stated the widened rule without the usability caveat its two sibling sections gained in round 1, while the research controller still discards a report whose marker is not its first line; the caveat was added → 6dd6ae6 ← 1/2: r2:M2
- [M6] carried — the two negative wording pins forbid the bare phrases `opens with` and `hang the dispatch` over whole files, so an unrelated future sentence fails a test whose name points elsewhere. Not fixed: the `tests/codex/test-subagent-guard.js` form is plan-mandated (Task 1, Contract item 3) ← 1/2: r2:M3

## Round 2 verification 2 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 1 Important, 2 Minor | r2: 0 Critical, 1 Important, 3 Minor
**Sources mapped:** 7/7
**Reviewer verdict:** 0 Critical, 2 Important, 3 Minor
### Dispositions
- [I1] fixed — the loss sentence rewritten in round 2 and verification 1 had attached a 10-non-blank-line window to the lost-return rule, which the plan keeps keyed on the marker's presence (Task 3 "Does NOT cover"), and the window made acceptance narrower than before the branch; the sentence now keys on presence with no window and names its prefix test explicitly → e86c93d ← 1/2: r1:I1
- [I2] user-decision — the hook exemption widened for all three markers while only the orchestrator's controller-return contract widened with it, so a preambled reviewer or research report now passes the hook and is then discarded or re-dispatched by its receiver, which costs more than the block it replaced; reviewer proposes widening the three receiver-side usability rules to the same window, or narrowing the hook to the orchestration marker alone (plan-mandated) — at skills/multi-code-review/SKILL.md:602 — clause: Task 4 "The skill-side usability rules of the other two markers are untouched: `skills/multi-code-review/SKILL.md:602`, `skills/multi-doc-review/SKILL.md:114`," ← 1/2: r2:I1
- [M1] carried — taking the FIRST qualifying marker line makes a controller's self-correction unreachable; this is the same open item already logged `user-decision` as round 2 [I2] and verification 1 [I1], and it is Minor here, so it carries rather than opening a second item ← 2/2: r1:M1, r2:M1
- [M2] fixed — the window qualifier in the loss sentence dangled behind a long subject, so two agents could attach it differently; the same rewrite that fixed [I1] removed the qualifier and stated the test unambiguously → e86c93d ← 2/2: r1:M2, r2:M2
- [M3] rejected: contradicts binding plan text — Global Constraint 5 "**The hook gains no second condition.** It never checks for a leading token, and its exemption only ever widens." — reviewer proposes tracking code-fence toggles in the window scan so a marker quoted inside a fence stops exempting; that is a second condition and it narrows the exemption ← 1/2: r2:M3

_Note — verification cycle 3 was dispatched over e86c93d and interrupted before any reviewer returned a report, so no entry is written for it (Error Handling: a failure before any reviewer report of the round owes no round entry). The fix commit e86c93d therefore ships without a later review round over it._

_Completed — 2026-09-07 — cap reached — HEAD e86c93dfc1a4d49f6d7b0c35f8172d98453947c6_
Secrets found: none

## Post-loop addendum — decisions — 2026-09-07
### Dispositions
- [I2] (round 2) decided (orchestrator): amend plan: Global Constraint 3 keeps the first-match rule and now also requires the orchestrator to record `note: return carried <n> marker lines; parsed from the first` in the orchestration log when the window holds more than one marker line — the amendment is committed and the plan already reads that way; fix it: state that note requirement in the Return contract of skills/orchestrating-development/SKILL.md, beside the first-match sentence, and leave the parse rule itself unchanged.
- [I2] (round 2) fixed — the Return contract bullet now records `note: return carried <n> marker lines; parsed from the first` in the orchestration log when the window holds more than one marker line, so the ignored block leaves a trace; the first-match parse rule is unchanged → 08c6c28
- [I1] (verification 1) decided (orchestrator): amend plan: as on the round 2 line above, Global Constraint 3 keeps first-match and gains the log note; fix it: the same single edit to the Return contract of skills/orchestrating-development/SKILL.md — one fix settles both ids, do not make it twice.
- [I1] (verification 1) fixed — same item as round 2 [I2]; settled by the single Return contract edit made for it, dispatched once → 08c6c28
- [I2] (verification 1) decided (orchestrator): amend plan: Global Constraint 4 now says every consumed field is read only from the marker line and the 14 lines below it, and a value standing below that block is never read — the amendment is committed; fix it: state that bound in the Return contract of skills/orchestrating-development/SKILL.md, next to the sentence saying the cap is not a malformed condition.
- [I2] (verification 1) fixed — the Return contract bullet now bounds every consumed field to the marker line and the 14 lines below it, and states that a value below that block is never read; the 15-line cap stays a non-malformed condition → 08c6c28
- [I2] (verification 2) decided (orchestrator): plan governs: "The skill-side usability rules of the other two markers are untouched: `skills/multi-code-review/SKILL.md:602`, `skills/multi-doc-review/SKILL.md:114`," — docs/superpowers-orchestrator/2026-09-06-marker-position-tolerance/plans/marker-position-tolerance.md
- [M1] (verification 2) carried — unchanged; it was already the Minor form of round 2 [I2], which this addendum decides and fixes

_Addendum note — the effective HEAD had moved past this entry's completion marker (e7a4ff8, the plan amendment) before the addendum was written, so the completion marker above is left unchanged, the verification re-review of the fix is skipped, and invocation 2 below reviews it._

_Invocation 2 — 2026-09-07 — N=2 M=2 — BASE..HEAD f112faa..08c6c28 — branch feature/marker-position-tolerance — gate: orchestration_

## Round 1 — Correctness & spec alignment — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 2 Important, 2 Minor | r2: 0 Critical, 1 Important, 3 Minor
**Sources mapped:** 8/8
**Reviewer verdict:** 0 Critical, 2 Important, 3 Minor
**Converged:** no
### Dispositions
- [I1] fixed — the Return contract now orders the note `note: return carried <n> marker lines; parsed from the first`, but `## Orchestration Log Format` listed no such line and the bullet named no entry to attach it to, so the trace had no defined home; the line was added to the log-format block and the bullet now names the phase or batch entry it is appended under → ae59204 ← 2/2: r1:I1, r2:M1
- [I2] user-decision — the rewritten lost-return rule tests every line of a fork's final message with an unbounded prefix match, while both other sides of the change use the first 10 non-blank lines, so a message that merely quotes the reviewer marker at the start of any line counts as a usable return toward the two-usable-returns threshold; both reviewers propose bounding it to the same window, which the plan says is not touched (plan-mandated) — at skills/orchestrating-development/SKILL.md:1432 — clause: Task 3 "The lost-return **rule** of `## In-run rulings` a fork return whose completion notice arrives without the marker line is lost, re-dispatched once, and a second " ← 2/2: r1:I2, r2:I1
- [M1] carried — the "Measured on 2026-09-06" sentence in `## Guard Interaction` names no record of the measurement and no re-test condition. Not fixed: the probe run, its method and its observation are recorded in this run's own spec at `docs/superpowers-orchestrator/2026-09-06-marker-position-tolerance/specs/marker-position-tolerance-design.md:67-79`, and a citation of a repository-local topic folder inside a shipped skill file would not resolve in any other installation (harness field dropped: repository-readable) ← 2/2: r1:M2, r2:M2
- [M2] fixed — `## Guard Interaction` said an unmarked return is answered with `decision: block` without the condition that the message also names a plugin skill, so it contradicted the `**Lost returns.**` paragraph of the same file; the condition was added → ae59204 ← 1/2: r1:M1
- [M3] carried — the plan's Goal sentence says a reviewer report is accepted within the window, while Task 4 leaves the two reviewer-side usability rules on the first line, so the Goal overstates the reviewer side. Not fixed: the reviewer states no code change is needed and the branch follows Task 4; the mismatch is inside the plan's own text ← 1/2: r2:M3
- [CF1] carried — task-1 report's RED list names 11 failures where the plan's Step 2 predicts 12; both reviewers recommend ship-as-is, the plan's Contract note explains the difference and the report is a completed-step artifact
- [CF2] carried — `tests/codex/test-subagent-guard.js:465,:474`, the two blank-line cases pass against the unmodified hook; both reviewers recommend ship-as-is, the CRLF sibling carries the discriminating comment
- [CF3] carried — `tests/codex/test-subagent-guard.js:439-442`, the whole-file negative wording assertion; both reviewers recommend ship-as-is, it is the plan-mandated form and a self-pin the same task owns
- [CF4] carried — residual exposure: a marker quoted at the start of a line inside the first 10 non-blank lines escapes leakage detection; both reviewers recommend ship-as-is, the bounded window is the documented mitigation. Both add that the unbounded form of the same exposure on the fork path is [I2] above
- [CF5] carried — the colloquial clause "and keep saying so"; both reviewers recommend ship-as-is, it was already fixed in invocation 1
- [CF6] carried — the long measurement sentence in `## Guard Interaction`; both reviewers recommend ship-as-is, it was already split in invocation 1; its precision problem is [M1] above
- [CF7] carried — the six rewritten passages copy the plan's reference wording nearly verbatim; both reviewers recommend ship-as-is, the plan's Body-authority note permits it

## Round 2 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 2 Important, 3 Minor | r2: 0 Critical, 2 Important, 4 Minor
**Sources mapped:** 11/11
**Reviewer verdict:** 0 Critical, 3 Important, 5 Minor
**Converged:** no
### Dispositions
- [I1] user-decision — with preamble now tolerated, a controller that quotes an earlier return above its own has the QUOTED block parsed: both reviewers give a concrete message whose first marker line carries a stale `BLOCKED` or a stale `unresolved=0`, so a stop rule is skipped or a completed batch discarded, and one adds that a consumed field appearing twice inside the 15-line block has no stated tie-break; both propose selecting the marker line whose next non-blank line carries a recognised leading token (plan-mandated) — at skills/orchestrating-development/SKILL.md:169 — clause: Global Constraints "With more than one such line, the first begins the report; everything above it is ignored." ← 2/2: r1:I1, r2:I2
- [I2] user-decision — the lost-return rule tests every line of a fork's final message with an unbounded prefix match, so a message that quotes the reviewer marker anywhere counts as a usable return toward the two-usable-returns threshold; one reviewer adds that no `VERDICT:` presence test exists either. Same open item as round 1 [I2] of this invocation, at Important again; both propose bounding it to the first 10 non-blank lines, which the plan says is not touched (plan-mandated) — at skills/orchestrating-development/SKILL.md:1435 — clause: Task 3 "The lost-return **rule** of `## In-run rulings` a fork return whose completion notice arrives without the marker line is lost, re-dispatched once, and a second " ← 2/2: r1:M2, r2:I1
- [I3] fixed — a return carrying two adjacent marker lines was malformed, because the first non-blank line below the first marker was the second marker and carried no leading token, which contradicted the note rule that presumes such a return parses; further lines equal to the marker are now skipped when the leading token is located → df23bd9 ← 1/2: r1:I2
- [M1] fixed — the hook's prefix match exempts a marker line carrying trailing text and a shipped guard test pins that shape, while the Return contract's equality test calls the same return malformed; the contract now states that content after the marker on the marker line is malformed even though the hook tolerates it → df23bd9 ← 2/2: r1:M1, r2:M4
- [M2] carried — `tests/codex/test-subagent-guard.js:440` scans the whole hook source for the phrase `opens with` while its message speaks of comments, so an unrelated future line fails the suite. Not fixed: the assertion's form is plan-mandated (Task 1, Contract item 3) ← 1/2: r1:M3
- [M3] carried — the CRLF blank-line case cannot distinguish `trim()` from `trimStart()`, because `'\r'.trimStart()` is the empty string and the match is a prefix test, so the trailing-trim half of Global Constraint 10 is pinned by no test; the reviewer ran both variants and observed the same result. Not fixed: the test case, the hook comment and Task 1's Contract all state the claim, and Task 1's Contract mandates that case ← 1/2: r2:M1
- [M4] fixed — the field-read bound said "the 14 lines below it" in a bullet that declares "non-blank" throughout, so two agents could count raw or non-blank lines and a value at raw line 25 could still be read; the bound now names raw lines and excludes itself from the "non-blank" qualifier → df23bd9 ← 1/2: r2:M2
- [M5] carried — the widened exemption applies to every subagent, not only ones with a report contract, and the hook comment states the residual for a quotation "further down the message" without naming the in-window case for an ordinary implementer subagent. Not fixed: the reviewer's alternative is to gate the exemption on the dispatch label, which Global Constraint 5 forbids, and the comment already records the accepted residual ← 1/2: r2:M3

## Round 2 verification 1 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 1 Important, 3 Minor | r2: 0 Critical, 3 Important, 4 Minor
**Sources mapped:** 11/11
**Reviewer verdict:** 0 Critical, 3 Important, 6 Minor
### Dispositions
- [I1] fixed — the duplicate-marker clause written in round 2 promised that a window holding several marker lines always reaches the note rule, while the rule skips only lines EQUAL to the marker, so an ordinary sentence between two marker lines still made the return malformed; the promise was narrowed to what the rule delivers and the malformed case is now stated → 7be9b15 ← 2/2: r1:I1, r2:I1
- [I2] rejected: plan governs (loop decision) — "With more than one such line, the first begins the report; everything above it is ignored. (amended by ruling 1)" — reviewer proposes parsing from the last marker line, or declaring a window holding two complete returns malformed, so that a quoted superseded return cannot be the one parsed; ruling 1 of this run kept first-match and answered the silence with the log note ← 1/2: r2:I2
- [I3] rejected: plan governs (loop decision) — "The skill-side usability rules of the other two markers are untouched: `skills/multi-code-review/SKILL.md:602`, `skills/multi-doc-review/SKILL.md:114`," — reviewer proposes widening the two reviewer-side usability rules or narrowing the hook to the orchestration marker; ruling 3 of this run kept the plan and recorded that the guard's block text never mentions the marker, so the block never converted an unusable preambled report into a usable one (harness field dropped: repository-readable) ← 1/2: r2:I3
- [M1] fixed — "a longer report is **not** malformed" and the new field-read bound gave opposite answers for a consumed field on raw line 16; the cap sentence now says "not malformed on its length alone" and the bound states that a field standing outside the block counts as absent, which the existing malformed list already covers → 7be9b15 ← 2/2: r1:M3, r2:M2
- [M2] fixed — the note had no destination for a return whose leading token is `BLOCKED` or that produces a ruling, because those make a `## STOPPED` or `## RULING` entry and no phase entry; the note is now appended under whatever orchestration-log entry the return produces → 7be9b15 ← 1/2: r1:M1
- [M3] carried — the lost-return rule tests every line of a fork's final message with an unbounded prefix match while both other predicates use the 10-non-blank-line window. Not fixed: this is the same open item already logged `user-decision` as round 1 [I2] and round 2 [I2] of this invocation, and it is Minor here, so it carries rather than opening a second item ← 1/2: r1:M2
- [M4] carried — the Return contract's equality test rejects a marker line carrying its token, while a shipped guard test pins that same shape as exempt; the reviewer proposes accepting a prefix in the contract too. Not fixed: whole-line equality is Global Constraint 3, and round 2 [M1] already added the sentence that states the asymmetry ← 1/2: r2:M1
- [M5] carried — the widened exemption covers every subagent, not only ones with a report contract, so a fix subagent that quotes a report marker early is outside leakage detection. Not fixed: the reviewer's alternative gates the exemption on the dispatch label, which Global Constraint 5 forbids, and the hook comment records the accepted residual ← 1/2: r2:M3
- [M6] carried — the whole-file negative needles `opens with` and `hang the dispatch` fail on future correct text and their messages point elsewhere. Not fixed: both forms are plan-mandated (Task 1 Contract item 3, Task 3 Contract item 3) ← 1/2: r2:M4

## Round 2 verification 2 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 3 Important, 1 Minor | r2: 0 Critical, 2 Important, 5 Minor
**Sources mapped:** 11/11
**Reviewer verdict:** 0 Critical, 3 Important, 6 Minor
### Dispositions
- [I1] rejected: plan governs (loop decision) — "The orchestrator predicate is whole-line equality over its own marker only: a line whose surrounding whitespace is removed equals `<!-- orchestration report -->`, among the first 10 non-blank lines. (amended by ruling 1)" — both reviewers observe that the tolerance widened in one direction only: a token on the marker line, and a sentence between the marker line and its token, are still malformed and can end an unattended run; both propose reading the token from the marker line's remainder or skipping non-token lines to find it. Whole-line equality is the constraint quoted above, and ruling 1 of this run considered and rejected gating the choice on a recognised leading token ← 2/2: r1:I2, r2:I2
- [I2] user-decision — the lost-return rule tests every line of a fork's final message with an unbounded prefix match, so a failure notice that merely quotes the reviewer marker counts as a usable return toward the two-usable-returns threshold and no re-dispatch fires; one reviewer adds that the unbounded form also breaks the plan's own hook-exempt-superset invariant. Same open item as round 1 [I2] and round 2 [I2] of this invocation, at Important again (plan-mandated) — at skills/orchestrating-development/SKILL.md:1449 — clause: Task 3 "The lost-return **rule** of `## In-run rulings` a fork return whose completion notice arrives without the marker line is lost, re-dispatched once, and a second " ← 2/2: r1:I3, r2:I1
- [I3] fixed — `skills/multi-code-review/SKILL.md:816` and `skills/multi-code-review/fix-prompt.md:14` still said the guard blocks an unmarked message "and only reviewers emit that marker", a guarantee the widened exemption removed, because any subagent that starts a line with a report marker inside the window is exempt; both passages now state the widened rule → 82bb81e ← 1/2: r1:I1
- [M1] carried — the "Measured on 2026-09-06" sentence names no platform or version and the negative wording pin makes a later correction a test edit as well. Not fixed: the probe run and its observation are recorded in this run's own spec at `docs/superpowers-orchestrator/2026-09-06-marker-position-tolerance/specs/marker-position-tolerance-design.md:67-79`, a repository-local citation inside a shipped skill file would not resolve elsewhere, and the negative pin's form is plan-mandated (Task 3, Contract item 3) (harness field dropped: repository-readable) ← 1/2: r1:M1
- [M2] fixed — the `## Guard Interaction` sentence written in round 1 said the guard blocks when the message "names a plugin skill", while every violation pattern requires an action verb beside the name or a `Skill(` call form; the sentence now reuses the accurate phrasing the same section already carries → 82bb81e ← 1/2: r2:M1
- [M3] carried — the widened exemption leaves no trace: a message exempted by a marker that is not on its first line is never recorded, so a bypass cannot be audited afterwards; reviewer proposes a new log entry kind in the hook. Not fixed: a new hook behaviour is beyond this branch, whose scope is the exemption condition and the prose that documents it ← 1/2: r2:M2
- [M4] fixed — the note's count read "the count of marker lines in the window", unqualified, so a preamble quoting another skill's marker gave two orchestrators two different counts; the count now names lines equal to `<!-- orchestration report -->` → 82bb81e ← 1/2: r2:M3
- [M5] fixed — the `## Orchestration Log Format` sample showed the note inside the Phase 3 entry with nothing marking it conditional, so it read as part of the entry's fixed shape; the sample line now says it is written only when the window held more than one marker line → 82bb81e ← 1/2: r2:M4
- [M6] carried — the two negative wording pins match a common English phrase over a whole file rather than over the passage they guard. Not fixed: both forms are plan-mandated (Task 1 Contract item 3, Task 3 Contract item 3) ← 1/2: r2:M5

## Round 2 verification 3 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 3 Important, 2 Minor | r2: 0 Critical, 1 Important, 3 Minor
**Sources mapped:** 9/9
**Reviewer verdict:** 0 Critical, 3 Important, 4 Minor
### Dispositions
- [I1] user-decision — the lost-return rule tests every line of a fork's final message with an unbounded prefix match and `## In-run rulings` defines a usable return only as one that is not lost, so a fork that failed and merely quotes its own return contract is counted usable and the `VERDICT:` field of the ruling cannot be filled from it; one reviewer proposes requiring an `ITEM:` and a `VERDICT:` line as well, both propose the 10-non-blank-line window. Same open item as round 1 [I2], round 2 [I2] and verification 2 [I2] of this invocation (plan-mandated) — at skills/orchestrating-development/SKILL.md:1450 — clause: Task 3 "The lost-return **rule** of `## In-run rulings` a fork return whose completion notice arrives without the marker line is lost, re-dispatched once, and a second " ← 2/2: r1:I1, r2:M1
- [I2] unresolved: verification cap — the `## Guard Interaction` sentence this loop wrote in round 1 and refined in verification 2 states a false necessary condition: it says an unmarked message is blocked only when it pairs an action verb with a skill name, while `hooks/subagent-guard.js` also blocks four verb-free patterns (`Skill(superpowers…`, `skill: <name>`, `I'm using the … skill`, `Invoke the superpowers-…`), so an orchestrator would rule the guard out as the cause of a repeated controller turn; the `**Lost returns.**` sentence carries the same inaccuracy in the opposite direction. The third verification cycle is the last one available, so no fix may be dispatched for it here — at skills/orchestrating-development/SKILL.md:2280 — clause: none ← 2/2: r1:I2, r2:I1
- [I3] rejected: plan governs (loop decision) — "The orchestrator predicate is whole-line equality over its own marker only: a line whose surrounding whitespace is removed equals `<!-- orchestration report -->`, among the first 10 non-blank lines. (amended by ruling 1)" — reviewer proposes taking the remainder of a marker line that carries trailing content as the leading-token line, so that a one-line return the hook exempts is not malformed; the constraint quoted above fixes whole-line equality, and ruling 1 of this run rejected token-gating ← 1/2: r1:I3
- [M1] carried — the `## Orchestration Log Format` sample now carries a prose parenthetical inside a fenced block whose every other line is a literal shape, and the note has no destination for a malformed return, which produces no entry at all. Not fixed: the third verification cycle is the last available, so a fix made here would ship without a review round over it ← 1/2: r1:M1
- [M2] carried — `skills/multi-code-review/SKILL.md:815` and `skills/multi-code-review/fix-prompt.md:13` carry the same narrowed description as [I2] and omit the four verb-free patterns. Not fixed: same cause as [I2], and the reviewer notes the preamble never reaches the fix subagent because `fill-prompt.js` writes only the fenced body ← 1/2: r1:M2
- [M3] carried — the two reviewer-prompt notes state the widened hook rule without saying that a report whose first line is not the marker is still discarded as unusable, so a reviewer could act on a tolerance its own loop does not grant. Not fixed: ruling 3 of this run kept the two receiver-side usability rules untouched, and a fix made in the last available cycle would ship unreviewed ← 1/2: r2:M2
- [M4] carried — the fix-prompt paragraph that forbids naming a plugin skill now ends by describing the marker exemption, which states a bypass to the one agent the prohibition targets; reviewer proposes adding an explicit prohibition on quoting a marker to obtain the exemption. Not fixed: a new rule for the fix subagent is beyond this branch, and the last verification cycle is spent ← 1/2: r2:M3

_Completed — 2026-09-07 — cap reached — HEAD 82bb81ef504c7f00c5356ed002e687f067483cef_
Secrets found: none

## Post-loop addendum — decisions — 2026-09-07

- [I2] (round 1) decided (orchestrator): amend plan: Task 3's Does-NOT-cover entry now says the lost-return rule's marker test is bounded to the same first 10 non-blank lines as every other predicate of this change, because rewriting its sentence turned an opens-with test into an every-line prefix test and so changed the rule after all — the amendment is committed and the plan already reads that way; fix it: in `skills/orchestrating-development/SKILL.md`, bound the reviewer-return loss test to the first 10 non-blank lines of the reviewer's final message, so a failure notice that quotes the reviewer marker on a later line is still a lost return. Do not add an `ITEM:` or `VERDICT:` presence test — that gap predates this branch and is recorded for the release step.
- [I2] (round 1) fixed — the reviewer-return loss test is now bounded to the first 10 non-blank lines of the reviewer's final message, blank lines skipped and not consuming the budget; it stays a prefix test and no `ITEM:`/`VERDICT:` presence test was added → db7d0e4
- [I2] (round 2) decided (orchestrator): same item as round 1 [I2] of this invocation — the answer names one item and one fix; the plan amendment and the single edit above settle it, do not make the edit twice.
- [I2] (round 2) fixed — same item as round 1 [I2]; settled by the single bounded-window edit, dispatched once → db7d0e4
- [I1] (round 2) decided (orchestrator): amend plan: Global Constraint 4 now says that when a consumed field appears more than once inside the marker line and the 14 lines below it, the first occurrence is its value, matching the first-match rule of constraint 3 — the amendment is committed; fix it: state that tie-break in the Return contract of `skills/orchestrating-development/SKILL.md`. The marker-selection half of this finding is not reopened: ruling 1 decided it after a three-lens review and the clause carries `(amended by ruling 1)`.
- [I1] (round 2) fixed — the Return contract bullet now states that a consumed field appearing more than once inside the marker line and the 14 raw lines below it takes its value from the first occurrence, matching the first-match rule the same bullet applies to a window holding more than one marker line; the read bound, the malformed list and the marker-selection rule are unchanged → db7d0e4
- [M3] (verification 1) carried — unchanged; it was already the Minor form of the lost-return item this addendum decides and fixes.
- [I2] (verification 2) decided (orchestrator): same item as round 1 [I2] and round 2 [I2] of this invocation; settled by the same single edit.
- [I2] (verification 2) fixed — same item as round 1 [I2]; settled by the single bounded-window edit, dispatched once → db7d0e4
- [I1] (verification 3) decided (orchestrator): same item as round 1 [I2], round 2 [I2] and verification 2 [I2] of this invocation; settled by the same single edit, and the reviewer's additional proposal of an `ITEM:`/`VERDICT:` presence test is explicitly refused by the answer.
- [I1] (verification 3) fixed — same item as round 1 [I2]; settled by the single bounded-window edit, dispatched once → db7d0e4
- [I2] (verification 3) decided (orchestrator): fix it: the `## Guard Interaction` sentence and the `**Lost returns.**` sentence must state the blocking condition correctly — the guard answers an unmarked message with `decision: block` when the message matches any of the hook's violation patterns, and four of those patterns need no action verb at all (`Skill(superpowers…`, `skill: <name>`, `I'm using the … skill`, `Invoke the superpowers-…`). Neither sentence may keep the verb-and-skill-name pairing as a necessary condition.
- [I2] (verification 3) fixed — both sentences now say the guard blocks an unmarked message that matches any of its violation patterns, and both name the four patterns that need no action verb; the verb-and-skill-name pairing is no longer a necessary condition in either direction → db7d0e4
- [M2] (verification 3) carried — unchanged; `skills/multi-code-review/SKILL.md:815` and `skills/multi-code-review/fix-prompt.md:13` carry the same narrowed description, and the answer for [I2] names only the two `skills/orchestrating-development/SKILL.md` sentences, so those two passages are outside what this addendum was told to change.

_Addendum note — the effective HEAD had moved past this entry's completion marker (7e48995, the ruling 4 plan amendment) before the addendum was written, so the completion marker above is left unchanged, the verification re-review of the fix is skipped, and invocation 3 below reviews it._

_Invocation 3 — 2026-09-07 — N=2 M=2 — BASE..HEAD f112faa..db7d0e4 — branch feature/marker-position-tolerance — gate: orchestration_

## Round 1 — Correctness & spec alignment — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 0 Important, 6 Minor | r2: 0 Critical, 1 Important, 5 Minor
**Sources mapped:** 12/12
**Reviewer verdict:** 0 Critical, 1 Important, 8 Minor
**Converged:** no
### Dispositions
- [I1] fixed — the step-4 sentence of `skills/multi-code-review/SKILL.md` kept by "one reason stays in this file" had lost the clause that made it a reason, so it described the widened exemption and stated no constraint on the fix subagent at all; a short constraint clause is restored (the fix subagent's final message carries no report marker, so it must not name a plugin skill) without restoring the false "only reviewers emit that marker" guarantee an earlier round removed → 5ac3c41 ← 2/2: r1:M5, r2:I1
- [M1] fixed — the fenced `## Orchestration Log Format` sample carried a prose parenthetical on its `note:` line while every other line of the fence is literal log text, so an orchestrator copying the sample would write the condition into the committed log; the fence now holds the bare note line and the condition is stated in prose below it → 5ac3c41 ← 2/2: r1:M3, r2:M1
- [M2] fixed — the lost-return rule described its window as "the same window every other predicate of this change uses", naming the branch rather than the rule, which leaves a later reader of the shipped file with no referent; it now names the 10-non-blank-line window of the Return contract above → 5ac3c41 ← 2/2: r1:M4, r2:M2
- [M3] fixed — the `## Guard Interaction` sentence said four violation patterns "match with no action verb at all", while two of those four require the words "using" and "Invoke" in their own text; the sentence now states the accurate property, that those four match without pairing an action verb with a plugin skill name → 5ac3c41 ← 1/2: r1:M1
- [M4] fixed — the same passage ended "not blocked only when it matches none of those patterns", whose nearest antecedent is the four verb-free patterns rather than the whole violation set, so read literally it exempted a markerless message that pairs a verb with a skill name; it now names the guard's violation patterns → 5ac3c41 ← 1/2: r1:M2
- [M5] carried — the lost-return rule makes a fork return usable on marker presence alone, with no `VERDICT:` requirement, unlike the reviewer-report usability rule of `skills/multi-code-review/SKILL.md`. Not fixed: the orchestrator's answer on the lost-return item explicitly refused an `ITEM:` or `VERDICT:` presence test, recording that the gap predates this branch and belongs to the release step; the reviewer itself recommends no change here ← 1/2: r1:M6
- [M6] rejected: prior fix governs — the reviewer asks the loss test to name the completion notice instead of the reviewer's final message, for consistency with the rest of the paragraph. Invocation 1 verification 1 [M4] deliberately moved that test onto the final message, because both the Return contract and the hook count their window in the final message; the paragraph's remaining "completion notice" references name a different object on purpose (the notice reports that the reviewer failed), so the proposed change would reintroduce the defect that fix removed ← 1/2: r2:M3
- [M7] carried — `skills/multi-code-review/fix-prompt.md` is modified by the branch but appears in neither the plan's File Structure table nor any task's Files list, so a later reader reconstructing the change from the plan will not know the file moved. Not fixed: the subject is the plan's own text, and this loop never edits plan text — the orchestrator's amendment route is the only one ← 1/2: r2:M4
- [M8] fixed — `skills/multi-code-review/SKILL.md` and `skills/multi-code-review/fix-prompt.md` both described the guard as blocking a message that pairs an action verb with a plugin skill name, an under-broad condition that misses the four name-only patterns; both now word the condition so it covers the whole violation set. Dispatched in this round's fix list under the id `M5` → 5ac3c41 ← 1/2: r2:M5
- [CF1] carried — task-1 report's RED list names 11 failures where the plan's Step 2 predicts 12; both reviewers recommend ship-as-is, the plan's own Contract note explains the difference and the report is a completed-step artifact
- [CF2] carried — `tests/codex/test-subagent-guard.js:465,:474`, the two blank-line cases pass against the unmodified hook; both reviewers recommend ship-as-is, the CRLF sibling carries the discriminating comment
- [CF3] carried — `tests/codex/test-subagent-guard.js:439-442`, the whole-file negative wording assertion; both reviewers recommend ship-as-is, it is the plan-mandated form and currently green
- [CF4] carried — residual exposure: a marker quoted at the start of a line inside the first 10 non-blank lines escapes leakage detection; both reviewers recommend ship-as-is, the bounded window is the documented mitigation and the hook comment records it
- [CF5] carried — the colloquial clause "and keep saying so"; both reviewers recommend ship-as-is and both observe it was already resolved on this head, the bullet now reading "and that instruction does not change"
- [CF6] fixed — the `## Guard Interaction` sentences were long and mixed several clauses; one reviewer recommends fix-before-merge together with [M3] and [M4], which sit in the same two sentences, and the section is now split into shorter sentences with every statement it carried preserved. Dispatched in this round's fix list under the id `CF1` → 5ac3c41
- [CF7] carried — the six rewritten passages copy the plan's reference wording nearly verbatim; both reviewers recommend ship-as-is, the plan's Body-authority note permits it and the review fixes have since diverged most of them

## Round 2 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 1 Important, 4 Minor | r2: 0 Critical, 1 Important, 3 Minor
**Sources mapped:** 9/9
**Reviewer verdict:** 0 Critical, 2 Important, 7 Minor
**Converged:** no
### Dispositions
- [I1] rejected: decided in this run — the reviewer gives a concrete trigger for a fork that returns the marker with no verdict and is counted usable, and proposes adding an `ITEM:`/`VERDICT:` presence test to the lost-return rule. The post-loop addendum above records the orchestrator's answer on that same item, which refuses that test in those words and records the gap for the release step; the bounded window it did order is already in place ← 1/2: r1:I1
- [I2] rejected: plan governs (loop decision) — "With more than one such line, the first begins the report; everything above it is ignored. (amended by ruling 1)" — reviewer proposes preferring, among the window's marker lines, the first whose next non-blank non-marker line begins with a recognised leading token, falling back to first-match, so that a quoted or superseded block cannot be the one parsed. Ruling 1 of this run listed that exact option as defensible answer (c) and rejected it after a three-lens review, and the addendum above records that the marker-selection half is not reopened ← 1/2: r2:I1
- [M1] carried — `tests/codex/test-subagent-guard.js:471`'s comment claims the CRLF blank-line case pins the trailing half of the line trim, while `'\r'.trimStart()` is the empty string, so no case in the file distinguishes the two trims. Not fixed: Task 1's Contract states the same claim and mandates that case, so correcting the comment alone would put the test at odds with binding plan text ← 1/2: r1:M1
- [M2] fixed — the duplicate-marker note was to be appended under whatever orchestration-log entry the return produces, while a return that carries two marker lines and is malformed produces no entry at all, leaving the note with no destination; the passage now says no note is written for a malformed return and that the retry's return is noted on its own terms → 4b0f926 ← 1/2: r1:M2
- [M3] fixed — the field-read bound justified its 14 raw lines by "matching the 15-line cap the four controller templates give the controller", while the templates state that cap over the whole final message, which may now carry narration above the marker; the paragraph now says the two counts measure different things and that the read block is the marker line plus 14 raw lines whatever preceded the marker → 4b0f926 ← 1/2: r1:M3
- [M4] carried — the widened window makes an accidental exemption of an unmarked worker more likely in this repository than the hook comment's fenced-quotation example suggests, because this repository's own files carry lines that begin with a bare marker. Not fixed: the reviewer states nothing needs changing in the predicate, and the accepted residual is already recorded in the hook comment ← 1/2: r1:M4
- [M5] fixed — the note's `<n>` counted marker lines inside the window while the leading-token rule skipped further marker lines with no window bound, so a marker on the eleventh non-blank line was skipped and never counted; the passage now states that a marker line outside the window is neither counted nor a reason to write the note → 4b0f926 ← 1/2: r2:M1
- [M6] fixed — the `fix-prompt.md` paragraph that forbids naming a plugin skill ended on the marker exemption with no closing clause, so it read as an escape from its own rule, while the parallel passage in `skills/multi-code-review/SKILL.md` closes the loop; the same closing clause is now on both → 4b0f926 ← 1/2: r2:M2
- [M7] carried — the plan's Step 3 reference block justifies the window bound with a rationale that does not hold under the shipped prefix predicate, and the shipped hook comment states the correct one. Not fixed: the subject is the plan's own text, and this loop never edits plan text — the orchestrator's amendment route is the only one ← 1/2: r2:M3

## Round 2 verification 1 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 2 Important, 1 Minor | r2: 0 Critical, 2 Important, 2 Minor
**Sources mapped:** 7/7
**Reviewer verdict:** 0 Critical, 4 Important, 3 Minor
### Dispositions
- [I1] rejected: plan governs (loop decision) — "amend plan: Task 3's Does-NOT-cover entry now says the lost-return rule's marker test is bounded to the same first 10 non-blank lines as every other predicate" — reviewer gives a fork return that reproduces the output format inside a failure narration and is counted usable, and proposes making an `ITEM:`/`VERDICT:` pair part of the usability test. The decision quoted above ordered the bounded window and nothing more, and the answer it records refuses that presence test in those words, recording the gap for the release step ← 1/2: r1:I1
- [I2] rejected: plan governs (loop decision) — "With more than one such line, the first begins the report; everything above it is ignored. (amended by ruling 1)" — reviewer proposes reading the leading token of every block in the window and declaring the return malformed when two tokens differ. That is an acceptance gate on the token, which ruling 1 of this run listed as defensible answer (c) and rejected after a three-lens review because it narrows what the orchestrator accepts today ← 1/2: r1:I2
- [I3] rejected: plan governs (loop decision) — "The skill-side usability rules of the other two markers are untouched: `skills/multi-code-review/SKILL.md:602`, `skills/multi-doc-review/SKILL.md:114`," — reviewer observes that for the research marker the widened exemption removes a block and its violation-log entry while the consumer still requires a first-line marker, and proposes widening the consumers or narrowing the hook to two markers. Ruling 3 of this run decided that same question and kept the plan; invocation 2 verification 1 [I3] rejected it under this clause already ← 1/2: r2:I1
- [I4] rejected: plan governs (loop decision) — "The orchestrator predicate is whole-line equality over its own marker only: a line whose surrounding whitespace is removed equals" — reviewer proposes making the receiver's marker test a prefix test like the hook's, taking the marker line's remainder as content and skipping narration before the leading token. Ruling 1 of this run rejected token-gating and invocation 2 verification 2 [I1] rejected the prefix proposal under this same clause. The finding's second proposal, letting the single retry name the malformed condition, is against the pre-existing identical-retry rule, which this branch does not touch ← 1/2: r2:I2
- [M1] carried — `tests/codex/test-subagent-guard.js:471-478`'s comment claims the CRLF blank-line case pins the trailing half of the line trim, while `'\r'.trimStart()` is the empty string, so no case in the file distinguishes the two trims. Not fixed: Task 1's Contract states the same claim and mandates that case, so correcting the comment alone would put the test at odds with binding plan text ← 1/2: r1:M1
- [M2] fixed — the receiver-side acceptance sentence said only that surrounding whitespace is removed, while the hook's own comment spells out that a trailing carriage return of a message using CRLF line endings is part of it and Global Constraint 10 requires the same trimming on both sides; the sentence now carries the same parenthetical → 87b98bd ← 1/2: r2:M1
- [M3] carried — `tests/codex/test-subagent-guard.js:438-442`, the whole-file negative wording assertion fails on any future comment containing the phrase it forbids, including a correct one. Not fixed: the assertion's form is plan-mandated (Task 1, Contract item 3) ← 1/2: r2:M2

## Round 2 verification 2 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 0 Important, 2 Minor | r2: 0 Critical, 1 Important, 3 Minor
**Sources mapped:** 6/6
**Reviewer verdict:** 0 Critical, 1 Important, 5 Minor
### Dispositions
- [I1] user-decision — the widened fork lost-return rule states no rule about the text ABOVE the marker and no bound on where a fork's fields are read, while the controller Return contract states both; a fork that writes a sentence above its marker, as this change was built to tolerate, can present two `VERDICT:` lines with no stated tie-break, so one orchestrator reads the preamble's verdict and another the report's. Before this branch such a return was simply lost and re-dispatched. This is the same question rulings 1, 2 and 5 answered for the controller side and that nobody has asked for the fork side; the loop may not answer it, because the decision the addendum records bounded the lost-return change to its window and to nothing else (plan-mandated) — at skills/orchestrating-development/SKILL.md:1466 — clause: Task 3 "The lost-return **rule** of `## In-run rulings` a fork return whose completion notice arrives without the marker line is lost, re-dispatched once, and a second " ← 1/2: r2:I1
- [M1] carried — the Major-Error Stop Policy sign test still reads "carries neither the report marker nor any of these tokens", which one orchestrator reads as presence anywhere and another as the Return contract's in-window marker, so the same preambled return can be recorded under two different stop causes. Not fixed: both readings are fatal and only the recorded cause text differs, the row is a third passage this branch's tasks do not own, and its cause texts are pinned by a wording suite ← 1/2: r1:M1
- [M2] carried — the note rule names `## STOPPED` and `## RULING` entries as destinations while neither entry template shows the `note:` line or fixes its position, so two orchestrators place it differently inside a closed, ordered block. Not fixed: changing those two entry templates is beyond the passages this branch owns, and the Phase 3 sample already shows the note's shape ← 1/2: r1:M2
- [M3] fixed — the sentence written in round 2 said a malformed return produces no orchestration-log entry, while a second malformed return produces a `## STOPPED` entry through the major-error stop, so it contradicted the note rule three lines above it; the claim is now narrowed to the phase entry and states that the `## STOPPED` entry carries no note either, because nothing was parsed → aed9ae3 ← 1/2: r2:M1
- [M4] carried — the field-read block counts 14 raw lines while every other count in the bullet is over non-blank lines, so a controller that separates its report lines with blank lines can push a field out of the block. Not fixed: naming raw lines was itself a fix made in invocation 1 for the opposite defect, and the reviewer's own trigger requires a controller that exceeds its template's cap ← 1/2: r2:M2
- [M5] fixed — both copies of the guard sentence ended "so a message that quotes a marker line at the start of a line is exempt too", dropping the window bound the same sentence sets a few words earlier and so stating something false for a quote past the tenth non-blank line; both now carry the bound → aed9ae3 ← 1/2: r2:M3

## Round 2 verification 3 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 1 Important, 2 Minor | r2: 0 Critical, 1 Important, 2 Minor
**Sources mapped:** 6/6
**Reviewer verdict:** 0 Critical, 2 Important, 3 Minor
### Dispositions
- [I1] user-decision — the widened fork lost-return rule states no bound on where a fork's five consumed lines are read and no rule about the text above the marker, so a fork that writes `VERDICT:` in a preamble above its marker gives two orchestrators two different verdicts, and the wrong one is copied into the ruling record and the Phase 5 report. Same open item as verification 2 [I1] of this invocation, at Important again; the reviewer proposes the same bound and first-occurrence rule that Amendments 2 and 5 gave the controller side (plan-mandated) — at skills/orchestrating-development/SKILL.md:1468 — clause: Task 3 "The lost-return **rule** of `## In-run rulings` a fork return whose completion notice arrives without the marker line is lost, re-dispatched once, and a second " ← 1/2: r1:I1
- [I2] user-decision — the widened exemption is reachable by ordinary content in this repository: 16 files under `skills/` and `docs/` carry a line whose only content is one of the three markers, so an implementer subagent that quotes a template it just edited, inside the first 10 non-blank lines, turns the leakage guard off for its whole message; before this branch only a first-line quotation did that. The reviewer's first proposal, requiring the matching line to carry no leading whitespace, contradicts the global constraint quoted below; its second, recording the residual in the hook comment as this repository's routine quoting case rather than a rare one, is an ordinary fix that the last verification cycle leaves no room to dispatch (plan-mandated) — at hooks/subagent-guard.js:109-117 — clause: Global Constraints "The hook predicate is a prefix match over all three markers, in one predicate: exempt when one of the first 10 non-blank lines, with its surrounding whitespace" ← 1/2: r2:I1
- [M1] carried — both copies of the guard sentence justify the no-skill-names rule with "the fix subagent's final message carries no report marker" immediately after a clause saying a message that quotes a marker early is exempt, so the stated reason is false for a fix subagent that quotes a marker line it changed; both reviewers propose dropping the exemption clause and keeping the directive unconditional. Not fixed: the third verification cycle is the last one available, so a fix made here would ship without a review round over it ← 2/2: r1:M2, r2:M2
- [M2] carried — the clause "so extra marker lines standing immediately below the first one still reach the note above", written before this invocation, contradicts the window bound added in round 2 of this invocation for a marker line on the eleventh non-blank line, so the same return produces a note under one reading and none under the other. Not fixed: same cause as [M1], the last verification cycle is spent ← 1/2: r1:M1
- [M3] carried — block `1b` of `tests/orchestrating-development/run-tests.sh` pins the window, the marker spelling and the cap's new status, but pins nothing about the duplicate-marker note the same bullet now mandates, so a later edit could delete the note requirement with every suite still green. Not fixed: adding a test at the last verification cycle would ship it unreviewed ← 1/2: r2:M1

_Completed — 2026-09-07 — cap reached — HEAD aed9ae329148ce1b70d052f1ca2f00cfc6d772ed_
Secrets found: none

## Post-loop addendum — decisions — 2026-09-07

- [I1] (verification 3) decided (orchestrator): amend plan: Global Constraint 3 now applies its three reading rules to a fork's reviewer return as well — the first line in the window that starts with `<!-- multi-review report -->` begins the report, everything above it is ignored, and the return's `ITEM:`, `VERDICT:`, `REASON:`, `CONTRADICTS:` and `TABLED:` lines are read only from that line and the 24 lines below it, the first occurrence of each being its value; the amendment is committed and the plan already reads that way. Fix it: state those rules in the fork-return text of the `## In-run rulings` section of `skills/orchestrating-development/SKILL.md`, beside the lost-return rule, using the fork's own 25-line cap and not the controller's 15.
- [I1] (verification 3) fixed — the "Lost returns" paragraph of `## In-run rulings` now states the three reading rules for a fork's reviewer return: the first window line starting with the reviewer marker begins the report and everything above it is ignored; `ITEM:`, `VERDICT:`, `REASON:`, `CONTRADICTS:` and `TABLED:` are read only from that marker line and the 24 lines below it, the fork's own 25-line cap and never the controller's 15; and a field appearing twice inside that block takes its first occurrence → a19c4c4
- [I1] (verification 2) decided (orchestrator): same item as verification 3 [I1] of this invocation — one item, one fix; settled by the single edit above, not made twice.
- [I1] (verification 2) fixed — same item as verification 3 [I1]; settled by the single fork-return reading-rules edit, dispatched once → a19c4c4
- [I2] (verification 3) decided (orchestrator): plan governs: "A leaking subagent that emits an exact marker at the start of one of its first 10 non-blank lines is exempt; that was already true at line 1 and the prompt inst" — docs/superpowers-orchestrator/2026-09-06-marker-position-tolerance/plans/marker-position-tolerance.md

_Addendum note — the effective HEAD had moved past this entry's completion marker (1558621, the ruling 7 plan amendment) before the addendum was written, so the completion marker above is left unchanged, the verification re-review of the fix is skipped, and invocation 4 below reviews it._

_Invocation 4 — 2026-09-07 — N=2 M=2 — BASE..HEAD f112faa..a19c4c4 — branch feature/marker-position-tolerance — gate: orchestration_

## Round 1 — Correctness & spec alignment — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 2 Important, 3 Minor | r2: 0 Critical, 3 Important, 4 Minor
**Sources mapped:** 12/12
**Reviewer verdict:** 0 Critical, 5 Important, 6 Minor
**Converged:** no
### Dispositions
- [I1] fixed — the leading-token sentence contradicted itself: its operative half read the first non-blank line below the marker that is not itself a marker, while its explanatory half said an ordinary non-blank line between two marker lines makes the token absent and the return malformed; the clause is now conditional, so such a line is itself read as the token line and the return is malformed only when that line carries no leading token → 7698180 ← 1/2: r1:I1
- [I2] user-decision — the same bullet states the 15-line cap twice and incompatibly: "The 15-line cap counts from the marker line, which is line 1 of the 15" and, four lines later, "the templates state that cap over the whole final message, whatever number of lines preceded the marker"; the four templates say `## Return (final message, 15 lines max)`, so the second is the true one and the first misdescribes the instruction it attributes to the controller. The reviewer proposes restating the cap sentence as the templates state it, which is the verbatim wording of binding Global Constraint 4 (plan-mandated) — at skills/orchestrating-development/SKILL.md:195 — clause: Global Constraints "**The 15-line cap counts from the marker line, which is line 1 of the 15, and exceeding it is not a malformed condition.**" ← 1/2: r1:I2
- [I3] fixed — the sentence "Content after the marker on the marker line makes the return malformed under this equality test" added a malformed condition that binding Global Constraint 4 closes and that binding Global Constraint 3 contradicts, since constraint 3 qualifies a line and never makes a non-qualifying line fatal for the whole return; the sentence is now a property of the line, so a line carrying content after the marker is simply not a qualifying marker line and the malformed list stays closed → 7698180 ← 1/2: r2:I1
- [I4] user-decision — the leading-token line is located by an unbounded search ("the first non-blank line below that marker line"), so a `BLOCKED` token standing 20 blank lines below the marker, far outside the marker line plus 14 raw lines, is read as the return's token and stops the run; ruling 2 closed exactly this hole for consumed fields but the token, which selects the phase outcome, is left unbounded. The reviewer proposes bounding the token search to the same block, which contradicts the Task 2 Contract's Must-convey wording (plan-mandated) — at skills/orchestrating-development/SKILL.md:187 — clause: Task 2 "Must convey, all of: the templates still tell the controller to put the marker on its first line; a return is accepted when a line whose surrounding whitespace " ← 1/2: r2:I2
- [I5] rejected: prior decision governs (orchestrator) — the reviewer asks for a clause making a fork return whose 25-line block carries no `VERDICT:` line unusable, so that a quoted marker cannot count toward the two-usable-returns threshold. The orchestrator answer recorded in the post-loop addendum of invocation 2 of this run refused exactly that addition: "Do not add an `ITEM:` or `VERDICT:` presence test that gap predates this branch and is recorded for the release step." The gap is real and predates this branch; it is recorded for the release step, not fixed here ← 1/2: r2:I3
- [M1] carried — `skills/multi-code-review/fix-prompt.md` and the `## Guard Interaction` body of `skills/multi-code-review/SKILL.md` are edited by the branch but appear in no task's Files list and in no verification; both reviewers state the new content is accurate and ask for no behavioural fix, only that the extra files be recorded against the plan. Not fixed: the loop never edits the plan ← 2/2: r1:M2, r2:M2
- [M2] fixed — the fork reading-rules sentence called its read block "the fork's own 25-line cap, never the controller's 15-line cap", conflating a read window with a whole-message cap that the fork prompt states over the whole final message; the apposition is replaced by the controller side's own disambiguation → 7698180 ← 1/2: r1:M1
- [M3] fixed — the note rule was spliced: "appended under whatever orchestration-log entry that return produces" stood five lines away from its subject, behind an em-dash aside whose nearest noun was "a marker line standing outside the window"; the placement clause now closes the first sentence and the outside-the-window exception is its own sentence → 7698180 ← 1/2: r1:M3
- [M4] fixed — the fork block was stated as "that marker line and the 24 lines below it" while the controller block spells out "the 14 raw lines below it, blank lines included", so an executing agent could count the fork's 24 lines as non-blank lines; the fork side now carries the same raw-lines qualifier, in the same edit as [M2] → 7698180 ← 1/2: r2:M1
- [M5] carried — the fork side copies the first-match rule and the field-read bound but not Global Constraint 3's `note: return carried <n> marker lines` requirement, so a fork return with a quoted marker above its real one leaves no trace. Not fixed: Amendment 7 lists three reading rules and the note is not among them, so adding it would add behaviour the plan does not state, and the reviewer's alternative (a sentence saying the fork side deliberately carries no note) states a rule the plan does not state either ← 1/2: r2:M3
- [M6] carried — `GUARD_HANG_OLD_CLAIM='hang the dispatch'` pins only the first half of the superseded claim, so an edit reintroducing "and stall the unattended run" alone would keep the suite green. Not fixed: the loop was told to edit no test file this round, and the pin is a self-pin of Task 3 that a later ordinary fix may amend together with its assertions ← 1/2: r2:M4
- [CF1] carried — task-1 report's RED list names 11 failures where the brief predicts 12; both reviewers recommend ship-as-is, the discrepancy is confined to a sidecar report and the brief's own Contract note explains it.
- [CF2] carried — `tests/codex/test-subagent-guard.js:465,:474`, the two blank-line cases carry no comment naming the wrong implementation they discriminate against; both reviewers recommend ship-as-is, the test names already state the property and both cases carry `VERB_SKILL_BODY`.
- [CF3] carried — `tests/codex/test-subagent-guard.js:439-442`, `!/opens with/.test(source)` is a whole-file negative wording assertion; both reviewers recommend ship-as-is, it is plan-mandated by Task 1 Contract 3 and currently green.
- [CF4] carried — the residual exposure, a quoted marker at the start of a line inside the window escaping leakage detection, is exempt by design; both reviewers recommend ship-as-is, the accepted cost is stated in the `MARKER_SEARCH_LINES` comment.
- [CF5] fixed — "and keep saying so" was already reworded to "and that instruction does not change" before this invocation; both reviewers recommend ship-as-is and report the item resolved on HEAD.
- [CF6] fixed — the `## Guard Interaction` body was one long paragraph mixing a colon-style enumeration with a mid-paragraph "But" clause; one reviewer recommended fix-before-merge and the other ship-as-is, so the corroborated caution rule makes it a fix. The section is now three paragraphs of shorter sentences with no statement added or removed, and every pinned fragment survives → 7698180
- [CF7] carried — the six rewritten passages copy the brief's reference wording essentially verbatim; both reviewers recommend ship-as-is, the plan's Body-authority note permits it.

## Round 2 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 1 Important, 3 Minor | r2: 0 Critical, 1 Important, 4 Minor
**Sources mapped:** 9/9
**Reviewer verdict:** 0 Critical, 1 Important, 6 Minor
**Converged:** no
### Dispositions
- [I1] user-decision — the hook exemption is widened to the first 10 non-blank lines, but the two review skills keep their usability test at "first line is the marker", so the exact message shape the branch newly lets through is discarded downstream while the guard block that previously sent the reviewer back is gone; both reviewers reproduced it (new guard returns `{}`, the pre-change predicate returned `decision: block`) and both note that the plan's Goal line names a reviewer report as well. Proposed fixes are to widen the usability test in both skills to the same 10-non-blank-line window, or to state in both Guard Interaction sections that the retry is now the only repair. Both reviewers label it plan-mandated because Task 4 scopes the skill-side usability rules out (plan-mandated) — at skills/multi-code-review/SKILL.md:1871 — clause: Task 4 "The skill-side usability rules of the other two markers are untouched: `skills/multi-code-review/SKILL.md:602`, `skills/multi-doc-review/SKILL.md:114`, `skills/" ← 2/2: r1:I1, r2:I1
- [M1] carried — the `!/opens with/.test(source)` pin of `tests/codex/test-subagent-guard.js:440` is a whole-file negative wording assertion that both a reworded restoration would pass and an unrelated future comment would fail, and its message says "No comment" while the check covers the whole file. Not fixed: the assertion is plan-mandated by Task 1's Contract 3, and round 1 carried the same item as [CF3] on the same ground ← 2/2: r1:M3, r2:M4
- [M2] carried — `assert_file_not_contains` pins the superseded `hang the dispatch` claim per line with `grep -qF`, while every positive pin of the same block folds line wraps, so the claim reintroduced across a wrap would pass. Not fixed: it needs a new `assert_folded_not_contains` helper in a test file, and round 2 is the cap round, so the change would ship without a review round over it ← 1/2: r1:M1
- [M3] carried — the fork reading rules copy the first-marker-wins and first-field-wins rules but not Global Constraint 3's `note: return carried <n> marker lines` trace, so a fork parsed from a quoted marker leaves nothing in the orchestration log. Not fixed: same reason as round 1 [M5], Amendment 7 lists three reading rules and the note is not among them ← 1/2: r1:M2
- [M4] carried — `hasReportMarker` is evaluated before the violation loop, so an exempt message never reaches `logViolation` and the widened exemption also widens the blind spot in the violations log; the reviewer proposes an `exempted` record so the leakage rate stays observable. Not fixed: the plan's Task 1 changes the predicate only, adding a log record is new behaviour, and the cap round leaves no review over it ← 1/2: r2:M1
- [M5] carried — the leading-token search has no upper bound while every consumed field is bounded to the marker line and the 14 raw lines below it, so a token found 15 blank lines down is present for the token test and its fields absent for the field test. Not fixed: this is the Minor form of round 1 [I4], which is already open for the orchestrator's decision, and the loop may not change the Task 2 Contract wording it collides with ← 1/2: r2:M2
- [M6] carried — the `note: return carried <n> marker lines; parsed from the first` line records only a count, so a reader cannot tell whether the parsed leading token came from the controller's own report or from a quoted nested return; the reviewer proposes adding the parsed token to the note. Not fixed: the note's wording is fixed verbatim by binding Global Constraint 3 ← 1/2: r2:M3

_Completed — 2026-09-07 — cap reached — HEAD 76981803479553fb0e9a295b7845f91ac9328d57_
Secrets found: none

## Post-loop addendum — decisions — 2026-09-07

- [I2] (round 1) decided (user): plan governs: "**The 15-line cap counts from the marker line, which is line 1 of the 15, and exceeding it is not a malformed condition.**" — docs/superpowers-orchestrator/2026-09-06-marker-position-tolerance/plans/marker-position-tolerance.md
- [I4] (round 1) decided (user): amend plan: Task 2's Contract now bounds the leading-token search to the marker line and the 14 raw lines below it, the same block every consumed field is read from, and a return whose block holds no token line is malformed for the token's absence; the amendment is committed as Amendment 10 and the plan already reads that way. Fix it: state that bound in the `**Return contract:**` bullet of `## Controller Dispatch Rules` in `skills/orchestrating-development/SKILL.md`, so the token search is no longer unbounded.
- [I4] (round 1) fixed — the `**Return contract:**` bullet now searches for the leading token only inside the marker line and the 14 raw lines below it, the same block every consumed field is read from, and states that a token standing below that block is not read, so the return is malformed for the token's absence exactly as an absent token is today → 0d73bf5
- [I1] (round 2) decided (user): amend plan: Task 4's Does-NOT-cover entry now brings the two skill-side usability rules into scope and widens them to the same 10-non-blank-line window as the hook, while `skills/researching-prior-art/` keeps its first-line rules; the amendment is committed as Amendment 11 and the plan already reads that way, superseding ruling 3 of this run. Fix it: widen the usability rule of `skills/multi-code-review/SKILL.md` and of `skills/multi-doc-review/SKILL.md` so a report whose marker starts one of its first 10 non-blank lines is usable, and leave the unusable-report path unchanged.
- [I1] (round 2) fixed — both review skills now call a report usable when the marker line `<!-- multi-review report -->` starts one of its first 10 non-blank lines and a Verdict block is present, matching the window `hooks/subagent-guard.js` exempts; the unusable-report path (one identical retry, then `inconclusive`) is unchanged and `skills/researching-prior-art/` is untouched → 0d73bf5

_Addendum note — the effective HEAD had moved past this entry's completion marker (aa7e4d7, the ruling 9 follow-up carrying Amendments 10 and 11) before the addendum was written, so the completion marker above is left unchanged, the verification re-review of the fix is skipped, and invocation 5 below reviews it._

_Invocation 5 — 2026-09-07 — N=2 M=2 — BASE..HEAD f112faa..0d73bf5 — branch feature/marker-position-tolerance — gate: orchestration_

## Round 1 — Correctness & spec alignment — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 3 Important, 2 Minor | r2: 0 Critical, 1 Important, 3 Minor
**Sources mapped:** 9/9
**Reviewer verdict:** 0 Critical, 3 Important, 4 Minor
**Converged:** no
### Dispositions
- [I1] fixed — each `## Guard Interaction` section still ended "the validation step above is unchanged, and a report is still usable only when the marker is its first line", which the same branch had just made false: both files stated two opposite acceptance rules, and a controller following the section would discard a report the validation step accepts, with no guard block left to send the reviewer back. Both sections now say the validation step uses the same 10-non-blank-line window and that only `reviewer-prompt.md` still asks for the marker as the first output line; the headings and the never-remove-the-marker sentences are unchanged → 607c9be ← 2/2: r1:I1, r2:I1
- [I2] fixed — the widened usability rule accepted text above the marker but stated no reading rule, so a report whose preamble quotes an earlier Verdict block and some finding lines could feed two different finding sets into the consolidation step. Both validation steps now state the same rule the plan's Global Constraint 3 gives the orchestrator and fork sides: the first line among the first 10 non-blank lines that starts with the marker begins the report, everything above it is ignored, and the Verdict block and the enumerated findings are read only from that line downward → 607c9be ← 2/2: r1:I3, r2:M1
- [I3] fixed — two passages of `skills/multi-code-review/SKILL.md` still gave "a preamble line before the marker" as an example of a report unusable on format alone, which the widened rule makes usable; both now name a marker that first appears below the report's 10th non-blank line, and the surrounding pointer-failure reasoning is unchanged → 607c9be ← 1/2: r1:I2
- [M1] carried — `skills/researching-prior-art/controller-prompt.md` still discards a research report whose marker is not its first line, so the shape the hook now exempts is dropped there with no retry signal. Not fixed: Amendment 11 keeps `skills/researching-prior-art/` on its first-line rules by name, and Task 4's Does-NOT-cover lists those two paths as untouched ← 1/2: r1:M1
- [M2] carried — `skills/orchestrating-development/SKILL.md` exempts a malformed return from the `note: return carried <n> marker lines` trace, while Global Constraint 3 states the note requirement without such an exemption, so a run stopped by two malformed multi-marker returns records no trace of the ignored blocks. Not fixed: the constraint is binding text and states no rule for the malformed case, so either wording would add behaviour the plan does not state ← 1/2: r1:M2
- [M3] fixed — `skills/multi-code-review/fix-prompt.md` and the fix-dispatch bullet of `skills/multi-code-review/SKILL.md` described the guard's verb-free patterns as "a few name-only forms", while three of the four match on wording other than a bare skill name; both now use the description `skills/orchestrating-development/SKILL.md` already carries → 607c9be ← 1/2: r2:M2
- [M4] fixed — the previous commit's widening left one line of each edited paragraph at about 100 columns in two files that wrap at roughly 72; both paragraphs are re-wrapped with no wording change → 607c9be ← 1/2: r2:M3
- [CF1] carried — task-1 report's RED list names 11 failures where the brief predicts 12; both reviewers recommend ship-as-is, the discrepancy is confined to a sidecar report and the brief's own Contract note explains it.
- [CF2] carried — `tests/codex/test-subagent-guard.js:465,:474`, the two blank-line cases carry no comment naming the wrong implementation they discriminate against; both reviewers recommend ship-as-is, the CRLF sibling case is the discriminating one and carries the comment.
- [CF3] carried — `tests/codex/test-subagent-guard.js:439-442`, `!/opens with/.test(source)` is a whole-file negative wording assertion; both reviewers recommend ship-as-is, it is a plan-mandated self-pin that a later ordinary fix may narrow.
- [CF4] carried — the residual exposure, a quoted marker at the start of a line inside the window escaping leakage detection, is exempt by design; both reviewers recommend ship-as-is, the accepted cost is stated in the `MARKER_SEARCH_LINES` comment.
- [CF5] carried — "and keep saying so" is colloquial; both reviewers recommend ship-as-is and report the item already resolved on HEAD, the bullet now reading "and that instruction does not change".
- [CF6] carried — the `## Guard Interaction` sentence carrying the measurement was long and mixed a colon with a "but" clause; both reviewers recommend ship-as-is and report the item already resolved on HEAD, the section now being several short paragraphs.
- [CF7] carried — the six rewritten passages copy the brief's reference wording essentially verbatim; both reviewers recommend ship-as-is, the plan's Body-authority note permits it and several passages have since diverged through review fixes.

## Round 2 — Adversarial red-team — opus
**Reviewers:** M=2, usable 2/2
**Reviewer verdicts:** r1: 0 Critical, 1 Important, 5 Minor | r2: 0 Critical, 2 Important, 2 Minor
**Sources mapped:** 10/10
**Reviewer verdict:** 0 Critical, 3 Important, 7 Minor
**Converged:** no
### Dispositions
- [I1] user-decision — a controller `BLOCKED` return that quotes the required format in a fenced preamble puts a bare marker line above its real one; the orchestrator then takes the first marker line, reads the closing fence as the token line, finds no leading token and declares the return malformed, although a well-formed report stands two lines lower. The reviewer proposes a token-gated first-match (the first marker line whose own read block yields a leading token, else the first marker line), which it argues is a strict superset of today's rule, while Amendment 1 rejected last-match and token-gating together on the ground that the alternative would narrow what is accepted (plan-mandated) — at skills/orchestrating-development/SKILL.md:187 — clause: Global Constraints "With more than one such line, the first begins the report; everything above it is ignored." ← 1/2: r1:I1
- [I2] fixed — the widened usability test asked only that a Verdict block be present somewhere in the message while the reading rule scoped the read to the marker line downward, so a report whose Verdict block and findings stood above its marker passed as usable with an empty finding set and made the round clean; both skills now require the Verdict block to stand below the qualifying marker line and call a report whose qualifying marker line is its last non-blank line unusable, the retry-then-`inconclusive` path unchanged → f8136a5 ← 1/2: r2:I1
- [I3] rejected: prior decision governs (user) — the reviewer asks the Return contract bullet to state the 15-line cap once, over the whole final message, because the bullet's own two statements of it disagree. That is the same item the user decided at the resume of 2026-09-07, recorded in the post-loop addendum of invocation 4 of this run as `plan governs: "**The 15-line cap counts from the marker line, which is line 1 of the 15, and exceeding it is not a malformed condition.**"`: the constraint stands, no fix is made, and the templates' own cap wording is left for the release step ← 1/2: r2:I2
- [M1] carried — `tests/codex/test-subagent-guard.js:438`, `!/opens with/.test(source)` is a case-sensitive whole-file substring ban that misses the near-synonyms an editor would reintroduce and would fail on any unrelated future occurrence. Not fixed: it is plan-mandated by Task 1's Contract 3 and has been carried on that ground in every invocation of this run ← 1/2: r1:M1
- [M2] carried — `tests/orchestrating-development/run-tests.sh:68`, the `RETURN_MARKER` needle is satisfied by wording that predates this branch, so it cannot fail if a later edit restores the old bullet, while the comment above it claims the three needles stop any such restoration. Not fixed: round 2 is the cap round and the change is a comment-accuracy correction in a test file, so it would ship with no ordinary review round over it ← 1/2: r1:M2
- [M3] carried — the Major-Error Stop Policy row of `skills/orchestrating-development/SKILL.md` still tests whether a message "carries" the report marker, which matches none of the three position tests this branch defines, so one orchestrator reads a marker on the 30th line as present and another as absent, and the two take different paths (retry versus immediate fatal). Not fixed: the row is not changed by this branch and Task 3's Does-NOT-cover limits the branch to the two passages describing the hook's exemption ← 1/2: r1:M3
- [M4] carried — the fork reviewer return copies three of the controller's reading rules but not Global Constraint 3's `note: return carried <n> marker lines` trace, so a fork parsed past an ignored block leaves nothing in the ruling record. Not fixed: same ground as invocation 1 [M5] and invocation 4 [M5]/[M3] — Amendment 7 enumerates three reading rules and the note is not among them ← 1/2: r1:M4
- [M5] carried — `hasReportMarker` splits the whole final message before it needs at most the first 10 non-blank lines, which defeats the loop's own short-circuit. Not fixed: the reviewer states it is neither a regression (the previous implementation copied the whole string too) nor exploitable at the bounded size of a subagent final message ← 1/2: r1:M5
- [M6] fixed — the widened rule named the window but not the comparison, while the hook removes each line's surrounding whitespace and skips a line that is empty afterwards, so a literal reading made an indented marker line and a CRLF marker line unusable in the two skills although the guard exempts them; both rules now state the whitespace removal, the trailing carriage return and the spaces-only-counts-as-blank case, in the wording the Return contract already uses → f8136a5 ← 1/2: r2:M1
- [M7] fixed — no case of the "Marker search window" section pinned a spaces-only line as blank, so narrowing `line.trim()` to `line.trimStart()` would consume the window with such lines and no test would fail; the section gains one case, twelve spaces-only lines above the marker asserted exempt, with no existing case changed → f8136a5 ← 1/2: r2:M2
